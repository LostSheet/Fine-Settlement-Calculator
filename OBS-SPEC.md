# OBS 오버레이 중계 v2 — 계정·로비 구현 명세

> v1(회차마다 방·주소 재발급)은 폐기. 이 문서가 대체한다.
> 결정 경위: 게임컴 주소창은 방송 화면에 찍힌다 — URL에 실린 권한은 유출된 것으로 본다.
> 따라서 **URL은 읽기까지만, 쓰기는 로그인 세션만**. 사람의 신원은 계정(id/pw)이고,
> 파티는 게임 로비 문법(대기실에 모여서 출발)으로 꾸린다.
> 화면 목업: https://claude.ai/code/artifact/1b2ca408-29d3-4ddc-9777-eea8ed21be7d (아트보드 ①~⑦, 부속 2장)

이 문서는 설계 논의가 끝난 결과물이다. 여기 적힌 결정은 재논의 없이 그대로 구현한다.
§8의 문구는 **한 글자도 바꾸지 말고** 그대로 쓴다 (해요체). 배포는 사용자 지시가 있을 때만 하며,
워커와 앱(docs/)을 **같이** 배포한다 (프로토콜이 갈라지면 옛 앱이 죽는다).

## 0. 원칙

1. **로컬이 원본이다.** 진본은 방장(서기) 브라우저의 localStorage. 서버는 릴레이 + 신원부.
2. **URL은 읽기 전용 권한까지만 나른다.** 쓰기(장부 push·자수·수락)는 전부 로그인 세션.
3. **좌석 없음.** 같은 계정이 몇 기기에서 열든 전부 유효. 중복 진입은 막지 않고 무해화한다.
4. **모든 제재는 방 범위·가역.** 영구 차단은 만들지 않는다. 내보내기/거절 = 이 방에서 빠짐, 끝.
5. **방송 화면에 에러를 그리지 않는다.** 오버레이는 실패 시 침묵(투명)이 기본.
6. 비밀번호 원문은 서버에 도착하지 않는다 (클라이언트 선해시).
7. 모집(서버의 `lobby.open`)은 화면이 아니라 **서버에 사는 상태**다. 방장이 화면을 떠나도 유지된다.
   (용어 2026-09-05) 서버·API 이름의 "lobby"는 이 모집 상태를 뜻한다. 화면 이름 **로비**는
   §3.0의 홈이다 — 둘은 다른 것이다.

## 1. 객체와 수명

| 객체 | 만드는 곳 | 수명 |
|---|---|---|
| 계정 (id/pw/닉네임) | 앱 가입 | 무활동 **1년** 삭제. OBS 주소 조회도 활동으로 침 |
| 익명 계정 | [가입 없이 주소 받기] — 앱이 무작위 id/pw로 조용히 만든다 | 위와 같음. 나중에 가입하면 **같은 계정이 그대로 정식 계정**이 된다(주소 안 바뀜) |
| 세션 | 로그인 | **90일**, 쓸 때마다 연장. Bearer 토큰(localStorage). 쿠키 안 씀 |
| 방 | 계정당 하나, 처음 필요할 때 자동 생성 | 계정과 함께. 판 내용(state)만 무활동 90일 삭제(방·멤버십은 유지) |
| 초대 링크 | [+ 새 판 만들기]가 자동 발급(없거나 죽었으면), 방에 하나 | **방장이 앱을 열어 둔 동안(서기 소켓) 유효**, 마지막 서기 소켓이 끊기면 **10분** 뒤 만료 (2026-09-06). 재발급하면 옛 코드 즉시 무효. **판이 있을 때만 통과**(§3.8). (폐기 2026-09-06) 발급 뒤 10분 고정 — 20분 모으다 보면 링크가 죽어 다시 붙여야 했다 |
| 내 방송용 주소(OBS 토큰) | 계정마다 하나 자동 | 영구(재발급 전까지). **읽기 전용** |
| 멤버십(계정↔자리) | 수락될 때 자리에 붙음 | 본인 [나가기]·방장 내보내기·**판의 끝(정산 끝내기·해산)** 까지 (2026-09-06: 파티원은 판에 속한다). 접속이 끊긴 것은 빠진 것이 아니다(`자리 비움`). (폐기 2026-09-06) "정산 끝내기·중단은 안 끊는다" |
| 모집 상태 (서버 lobby.open) | [+ 새 판 만들기]가 열고(= 판 존재 표시) [시작]·판의 끝이 닫는다 (2026-09-06) | 시작 전 판과 같이 산다. 6시간 자동 닫힘 폐기 |
| 판(= 파티) | [+ 새 판 만들기] (2026-09-06: 판은 만들면 생기고 끝내면 없다. 없음 → 시작 전 → 진행 중 → 없음) | [정산 끝내기]는 결과지를 남기고 판을 없앤다. 시작 전엔 [해산]. 없어질 때 명단·자리·이름·숫자·판 이름이 지워지고 **결과지와 기본값(항목·단가·인원)** 만 남는다. 무활동 90일이면 서버가 같은 정리를 한다. (폐기 2026-09-05) 무활동 24시간 자동 중단. (폐기 2026-09-06) "판은 늘 있고 준비 상태로 돌아간다" |
| 판 기록(결과지) | [정산 끝내기] 때 자동 | **로컬 전용**, 읽기 조회만. 최근 20판, 넘치면 오래된 것부터 삭제(수동 삭제는 확인창) |

- 계정 id: 영문·숫자 4~20자(소문자로 정규화). **닉네임: 2~3자**(한글·영문·숫자), 전역 중복 허용,
  변경 가능 — 참여 중인 방의 줄 이름도 따라 바뀐다.
- 초대 링크: `{RELAY}/r/{ROOMID}#j={CODE8}`. 내 방송용 주소: `{RELAY}/o/{TOKEN20}` —
  이 계정이 **지금 들어가 있는 방**(마지막으로 참여·활동한 방 자동)을 비춘다.
  명단에 내 이름(멤버십)이 없으면 침묵.
- 방 주소(`/r/ROOMID`)는 비밀이 아니다. 유효한 초대·세션·OBS 토큰 없이는 판이 안 보인다.
- 문자 알파벳: v1과 같은 혼동 제외 집합 `ABCDEFGHJKMNPQRSTVWXYZ23456789`.

## 2. 계정·보안

- **클라이언트 선해시**: 브라우저가 PBKDF2-SHA256(pw, salt = `"gold-settlement/v1|" + id소문자`,
  310,000회, 32B)를 hex로 보낸다. 서버는 무작위 솔트 16B + SHA-256(salt ‖ 선해시)로 저장.
  crypto.subtle이 없는 환경(비보안 컨텍스트)에서는 로그인을 막고 안내만 한다.
- 비밀번호 재설정 없음 — 가입 화면에 §8 문구로 명시.
- 로그인 실패 레이트리밋: id당 10분 창 30회 → 429. 자수: 계정당 분당 20회 → 429.
- WebSocket 인증은 쿼리 파라미터(`?s=세션` | `?j=초대` | `?o=OBS토큰`) — 브라우저 WS는 헤더를
  못 실어서다. 주소창에 노출되지 않는 내부 URL이다.
- CORS: `*` 유지 + `authorization` 헤더 허용 (쿠키를 안 쓰므로 자격 증명 모드 불필요).
- 화면의 초대 링크·방송용 주소는 **기본 가림**(●●●● + 복사 버튼) — 복구 코드에 쓰던 패턴.

## 3. 한 문법 (확정 — 상태 지도가 원본)

상태 지도: https://claude.ai/code/artifact/6f0ab290-13e1-42ff-94b3-1633d65a39d2
로비 목업: https://claude.ai/code/artifact/b9ba75d4-f1be-4f93-acf5-e4b6ff490f07 ·
초대 두 단계 목업: https://claude.ai/code/artifact/54bb910f-2cbf-4b5f-8078-f015bc5863b6

혼자와 파티가 **같은 문법**을 쓴다. 로컬 모드라는 것은 없다 — 혼자는 자리에 이름만 채운
방이다. 판은 [시작]으로만 생긴다.

**층은 둘이다 (2026-09-05 확정): 로비 → 판.** 로비는 "오늘 어디서 세나"를 고르는
홈(§3.0)이고, 판은 벌금표다. **판은 만들면 생기고 끝내면 없다 (2026-09-06, §3.8). 상태 셋: 없음 / 시작 전 / 진행 중.**
(폐기 2026-09-06) "판은 늘 있고 상태가 준비 / 진행 / 끝남" — 끝난 판이 준비 상태로 남아 로비에
시작 전 얼굴이 서고, 지난 사람의 글자 이름이 다음 대기실에 남는 그림이 됐다.
시작 전(준비)에도 화면은 벌금표 그대로다: 칸이 점선으로 잠겨 있고 [시작]이 유일한
채운 버튼이다. [시작]은 화면 전환이 아니라 판에 불이 들어오는 것이다.
화면 명칭은 헤더 브랜드 옆에 `로비` / `벌금판`. "대기실"은 화면 이름이 아니라 파티원에게
말하는 상태의 말(`대기실 2/8 모임`)로만 남는다.
(폐기 2026-09-05) "홈(판 없음)은 로비다" — 옛 로비는 판 이름·항목·[시작]이 차려진
**방장 준비실**이라, 파티에서 나온 파티원이 떨어지면 "이 차려진 판은 뭐지"가 됐다.
8인 파티에 방장은 1명 — 계정의 다수는 평생 파티원인데 그들의 홈이 남의 옷이었다.
(폐기 2026-09-05, 당일) 3층안 "로비 → 대기실 → 세션" — 로비가 집 기능(계정·주소·기록·
입장)을 가져가자 대기실에 남는 고유물이 [시작] 하나였고, 대기실의 내용물(명단·항목)은
처음부터 표의 데이터를 카드 옷 입힌 이중 UI였다. §3.7의 "화면 하나" 후보를 채택한 것.

### 3.8 물건 셋 · 상태 셋 (2026-09-06 확정·구현 — 이 절이 §3.0~3.4 위에 선다)

목업: https://claude.ai/code/artifact/136ffdef-af25-46fc-8dd7-70a3535e07fa (방장·파티원 두 페이지)

**물건은 셋뿐이다.**
- **방장의 문**: 초대 코드와 방송용 주소. 계정에 붙어 영구. 코드는 방장이 앱을 열어 둔 동안 열려 있고, 닫으면 10분 뒤 잠긴다.
  **문은 판이 있을 때만 통과한다** — 코드가 맞아도 판이 없으면 `지금은 열린 판이 없어요.` 한 줄. 기다리는 화면은 없다.
- **판(= 파티)**: 사람·이름·숫자·기록이 담긴 것. 만들면 생기고 끝내면 없다. 파티원은 판에 속하고, 판이 없어지면 파티원도 없다.
- **방장의 기본값**: 항목·단가·인원·룰렛 외형. 판 밖에 살고(브라우저) 새 판이 물려받는다. 대기실에서 고치면 기본값이 바뀐다.

**판의 상태는 셋, 길은 넷.**

| 상태 | 들어오는 길 | 나가는 길 |
|---|---|---|
| 없음 | 처음 · 정산 끝내기 · 해산 | [+ 새 판 만들기] |
| 시작 전 | 새 판 만들기 | [시작] · [해산] |
| 진행 중 | 시작 | [정산 끝내기] |

- 새 판 만들기: 오늘 날짜 이름, 방장 줄과 빈 자리, 기본값 항목, 로비 열림(= 판 존재 표시), 코드가 없거나 죽었으면 새로. 바로 대기실로.
- 시작: 이름 칸이 열리고 프리셋의 이름이 빈 줄을 채운다.
- 정산 끝내기: 기록이 있으면 결과지 → 판 없음. 기록이 없으면 `기록이 없어요 — 해산할까요?` 하나. 접기라는 세 번째 동사는 없다.
- 해산: 시작 전에만 있는 버튼(대기실 마스트·로비 카드). 남이 앉아 있으면 한 번 묻고, 혼자면 바로.
- 없어진다 = 서버 명단·내보냄 표시·판 존재 표시가 비고 로비가 닫히며(/end), 앱은 자리를 방장 줄만 남긴다. 파티원 로비의 복귀 줄이 사라진다.

**방장의 동사는 셋: 두고 나옴 · 해산 · 정산 끝내기. 나가기는 없다.** 두고 나오면 판이 남고 헤더 칩(`● 모집 중 · 대기실로` / `● 진행 중 · 돌아가기`)과 로비 카드가 문이다.

**파티원의 동사는 둘: 자리 비움 · 나가기.**

| | 자리 비움 (끔 · 로비로) | [파티 나가기] |
|---|---|---|
| 시작 전 | 소속 유지, `자리 비움` 표시, 로비에 복귀 줄 | 자리 사라짐(묻지 않음). 다시 오면 새 사람으로 즉시 착석 |
| 진행 중 | 소속 유지, `자리 비움` 표시, 로비에 복귀 줄 | 줄은 장부라 `퇴장`으로 남음(한 번 묻는다). 다시 오면 표에 서고 방장이 앉힌다 |

나가기는 로비 복귀 줄·대기실·판 안 세 곳에서 같은 버튼이다. `‹ 로비`는 구경이라 아무것도 안 바뀐다.

**(폐기 2026-09-06 당일) 진행 중 합류 — 자동 착석 없음, 예외 없음.** — 사용자: "시작 후에 왔는데 이미 처리 중인 줄 제일 위에 앉히는 게 말이 됨?" · [자리 정하기] 시트는 과한 결정 · "누더기처럼 붙이지 말고 똑바로 규칙 정하셈" → 아래 **앉히기 규칙 하나**로. 원문: 표는 시작 뒤엔 장부라 줄이 생기는 것도 방장의 손이어야 한다.
- 자기 줄이 있는 사람(퇴장·내보냄으로 남은 장부 줄, 계정 출처 `who`)의 요청은 **그 줄 밑에 붙는다**: `↳ {닉}이 돌아오려 해요 [거절] [받기]`. [받기] 한 번에 그 줄로.
- 줄이 없는 사람은 **표 맨 아래**: `{닉} · 들어왔어요 [거절] [자리 정하기]`. [자리 정하기]는 시트(새 줄 만들기 / 빈 줄 목록).
- 시작 전은 같은 자리에 [받기](즉시 착석 규칙). 내보냈던 사람은 시작 전·진행 중 어디서나 방장 승인. 내보냄 표시는 판의 것 — 판이 끝나면 사라진다(계속 막으려면 코드 새로 발급).
- 규칙이 자리로 읽히게 하는 것이 요지다 — 요청이 어디에 서느냐가 어디로 가는지를 말한다.

**화면은 상태를 비춘다.** 로비 카드는 상태 하나에 얼굴 하나(백지 `내 판이 없어요.` [+ 새 판 만들기] / `● 모집 중 · n/m 앉음` [해산] [대기실로] / 진행 중). 로비엔 내가 없는 동안 움직이는 것만 오른다 — 항목 칩은 없다. 상태는 늘 보이고 사건은 한 번 보인다(앉음: 줄 30초·띠 30초·토스트 8초. 들어오려는 사람: 표 아래 줄. "모집 중"은 진행 중엔 상태가 아니라 안 뜬다). 진행 중 화면은 마스트와 표뿐. 문은 공유 창(OBS 공유 설정)에 살고 쓰이는 자리에서 한 번 더 비친다(주소는 로비 계정 줄, 코드는 시작 전 모집 카드). 사람은 표가, 문은 카드가, 설정은 표 머리가 말한다 — 설명 문장은 그 셋이 못 하는 말만 한다. 시작 전 이름 칸은 글자다(누르면 `이름은 [시작] 뒤에 적어요.`). [시작]은 3초 주기로 느리게 빛난다.

**파티원 화면.** 링크 → 초대장(헤더 없이 정중앙, 앱 이름 작게) → [참여하기](로그인 없을 때만 닉 정하기) → 앉음 → 1.5초 뒤 헤더 버튼에 코치마크 한 번(`방송하시면 이 판을 OBS에도 띄울 수 있어요. 여기서요.`, 브라우저당 1회, 주소를 복사한 적 있으면 생략, 버튼이 곧 표적). 끝남·해산은 같은 띠 `판이 끝났어요 [로비로]`; 결과지를 켜 둔 채 방장이 새 판을 만들면 `● 새 판이 열렸어요 [들어가기] [로비로]`(같은 문을 다시 지남). 시작 전에 해산당하면 로비로 가고 파티 카드에 쪽지 `{방장}네 파티가 해산됐어요.` [×]. 대기실엔 설명 슬립이 없다(칩 `시작 전`과 자리 띠가 말한다).

**OBS 인지.** 헤더 버튼 `OBS 공유 설정`(사용자 지정). OBS 연결은 세지 않는다 — user agent 감지는 마이너 송출 프로그램에 비일관하고, 새어 나간 주소를 남이 띄워도 "연결됨"이 되어 오답이다. 앱이 아는 신호는 "주소를 복사한 적 있음" 하나. 유출의 답은 가림·눈·새로 발급(스트림 키 문법). 예시 화면은 폐기 — 나가는 게 없으면 미리보기 칸도 없다.

**앉히기 규칙 하나 (2026-09-06 확정·구현·검증).** 사람이 오면 앱이 앉힌다 — 방장이 자리를 정하는 시트는 없다.
- **계정 출처(`who`)가 같은 줄이 있으면 그 줄.** 시작 전엔 바로, 진행 중엔 그 줄 밑에 `↳ {닉}이(가) 돌아오려 해요 [거절] [받기]`(퇴장한 사람) · `↳ 내보냈던 {닉}이(가) 돌아오려 해요`(내보낸 사람 — 시작 전·진행 중 어디서나 승인). [받기] 한 번에 그 줄로.
- **없으면 — 시작 전:** 첫 빈 자리. 빈 자리가 없으면 표 아래 `{닉} {아이디 앞 두 글자}•••• · 자리가 다 찼어요 [거절] [받기]` — [받기]가 인원을 하나 늘려 앉힌다. 본인에겐 `자리가 다 찼어요 — 방장이 자리를 만들면 들어가요.` [신청 취소].
- **없으면 — 진행 중:** 표 아래에 선다 `{닉} {아이디 앞 두 글자}•••• · 들어왔어요 [거절] [자리 정하기]`, 토스트 `{닉}님이 들어왔어요 — 표 아래에서 받아 주세요.`. **[자리 정하기]는 시트다(2026-09-06 재정정 — 사용자가 여러 번 말한 "직접 배치")**: 제목 `{닉} 자리 정하기`, `어느 줄에 앉힐까요? 퇴장한 사람 줄에 앉히면 남은 벌금도 그 사람 것이 돼요.`, 줄마다 `{줄} 줄 · 빈 줄` 또는 `퇴장 · 벌금 {n}`, 끝에 `+ 새 줄 만들기`. 퇴장 줄에 벌금이 남아 있으면 확인창 `{줄}의 벌금 {n}이 남아 있어요 / {닉}님이 이 줄에 앉으면 그 벌금도 {닉}님 것이 돼요.` [이 줄에 앉히기](사용자: "퇴장 자리에도 채울 수 있어야… 대신 알림이"). 문구 전부 초안. (폐기 2026-09-06, 같은 날) `[받기]가 첫 빈 줄(빈 줄 = 이름도 벌금도 없는 자리표시 줄)에, 없으면 맨 아래 새 줄에 앉힌다. 방장이 줄을 고르는 시트는 없다.` — 되돌리면서도 남긴 자동 배치, 사용자: "[받기]를 눌러서 바로 앉히는 게 아니지 않나요? 몇 번씩 얘기된 건데". 본인 화면은 `자리를 기다리는 중이에요 / 방장이 받아 주면 앉아요.`
  (기록 2026-09-06) 같은 날 오전엔 "진행 중도 방장 손 없이 첫 빈 줄/새 줄에 자동"으로 구현·검증했다. 그 자동은 제(Claude)가 규칙표를 새로 쓰며 바꿔 넣은 것이고, 사용자의 마지막 말은 "id가 같으면 같은 자리로, 나머지는 자리 지정 필요"였으며 자동에 동의한 적이 없었다 — 사용자 지적으로 원래 말대로 되돌림. 자동 착석은 시작 전만.
- **이름으로는 아무것도 맞추지 않는다** (닉 일치 폐기 — 사용자가 여러 번 말한 것). 틀리면 이름 옆 **사람 아이콘** → `다른 줄로 옮기기…`.
- 표 아래 줄은 **승인이 필요한 사람만** 선다. 내보냄 표시는 판의 것 — 판이 끝나면 사라진다.

**사람 아이콘 (2026-09-06).** 계정 붙은 줄의 이름 옆(잉크 톤, 호버 금색). 호버 툴팁은 라벨 붙은 두 줄 `원래 닉네임: {닉}` / `ID: {앞 두 글자}••••`(2026-09-07 사용자 지정 라벨; 끊겼으면 셋째 줄 `연결 끊김`; (폐기) `{닉} {아이디}` 한 줄 — 무엇이 닉이고 아이디인지 안 읽혔다). 누르면 시트 `{닉}` / `{닉} {아이디 앞 두 글자}•••• · 연결됨` / [다른 줄로 옮기기…] [파티에서 내보내기] [닫기]. 옮기기(자리 바꾸기) 목록엔 계정 없는 줄이 전부 — `퇴장` 줄도 `{이름} 자리에`로 선다(방장 판단, 검증 관찰). 도구칸의 사람 버튼·"여기 앉히기"는 폐기.

**≡ 손잡이 (2026-09-06 오후 사용자 요청).** 방장 화면에서 이름 왼쪽의 ≡ 를 끌어 줄 순서를 바꾼다. 잡은 줄은 손을 따라 떠오르고 다른 줄들이 미끄러져 자리를 비킨다(포인터 드래그, 끄는 동안은 DOM transform 만 — 같은 날 사용자: 실시간으로; (폐기) HTML5 드래그 + 놓일 쪽 금색 선 — 투박). 방장 줄(1번)은 손잡이가 없고 그 위로는 놓을 수 없다(사용자: 방장 열은 제외). 줄과 자리는 같은 id 라 둘 다 같은 순서로 돌리고, 저장·중계·오버레이는 줄 순서를 그대로 따른다. 파티원 화면엔 없다. 튜토리얼에선 보이기만 하고 설명하지 않는다(사용자).

**[전부 비우기]**는 표 바로 위 도구줄(지우개 아이콘), 진행 중에만. **헤더 [초대 코드] 팝오버** — 판이 있을 때만 버튼, 코드·복사 셋·새로 발급. 공유 창(OBS 공유 설정)의 초대 카드는 폐기.

**파티원 로비 (2026-09-06).** 헤더 칩 `● {방장}네 파티 · 돌아가기`(구경 중). 파티 참여 카드의 복귀 줄 `● {방장}네 파티 · 시작 전/진행 중 [나가기] [돌아가기]`. 로비 [나가기]는 진행 중이면 묻고 시작 전이면 바로 — 그 근거는 서버 `/seat-of`의 `round`(진행 중). (검증에서 고침) `live`는 "판이 살아 있나"라 시작 전도 참 — 그걸로 표시하니 시작 전 파티가 `진행 중`이라 했고 나가기가 물었다. `live`는 돌아갈 카드(setBackCard)에 그대로.

**이어가기 (2026-09-06, 사용자 제안).** 대기실 표 머리 [지난 판 이어서…] → 피커(`[시작]을 누르면 고른 판의 줄·숫자·기록이 그대로 열려요. 앉은 사람은 자기 줄로 돌아가요.`, 판 목록 `{이름} {n}명 · {합계}`) → 칩 → [시작] 때 그 판의 줄·숫자·기록·항목이 열린다. 출처(`who`)가 같은 사람은 자기 줄(자리 id를 줄 id로 이음), 나머지 앉은 사람은 새 줄, 안 온 줄은 이름만. 같은 roundId를 이어받는다. 그러려고 기록(closeRound)이 줄마다 `who`를 남긴다.

**되살림·기억 (2026-09-06 검증에서 고침).** 새로고침 뒤 서버 마지막 스냅샷은 살아 있는 판(roundId 있고 end·lobby 아님)일 때만 진행 중으로 앉힌다(askResume) — 옛 판이 `진행 중 · 1명 · 49분째`로 되살아났다. relay 저장 화이트리스트에 boardOn·presetNames·resumeFrom(빠져서 새로고침에 판이 없는 것처럼 보였다). 끝내기 뒤 #board 가드에 justEnded 예외(결과지를 건너뛰고 로비로 갔다). 내보내진 사람의 [다시 들어가기]는 rejoinTick으로 입장 효과를 깨운다(joinOk가 이미 참이라 안 움직였다). 합류 성공 시 `goldSettlement.joincode.<room>`에 코드를 두고 로비 [돌아가기]가 `#live=ROOM&j=CODE`로 들어간다(코드 없이 돌아온 소켓이 판이 끝난 뒤 거절돼 띠가 안 바뀌었다). seatIn이 `left`·`who`를 살린다(새로고침 뒤 퇴장자가 사라졌다 — 사용자 지적).

**검증 주행 (2026-09-06).** 배우 다섯 — 방장(게스트) · 기존 계정 파티원 · 처음 오는 파티원(닉 정하기부터) · 늦게 오는 파티원 · 비로그인 방장 — 과 오버레이로 전 흐름을 origin별 계정으로 주행, 스크린샷 60여 장을 묶은 보고서 아티팩트. 남은 관찰: 진행 중 합류·복귀엔 줄 강조가 없고(대기실만 `방금 앉았어요`) 토스트뿐 — 위 "앉음: 줄 30초"는 대기실 얘기 · 비로그인 첫 방문은 대기실 위에 `처음 오셨나요?` 안내가 덮인다 · `[::1]` origin은 워커 소켓이 거절한다(시험 환경) · 배경 탭은 CSS 애니메이션이 멈춰 html2canvas가 모달·띠·토스트를 opacity 0으로 그린다(onclone에서 animation:none).

**안내 체계.** 튜토리얼 둘(방장 8장·파티원 6걸음)과 [?] 팝오버, 로비 권유 줄 — 전부 §3.9. (폐기 2026-09-06 밤) 화면별 사용법 넷·[?] 사용법 모달·방장/파티원 OBS 코치마크·같이 해보기 열 걸음.
파티원도 [기록]을 본다(읽기 전용, 취소 없음) — 기록은 이미 판과 함께 넘어오고 단가 변경도 한 줄로 남아 있어, 자수 카드의 금액이 단가 × 횟수와 다를 때 까닭이 거기 있다. 그래서 자수 카드의 항목별 금액은 남긴다.

**폐기 (2026-09-06)**: 판은 늘 있다는 골자 · 로비 시작 전 얼굴(판 이름·항목 칩·`자리 n/8`·안내 두 줄·[벌금판 열기]) · 판 버리기 · 접기 · `boardMade` 추정(기록·이름·로그로 "만든 적이 있는가") · [초대 링크 만들기]·`m분 남음`·`파티원 모으기` · 판 중 파티 줄과 표 안 [초대 링크] 창 · `들어오려는 사람이 있어요 [보기]` 카드 · 모집 카드 안의 들어오려는 사람 목록 · 대기실 도구줄 요약 · 파티원 설명 슬립 · 진행 중 자동 착석(닉 일치·자리표시) · 판을 넘어 남는 내보냄 표시 · `나감` 태그(→ `퇴장`) · 중단 띠와 이어가기 잔재 · 예시 화면 · 끝내기 뒤 자리·이름 유지 · (당일) [자리 정하기] 시트 · 여기 앉히기 · 도구칸 사람 버튼 · 공유 창 초대 카드 · 닉 일치 착석.

### 3.9 튜토리얼 (통합 가이드 — 2026-09-06 밤 확정·구현)

사용자: "기워 붙이지 말고 통합 가이드로. obs 버튼 안내, 안에서 주소 발급 받는 방법까지." 안내는 이 절이 전부다. 화면별 사용법·[?] 사용법 모달·방장/파티원 OBS 코치·옛 여섯 걸음 코스는 전부 폐기했다(원문 §8). 문구는 전부 초안, 예시 이름 실리안·니나브·웨이는 사용자 지정.

**둘뿐이다 — 방장 튜토리얼과 파티원 튜토리얼.**
- 둘 다 예시 앱 위에서 돈다(§3.8: 같은 앱을 iframe 에 한 번 더, 저장·서버 무접촉, 가짜 계정). 진짜 판·소켓·저장소는 밑에서 그대로라 진행 중이어도 어느 화면에서든 연다.
- 방장 튜토리얼 = 8장. 방장 예시 앱(`#demo`)이 1~3장을 하고, **4장 파티원 화면**은 파티원 예시 앱(`#demo&member&ch4`)이 그 위에 번져 나와 초대장 → 참여 → 자수·정정을 보인 뒤 번져 사라지고, 밑에 그대로 살아 있던 방장 예시가 5~8장을 이어 간다(2026-09-06 낮 사용자 확정 "1안"; 4~5장이 혼란스럽고 파티원 화면은 앞에 있어야 한다고). 처음 열 때만 `튜토리얼을 시작하는 중이에요.`(사용자 지정)가 보이고 — **단, 350ms 안에 예시 앱이 준비되면 문구 없이 바로 번져 들어오고, 문구가 떴으면 800ms 는 둔다**(2026-09-06 낮 사용자: 읽히기 전에 사라져 번쩍임, 부정적 UX; 4장 칩도 같은 규칙; 번짐 .35초, 창도 .2초 번져 들어옴) — 4장을 얹는 동안엔 방장 예시 위에 작은 칩 `실리안의 화면을 불러오는 중이에요.`(사용자 제안 — 파티원 중 한 명의 화면이라 이름 그대로). (폐기, 같은 날 밤) 8장이 맨 끝이고 같은 창에 갈아 끼우던 것 — 돌아올 수 없었다. (폐기) `튜토리얼 시작 중`·`파티원 화면으로 넘어가는 중`
- 파티원 튜토리얼 = 파티원 예시 앱(`#demo&member`) 8걸음(시나리오형; 2026-09-06 낮 두 걸음으로 나눠 7→8). 자수 화면부터 시작한다(이미 초대를 받아 들어온 사람이 보는 것이라 초대장은 없음).
- **걸음은 셋 중 하나이고, 자동으로 넘어가는 걸음은 없다 — 넘기는 건 언제나 사용자** (2026-09-06 낮 사용자 확정: 자동과 [다음]이 기준 없이 섞여 있었다). 하기 걸음(표적을 누르면 넘어감, 버튼 없음 — 표적 테두리가 숨 쉬고 버튼 자리에 옅은 글씨 `직접 눌러 보세요`) · 보기 걸음([다음], 장 끝은 [다음 장]; 테두리는 가만히, **잠긴 표적을 눌러도 [다음]과 같음** — 누르고 싶은 본능을 벌하지 않는다) · 기다림 걸음(더미가 움직이는 동안 잠기고 버튼이 없다가 끝나면 [다음]이 나타남). (2026-09-06 낮 사용자 확정: 두 종류가 구분이 안 갔다) 더미의 움직임(둘이 앉기, 여덟 명 채우기, 자수 도착)은 그대로 자동이고 페이지만 사용자가 넘긴다. 순수 확인 문장은 걸음으로 두지 않고 다음 말풍선에 합친다. (폐기, 같은 날) 서술 걸음의 시간 넘김(2.2·4·5.4초). 장별 다시 보기는 없다 — 다시 보기는 처음부터.
- 예시 앱 맨 위 띠는 **진행 표시**: 장 점 여덟 개를 선으로 잇고 지난 장은 채움, 지금 장은 크고 금색, 옆에 `3장 파티원 모으기 · 2/4`. 파티원 튜토리얼은 걸음 점 여섯 개와 `파티원 튜토리얼 · 2/6`. 띠 오른쪽에 [튜토리얼 나가기](2026-09-06 낮 사용자 지정 문구 — 나갈 수 있다는 걸 명시; 띠는 걸음 막 위라 늘 눌리고 4장 파티원 예시에서 눌러도 전체가 닫힘), ✕·Esc 도 나가는 길. (폐기, 같은 날 밤) [그만두기] 없음 — 어둠에 가려 안 눌리던 옛 버튼을 뺐던 것. 걸음의 어두운 막은 옅게(.36).
- 걸음이 떠 있는 동안 '방금 바뀐' 카드는 숨김, 토스트는 그대로 두고(**튜토리얼 때문에 앱 동작을 바꾸지 않는다 — 2026-09-06 낮 사용자, 앞으로도**; (폐기, 같은 날 몇 시간) 걸음 중 토스트를 오른쪽 위로) 말풍선이 화면 아래 토스트 자리 110px 을 피해 앉도록 스크롤하고 예시 앱 바닥에 200px 여백, 시트·창 안을 가리킬 땐 안내를 모달 위로. 표적으로의 스크롤은 창만 세로로, 표적과 말풍선이 안 보일 때만((폐기, 같은 날) 표 아래 줄을 화면 가운데로 — 스크롤이 튀고 scrollIntoView 가 표를 가로로 밀었다). 예시의 판 기록·장부는 메모리에만.
- 다루지 않는 것: 계정 만들기·로그인, 프리셋, 지난 판 이어서, 메모장 모드, 룰렛(항목 종류로도 안 다룬다 — 2026-09-06 낮 사용자: 튜토리얼에서 룰렛은 뺌; 예시 판은 잡힘·죽음으로 시작), 오버레이 외형, 내보내기의 실제 실행.

**[?] = 튜토리얼 고르기 팝오버.** 헤더 [?]를 누르면 팝오버 `튜토리얼을 볼까요?`에 두 행이 늘 있다. 행 = 이름 + 칩 + 역할 한 줄 + 서브: `방장 튜토리얼` / `판을 열고 파티원을 부르는 사람` (초안) / `8장 · 판 만들기부터 끝내기까지` — `파티원 튜토리얼` / `초대를 받은 사람` (사용자 지정) / `8걸음 · 자수와 내 방송 주소`. 이 화면에 맞는 쪽(남의 판을 보는 중이면 파티원, 아니면 방장)이 위에 채운 [보기]와 금색 테두리 칩 `추천`, 다른 쪽은 유령 [보기]. 본 것은 테두리 칩 `봤어요`와 [다시 보기]. (2026-09-06 낮 사용자: 상태·서브·버튼 안 `추천`의 시인성이 떨어지고 역할 설명이 필요하다 — (폐기) 한 줄 배치, 버튼 안 `추천`, [다시]) 발치 한 줄 `OBS에 넣는 방법과 방송 주소 안내는 [OBS 공유 설정] 창에 있어요.` 바깥을 누르면 닫힌다. [?]의 점은 이 화면에 맞는 튜토리얼을 아직 안 봤을 때.
- 파티원 첫 방문: 자수 화면에 처음 왔을 때 1.5초 뒤 이 팝오버가 저절로 한 번 열린다(브라우저당). 머리말은 `처음이시죠? 튜토리얼을 볼까요?`.
- 로비 권유 줄(`리뉴얼됐어요. 4인 파티를 예시로 처음부터 같이 열어 봐요.` [됐어요] [같이 해보기])은 로비의 **헤더 아래·두 카드 위** 띠로(2026-09-06 사용자 확정; (폐기, 같은 날) 내 판 카드 안 [+ 새 판 만들기] 바로 위 — 공지가 아니라 새 판 UI 의 일부처럼 읽힘), 판이 열려 있지 않은 로비에서 **모두에게** 뜨고 [됐어요]나 완주로만 사라진다(초대 코드로만 들어온 파티원 제외). 기록이 있는 사람도 본다 — 원래부터 쓰던 사람에게 리뉴얼을 알리는 길이라서(사용자).

**방장 튜토리얼 — 장과 걸음** (괄호는 걸음 종류)

| 장 | 걸음 |
|---|---|
| 1 판 만들기 | [+ 새 판 만들기] `먼저 판을 만들어요. 파티원을 부르는 건 그다음이에요.` (누름) — (폐기) `판부터 만들어 볼게요. 파티는 판 안에 있어요.` 사용자: 뒷문장이 뭔 소리냐 |
| 2 항목과 단가 | 첫 항목 단가 칸 `1회 단가는 여기를 누르면 고쳐요. 항목 이름은 바로 위 글자를 누르면 되고요.` (가리키기) → **항목 추가는 직접(2026-09-06 사용자 확정; 단가 수정은 가리키기만 — 값을 바꾸면 5장 예시 숫자가 어긋나고 진행 중엔 창이 하나 더)**: [+ 항목] `이번엔 암살도 세 볼까요? 항목을 하나 더 만들어요.` (누름) → 창 [보통 항목] `보통 항목을 골라요.` (누름) → 새 열 이름 칸 `새 열이 생겼어요. 이름 칸에 암살이라고 적고 [다음].` (적기, [다음]) → 새 열 단가 칸 `1회 10만이면 10. 적고 [다음]. 항목 이름 옆 ×를 눌러, 항목을 삭제할 수도 있어요.` (적기, [다음]; 사용자 지정 문구 2026-09-06 낮 — (폐기) `…지우는 건 항목 이름 옆 ×.`) → 인원 `인원은 여기서 정해요. 늦게 오는 사람은 나중에 줄을 늘려도 돼요.` (가리키기, [다음 장]). 암살 열은 뒷장까지 남고 8장(파티원 예시)에도 있다; 안 적고 지나오면 5장 채울 때 암살·10만으로 채운다. **더하는 항목은 암살 10만(2026-09-06 낮 사용자; 옛 벌금판 예시의 셋째 열이라 5장 숫자가 그대로 맞음). (폐기, 같은 날) 낙사 2만, `보통 항목을 골라요. 룰렛은 나중에.`** (폐기, 같은 날) [+ 항목] 가리키기 `항목은 여기서 늘려요. 지우는 건 항목 이름 옆 ×.` |
| 3 파티원 모으기 | [링크 복사] `초대 링크를 복사해서 디코에 붙이면 돼요. 보내는 건 이번엔 저희가 대신할게요.` (누름, 복사 안 함) → 모집 카드 `보냈어요. 사람들이 들어올 거예요…` (기다림; 1.8초·3.4초에 실리안·니나브 착석, 4.4초에 [다음]이 나타남 — (폐기 2026-09-06 낮) 5.4초 뒤 자동) → [시작하기] `두 명 왔어요. 한 명은… 안 들어오네요. 그냥 시작해 보죠.` (누름; 버튼 이름은 [시작하기] — 2026-09-06 낮 사용자 지정, (폐기) [시작]) |
| 4 파티원 화면 | 3장 [시작하기] 뒤 방장 예시에 `시작했어요. 그런데 실리안 쪽에선 어떻게 보였을까요? 링크를 받았을 때부터 볼게요.` [실리안의 화면 보기] (보기 걸음 — 눌러야 파티원 예시가 위에 번져 나온다; (폐기 2026-09-06 낮) `시작했어요. 이제 실리안의 화면으로 가 볼게요.` 자동 — 뜬금없다(사용자): 읽을 틈 없이 화면이 바뀌고, 방금 둘이 앉은 걸 봤는데 `1명 앉음` 초대장으로 시간이 거꾸로 가는 설명이 없었다). 초대장 `실리안이 링크를 열었을 때 뜬 초대장이에요. 그땐 방장만 앉아 있었죠. [참여하기]를 눌러요.` (`열었을 때` — 사용자, (폐기) `눌렀을 때`) (누름; 초대장엔 방장만 앉아 있음) → 자수 화면 `들어왔어요. 방장이 시작하면 이 화면이 떠요. 자기 줄만 있어요.` (가리키기, 막 없음) → 죽음 칸 `이번 판에 죽었군요. 죽음 칸을 🖱 눌러 자수해 봐요.` (누름; (폐기 2026-09-06 낮) 두 문장이 한 걸음) → 죽음 칸 `방장 벌금판에 바로 올라갔어요. 아, 그런데 잡힌 거였네요. 죽음 칸을 🖱 우클릭해서 되돌려요. 되돌리기는 30초 안에만 할 수 있어요.` (사용자 지정 문구; (폐기) `30초 안에만 돼요.`) (우클릭; 30초 설명은 되돌리는 걸음에 — 사용자; (폐기, 같은 날) `방장 벌금판에 바로 올라갔어요.` 2.2초 자동 걸음을 여기에 합침) → 잡힘 칸 `되돌렸어요. 이제 잡힘 칸을 🖱 눌러요.` (누름; (폐기) `되돌렸어요. 되돌리기는 30초 안에만 돼요. …`) → `올라갔어요. 이제 방장 화면으로 돌아가서 볼게요.` ([방장 화면으로] — 파티원 예시가 번져 사라지고 방장 예시로; (폐기) [다음 장]). 범위는 초대의 룩 → 자수·정정까지(사용자); OBS 는 없다. 판은 막 시작한 것이라 숫자 없음, 열은 잡힘·죽음·암살 |
| 5 벌금 세기 | 돌아오면 실리안의 잡힘 1이 올라오고 토스트 `실리안이 자수했어요 — 파티원이 누른 건 이렇게 올라와요.` → 방장 줄 `실리안이 자수한 잡힘 1회가 올라와 있죠? 방장은 칸을 직접 눌러요. 🖱 누르면 1회, 🖱 우클릭하면 되돌려요. 한번 눌러 보세요.` (누름) → `올라갔죠? 파티원이 자수한 것과 방장이 누른 게 한 표에 쌓여요.` ([다음]) → ((폐기 2026-09-06 낮 "1안") 방장이 먼저 누르고 3초 뒤 실리안 자수가 오던 `올라갔죠? 파티원은 자기 줄을 자수 탭에서 직접 눌러요. 실리안이 지금 누르는 중…` · `실리안이 자수했어요. 파티원이 누른 건 이렇게 올라와요.`) → ((폐기 2026-09-06 낮) 룰렛 머리 `룰렛 항목은 방장이 칸을 눌러 돌려요. 나온 숫자 × 단가가 벌금이에요.` — 사용자: 튜토리얼에서 룰렛은 뺌) → 사람 아이콘 `이름 옆 사람 아이콘. 줄을 옮기거나 파티에서 내보낼 땐 여기예요.` (가리키기) → 웨이 줄 `웨이가 늦게 왔어요. 진행 중에 들어온 사람은 방장이 직접 자리를 정해 줘야 해요. [자리 정하기]를 눌러요.` (누름; (폐기 2026-09-06 낮) `…표 아래에 서 있죠? [자리 정하기]로 줄을 골라 앉혀요.` — 사용자: 이상함; 이 걸음에 들어설 때 웨이가 표 아래에 서고 토스트 `웨이님이 들어왔어요 — 표 아래에서 받아 주세요.` — (폐기 2026-09-06 낮) 사람 아이콘 걸음에서 도착하던 것: 가리키기와 더미 움직임이 섞였다) → 시트 `빈 줄, 퇴장한 사람 줄, 새 줄 중에 골라요. (모험가4) 줄을 눌러 볼까요?` (누름, 그 줄 하나만 눌림) |
| 6 정산 보기 | 표 `한 판 돌았다고 칠게요… 넷이 더 들어와 여덟이 됐어요.` (기다림 4.5초 뒤 [다음]이 나타남 — 2026-09-06 낮 사용자: 템포; (폐기) 자동 넘어감; **정산 예시는 여덟 명** — 옛 벌금판 예시(DEFAULT_PEOPLE)의 숫자를 이름만 바꿔 그대로: 앞 넷은 방장·실리안·니나브·웨이(잡힘·죽음·암살 3·2·0 / 11·1·2 / 2·10·0 / 8·1·0, 누르기·자수로 들어와 기록도 남음), 뒤 넷은 그 사이 들어온 것으로 새 줄에 앉음(20·5·1 / 5·4·0 / 0·2·0 / 5·1·1); 셋째 열 암살(10만)은 2장에서 만든 열이라 옛 예시 숫자가 그대로 맞고 여덟 명 합은 172만(2026-09-06 낮; (폐기) 룰렛이 셋째 열이라 암살을 뺀 132만). **8인 라벨은 사용자 지정(2026-09-06): 니나브 웨이 실리안 샨디 아제나 이난나 바훈투르 카단** — 앞 셋은 파티의 더미, 나중 넷은 샨디·아제나·이난나·바훈투르, 방장 줄까지 여덟이라 카단은 뺌 — 사용자 확정) — **이 장의 걸음은 어두운 막 없이**(정산 내역이 가리면 안 됨; 결과지 걸음도 같음) → [정산 장부] `누른 게 사람별로 정산돼 있어요. 수수료와 나누는 방식도 여기서 정해요.` (누름) → [보낼 우편] `누가 누구에게 얼마 보낼지예요. 디코에 붙일 글도 여기서 복사해요.` (누름) → [벌금표] `벌금표로 돌아갈게요.` (누름) → [기록] `누른 기록이 전부 남아요. 잘못 누른 건 여기서 취소해요.` (가리키기) |
| 7 방송에 띄우기 | 헤더 [OBS 공유 설정] `방송에 띄우려면 여기예요.` (누름 → 창) → 창 안 [내 방송용 주소 받기] `주소는 계정마다 하나예요. 없으면 여기서 받아요. 게스트도 돼요.` (누름; 예시 앱은 주소 없는 계정으로 시작하고 여기서 계정 창 없이 바로 주소가 생김) → 주소 상자 `이게 내 방송용 주소예요. 파티가 바뀌어도 그대로. [복사]로 가져가요.` (가리키기) → [OBS에 넣는 방법] `브라우저 소스로 넣는 법은 여기. 한 번만 넣으면 돼요.` (가리키기; [다음 장]에 창 닫힘) |
| 8 끝내기와 기록 | [정산 끝내기] `다 끝나면 여기예요.` (누름 → 확인창) → 확인창 [정산 끝내기] `결과지가 판 기록에 남아요. 끝낼게요.` (누름; 예시 안에서 진짜 끝내기 흐름) → [닫기] `닫으면 로비로 가요. 이 판은 판 기록에 남아요.` (누름; 기록 언급은 2026-09-06 낮 사용자, (폐기) `닫으면 로비로 가요.`; (폐기 2026-09-06 낮) 그 앞의 결과지 머리 가리키기 `결과지예요. 파티원도 같은 걸 봐요.` — 사용자: 박스가 이상, 빼자) → 로비 판 기록 카드 `끝난 판은 여기 남아요. 결과지를 다시 볼 수 있어요. 여기까지예요.` (가리키기, [다 봤어요] → 끝). (폐기 2026-09-06 낮) `…이제 들어온 파티원이 보는 화면을 볼게요.` [다음 장] → 8장 파티원 화면 |

**파티원 튜토리얼 (독립, 시나리오형 — 2026-09-06 사용자 확정; 방장 4장은 이 걸음 ①~③ 앞에 초대장 걸음을 붙이고 OBS 없이 끝나는 것)** — 파티원 예시 앱, 실리안 자리, 판은 방장·실리안 잡힘 1회씩, 열은 잡힘·죽음·암살(룰렛 없음 — 2026-09-06 낮). 가리키기만으론 조작·되돌리기 연습이 안 된다(사용자)고 해서 이야기로: ① 자수 화면 `들어온 파티원은 이 화면을 봐요. 자기 줄만 있어요.` (가리키기, 막 없음) ② 죽음 칸 `이번 판에 죽었군요. 죽음 칸을 🖱 눌러 자수해 봐요.` (누름; (폐기 2026-09-06 낮) ①②가 한 걸음) ③ 죽음 칸 `방장 벌금판에 바로 올라갔어요. 아, 그런데 잡힌 거였네요. 죽음 칸을 🖱 우클릭해서 되돌려요. 되돌리기는 30초 안에만 할 수 있어요.` (사용자 지정 문구; (폐기) `30초 안에만 돼요.`) (우클릭; 30초 설명은 여기 — 사용자; (폐기 2026-09-06 낮) `방장 벌금판에 바로 올라갔어요.` 2.2초 자동 걸음 + `아, 잡힌 거였어요. …`) ④ 잡힘 칸 `되돌렸어요. 이제 잡힘 칸을 🖱 눌러요.` (누름; (폐기) `…되돌리기는 30초 안에만 돼요. …`) ⑤ [벌금표] 탭 `방장 표는 여기서 봐요. 정산 장부와 보낼 우편도 같이 보여요.` (가리키기) ⑥ 헤더 [OBS 공유 설정] `내 방송에도 이 판을 띄울 수 있어요. 여기서요.` (누름) ⑦ [내 방송용 주소 받기] `주소는 계정마다 하나예요. 없으면 여기서 받아요. 게스트도 돼요.` (누름) ⑧ 주소 상자 `이게 내 방송용 주소예요. OBS 브라우저 소스에 한 번만 넣으면 파티가 바뀌어도 그대로예요.` [알겠어요] (창 닫힘, 끝). (폐기 2026-09-06, 같은 날) 가리키기형 여섯 걸음 — `들어온 파티원은 이 화면을 봐요. 자기 줄만 있고, 칸을 🖱 누르면 1회, 🖱 우클릭하면 되돌려요. 한번 눌러 보세요.` · `방장 벌금판에 바로 올라갔어요. 되돌리기는 30초 안에만 돼요.`

**끝과 표시.** 다 보면 방장은 `party`, 파티원 튜토리얼은 `mtour` 로 봤음(브라우저당). ✕·Esc 로 그만두면 표시 없음. 검증(2026-09-06 밤, 5178 감독·5179 새벽): 방장 8장 완주(예시 안 결과지·판 기록 카드까지), 파티원 6걸음 완주, 첫 방문 팝오버 자동 열림, 예시 앱 서버 요청 0, 진짜 판 불변.

### 3.0 로비 (홈) — 나 | 파티 두 기둥 (2026-09-05 신설, 같은 날 구성 확정)

목업: https://claude.ai/code/artifact/0ad2d7ea-14b7-4490-8c0b-63df7d04f4d5 (로비 ⓐⓑⓒ · ⓑ의 두 얼굴 ·
준비 판 ⓐⓑ · 파티원 시점 · 초대장). **채택 = ⓑ**: 왼쪽 카드 하나가 내 것(계정 줄 → 내 방송용
주소 → 내 판), 오른쪽 기둥이 남과 하는 것(파티 카드 → 판 기록). 읽기 시작점에 '나'가 선다.
**무게는 데이터가 정한다** — 같은 레이아웃이 사람마다 얼굴을 바꾼다: 방장(내 판 있음)은 내 판
카드가 풍성하고, 파티원(내 판 백지·앉아 있음)은 복귀 줄이 주인공이며 만들기는 조용한 문이고,
첫 방문(백지)은 문 둘만 크고 나머지는 비운다.
(폐기 2026-09-05, 당일) ⓐ 프로필 허브(계정 줄+주소 한 줄 → 벤토 → 기록 → 누적) — 다섯 덩이가
같은 무게로 경쟁했다. ⓒ 타이틀 화면형 — 방장이 매일 보는 화면으로는 얇았다.

층이 서도 **아무의 핫루프에도 클릭이 늘지 않는 것**이 성립 조건이다. 로비를 실제로 보는
사람은 셋뿐이다: 첫 방문자, 순수 파티원, 방금 파티에서 나온 사람 — 정확히 이전 구조에서
붕 떠 있던 세 장면이다.

- **파티 허브 — 헤더 칩 하나 + 로비 2열 (2026-09-07 사용자 확정: 헤더 리뉴얼, 구현 완료)**. 내가 속할 수 있는 방은 하나다(§3.3 방 하나 규칙). 헤더는 **파티 칩 하나**와 [OBS 공유 설정]·[?]뿐이고, 칩은 지금 내가 속한 방 하나만 말한다: `파티 없음`(점 없음, 회색) / `시작 전 · 내 판`(비로그인) / `● 모집 중 · 내 판 n/정원` / `● 진행 중 · 내 판` / 파티원은 `● {방장}네 파티 · 진행 중|시작 전`. **로비에서는 칩이 없다** — 로비 2열이 같은 허브다. 칩을 누르면 허브 팝오버(왼쪽 정렬, 480px). **허브의 얼굴 = 판 상태 하나에 하나**(옛 내 판 카드의 네 얼굴을 그대로 물려받음): 판 없음 = `파티 없음` + 점선 상자 [+ 새 판 만들기] + 참여 칸(초대 코드나 주소) / 시작 전·모집 중 = 롤식 상자(상자 전체가 대기실 문, 안에 `대기실 n/정원`과 앉은 이름, 우상단 `× 해산`) + **초대 한 덩이** / 진행 중 = 금테 상자(`n명 · 벌금 · m분째`, 이름, `벌금판으로 ›`) + 초대 한 덩이 / 남의 파티에 앉음 = `● {방장}네 파티 · 진행 중` 상자(`돌아가기 ›`, 우상단 [나가기]) / 파티원 앱 = 앉은 사람·내 벌금 + [파티 나가기]. 보관된 초대(§3.3)가 있으면 어느 얼굴이든 맨 위에 `받은 초대가 기다려요. [지우기] [참여하기]`. **로비 2열**: 1열은 계정 카드(아바타·닉·게스트 뱃지·설정·내 방송용 주소·게스트 안내 한 줄)와 기록 카드(없어도 선다 — `아직 기록이 없어요. 판을 끝내면 결과지가 여기 남아요.`, 초안)가 위아래로, 2열은 `파티` 카드에 허브 그대로. **초대 한 덩이 (2안)**: [디코 메시지 복사](채운 금색, 맨 앞) · 코드 칩(가린 코드 + 눈) · [코드 복사] [링크 복사](유령) · `새로 발급`(작은 글자) — 왼쪽에 모여 한 줄, 좁으면 줄바꿈. 대기실 모집 카드도 같은 덩이: 머리 `● 모집 중  n/정원` → 앉은 사람 이름 → 초대 한 덩이. **폐기 (2026-09-07)**: 헤더의 세 칩(liveAway `● 진행 중 · 돌아가기` · memberAway `{방장}네 파티 · 돌아가기` · readyAway `모집 중 · 대기실로`) — 내 판 모집 중 + 남의 파티 착석이면 둘이 나란히 섰고 그 상태는 검증된 적이 없었다(사용자: 얼렁뚱땅 안 됨) · 헤더 [초대 코드] 버튼과 팝오버 · 로비 `파티 참여` 카드(복귀 줄·입력칸 — 허브로) · 계정·주소·내 판이 한 카드(사용자: 아바타와 기록이 왜 한 카드냐) · 빵부스러기의 [파티 나가기](허브로) · 진행 중 얼굴의 `판을 두고 나온 상태예요 — 파티원은 그대로 셀 수 있어요.`와 목업의 `방장 없이 12분째`(사용자: 로비를 볼 뿐 나간 게 아니다 — 필요 없는 말은 다 뺀다) · `앉음` 라벨(`대기실 n / 정원 앉음` → `대기실 n/정원`) · 모집 카드의 22px 코드 줄과 오른쪽 끝 글자 링크(사용자: 가시성·사용성이 떨어지고 룩이 구리다) · 모집 카드 발치의 게스트 안내(→ 로비 계정 카드). 목업: https://claude.ai/code/artifact/ae1057c5-6c86-4c9c-8840-f5efd0d757b6
- **도착 규칙 (부트)**: 앉은 파티 있음 → 그 파티의 판. 내 판 살아 있음 → 바로 벌금표
  (기존 규칙 유지). 내 판 있음 → 내 판(준비). **백지일 때만 로비.** 백지 판정은 §5.1.
- **로비 문은 헤더 브랜드 글자와 마스트의 빵부스러기 `‹ 로비`** (2026-09-05 — 브랜드 글자
  하나만으로는 문이 안 보였다). 화면 이동은 멤버십을 건드리지 않는다 — 자리 그대로, 파티
  카드의 복귀 줄로 언제든 복귀. 빵부스러기 줄 오른쪽은 파티원의 [파티 나가기](진행 중일 때).
- **판을 두고 나온 표시** (2026-09-05): 내 판이 진행 중인데 로비·기록 화면에 있으면 헤더에
  `● 진행 중 · 돌아가기` 칩이 선다 — 누르면 판으로. 로비 카드의 진행 중 얼굴과 한 벌이다.
- **마스트의 상태 칩** (2026-09-05): 판 이름 옆에 `시작 전`(회색 점선) / `진행 중`(금색).
  로비 카드의 제목과 같은 말이라 들어오기 전과 후가 이어진다. 파티원 화면에도 같은 칩.
- **계정 줄**: 아바타(첫 글자)·닉·`게스트` 뱃지(정식 계정은 가린 아이디). 설정(닉 바꾸기·
  로그아웃·아이디 만들기)은 여전히 오버레이 공유 설정 창(§5.7)이다 — 로비는 진열대다.
- **내 방송용 주소**: 가림 + [복사] 한 줄. 재발급·소스 나누기·외형은 창에서 — 표면이
  둘이어도 조작은 한 곳이다.
- **내 판 카드 — 네 얼굴, 한 뼈대** (2026-09-06 낮 사용자: 왼쪽 UI 일관성이 없다 — 백지만 점선 상자·가운데 정렬이고 시작 전은 맨 버튼이 왼쪽에 섰다): 어느 상태든 제목(상태) 아래 상자 하나에 사실 한 줄(있을 때)·설명 한 줄·버튼 줄이 가운데로 선다. 제목: `판 없음`(초안; (폐기) `내 판` — 다른 얼굴은 상태가 제목인데 백지만 카드 이름이었다) / `시작 전` / `모집 중` / `진행 중 · 판 이름`. 시작 전과 모집 중은 상자의 사실 줄에 `대기실 n / 정원 앉음`을 늘 보인다(2026-09-07 사용자: 시작 전에도 대기실 인원을 알려야 하고 UI 가 밀리면 안 된다 — (폐기) 모집 중 제목 옆 칩). **롤 로비처럼 (2026-09-07 사용자 확정)**: 제목 줄 오른쪽 끝에 작은 글자 버튼 `× 해산`(파티가 없는 시작 전에도), 상자 전체가 대기실로 가는 문(호버에 금테, 발치에 `대기실로 ›`; 진행 중도 상자 전체가 문, `벌금판으로 ›`), 상자 안에는 `대기실 n / 정원 앉음`과 앉은 사람 이름을 `·`로 나열(없으면 `아직 아무도 없어요`, 초안). (폐기, 같은 날) 상자 왼쪽 큰 유령 [해산] + 가운데 [대기실로] — 사용자: 추가만 딱 한 모양. `× 해산`은 상자 안 우상단, 테두리 있는 작은 버튼(2026-09-07 사용자 지정 자리 — 롤 로비처럼; 상자 클릭과 분리). 상자 테두리는 잉크 .48 + 옅은 판. (폐기, 같은 날) 제목 줄 오른쪽 끝 글자 버튼 — 사용자: 상자 안으로 옮기고 테두리를 주라. 로비의 [해산]은 늘 한 번 묻는다: 남이 앉아 있으면 `파티를 해산할까요? / 앉아 있는 파티원이 나가요.`, 혼자면 `판을 해산할까요? / 시작 전 판이 없어져요. 자리와 이름이 지워지고 항목과 단가만 남아요.`(초안). 대기실의 [해산]은 그대로(혼자면 바로). 설명(초안): `벌금을 셀 판을 만들어요 — 계정은 필요 없어요.`(로그인 뒤 `내 판이 없어요.`) / `판을 만들어 뒀어요. 대기실에서 파티원을 모으거나 바로 시작해요.` / `파티원이 모이는 중이에요. 대기실에서 자리를 보고 시작해요.` / 진행 중은 아래 원문 그대로. 상자는 빈 판만 점선, 두고 나온 진행 중만 금테. 버튼은 모두 같은 크기([+ 새 판 만들기]는 로그인 뒤 유령).
- **내 판 카드 — 상태가 제목인 세 얼굴** (2026-09-05 확정 — "두고 나왔다"와 "시작 전이다"가
  작은 뱃지로는 안 읽혔다): ① **진행 중**(판을 두고 나온 상태) — 금테·맥박 점, 제목
  `진행 중 · {판 이름}`, 한 줄 `{n}명 · 벌금 {총액} · {m}분째`(시작 시각은 roundId 에 든
  시각), 부제 `판을 두고 나온 상태예요 — 파티원은 그대로 셀 수 있어요.`(혼자 판이면
  `숫자는 그대로 남아 있어요.`), 버튼 **[벌금판으로 돌아가기]**. ② **시작 전** — 제목
  `시작 전 · {판 이름}`, 항목 칩(`잡힘 1만 · 죽음 3만 · ◎ 룰렛 8면`) · 자리 수 칩(`자리 3/8`),
  부제 `열어서 [시작]을 누르면 세기 시작해요.` + 내 판 기록이 있으면 `지난 판 '{이름}'은
  끝났어요 — 결과는 판 기록에.`, 버튼 **[벌금판 열기]**. ③ **백지** — [+ 새 판 만들기]
  얼굴(데이터는 지금처럼 암묵 생성 유지 — 마이그레이션 없음, 로비의 얼굴만 명시적).
  "새로 파야 하는" 순간은 처음 한 번뿐이다 — 정산 끝내기는 항목과 자리를 남기고 숫자만
  결과지로 보내니, 끝낸 뒤 판은 저절로 다음 판의 시작 전 상태다. ②의 둘째 줄이 그걸 말한다.
  (폐기 2026-09-05, 당일) 제목 `내 판` + 작은 `진행 중` 뱃지 + 상태와 무관한 [벌금판 열기] 하나.
  (폐기 2026-09-05, 당일) 자리 미리보기(이름 칩 나열) — "지금 저 사람들이 있다"로 읽혔다.
  명단은 판 안의 것이고 로비 카드는 예고편이다. 버튼 [들어가기]도 같은 날 [벌금판 열기]로 —
  오른쪽 파티 참여 카드의 [참여하기](남의 파티에 들어감)와 동사를 가른다.
- **파티 카드 — 초대 코드로 들어가는 문 (2026-09-05 확정)**: 본문은 입력칸 하나 —
  초대 주소를 그대로 붙여넣거나 코드(8자)만 쳐도 들어가진다(로아 비밀 파티 입장 문법).
  파티에 앉아 있으면 카드 머리에 복귀 줄(`퐁퐁네 파티에 앉아 있어요 [돌아가기]`)이
  선다 — 앉은 사람이 로비에 있는 건 일부러 들른 잠깐이라, 돌아가는 문이 잘 보여야
  한다. 받은 지목 초대는 여기 안 선다 — 초대는 알림(기존 우하단 카드)이 맞는 문법이다.
  (폐기 2026-09-05, 당일) 발치의 **함께한 파티원 칩 줄**(누르면 노크 시트) — 함께한 사람 UI 를
  통째로 걷었다(§3.3). 게스트 계정에선 관계 자체가 애매했고, 재합류는 멤버십 유지와 초대
  링크로 충분하다. 발치엔 한 줄만: `초대 링크나 코드를 받으면 여기 붙여넣어 들어가요.`
  (폐기 2026-09-05, 당일) "함께한 사람이 앉아 있는 방들의 목록/문패 + 서버 스냅샷
  `rs:`" — ① 초대는 토스트가 일반 문법인데 문으로 세웠고 ② 앉은 사람은 로비가 아니라
  대기실에 있어야 하며 ③ '아는 방장' 문패는 한 번은 같이 해야 생겨서 첫 합류를 못
  돕고, 재합류는 멤버십 유지([정산 끝내기]가 아무도 안 내보냄)가 이미 해결한다 —
  남는 수요(탈퇴 후 재합류)는 노크로 충분하다.
- **판 기록의 집은 로비다** — 파티원 로컬 기록(§3.4)이 생기면서 "기록은 대기실(내 판)
  소속"이 틀렸다. 순수 파티원은 내 판이 없어도 참여한 판 기록을 봐야 한다. 내 판
  기록과 참여한 판 기록(파티 이름표)이 여기 섞여 선다 — 최근 2~3판은 결과 요약 한 줄
  (`9월 5일 · 퐁퐁네 파티 · 총 4만G · 8명`)로 서고 발치에 [전체 보기]. 판 화면의 기록
  문(§5.4)은 같은 목록을 연다.
- **채우는 게 아니라 밀도다.** 카드 속을 실데이터 요약으로 채워 "썰렁함"을 막는다 —
  일러스트·명언 같은 가짜 채움은 안 쓴다. **누적 한 줄** `여태 12판 · 벌금 87만G · 최다 벌금왕
  순두부`는 **판 기록 카드 발치에 작은 글자**로(2026-09-05 확정) — 기록의 요약이니 기록 소속이다.
  로비 전체 발치에 두면 장난감처럼 보인다. 로컬 집계, 서버 0.
- **백지 첫 방문 로비는 채우지 않는다** — 타이틀 화면 문법: [+ 새 판 만들기]와 초대
  입력칸 두 문이 크게 서고 여백이 정상이다.
- **비로그인에도 로비는 있다** (2026-09-05 교정 — §6-5 "로그인 없이도 카운터·정산이 v1과 동일"
  회귀 조건과 어긋났다). 계정 줄 자리에 `로그인하면 파티에 들어가고 내 방송용 주소가 나와요` +
  [시작하기]. 초대 입력칸에 코드를 넣으면 시작하기 랜딩이 먼저 뜬다.
  (폐기 2026-09-05, 당일) "비로그인에는 로비가 없다 — 랜딩이 그 자리다".

### 3.1 판의 준비 상태 (시작 전 벌금표) — 옛 로비 벤토는 폐기 (2026-09-05)

시작 전의 판은 **벌금표 화면 그대로**다. 이 절의 옛 본문(2열 벤토)은 2026-09-05 통째로
폐기됐고 아래에 기록으로 남긴다. 살아남는 규칙과 이사 지도:

- **살아남는 규칙**: 인원 수 = 칸 수([4인]·[8인] 퀵 + 숫자 칸 1~16, 기본 8) · 빈 칸은
  `(모험가N)` 자리표시 · 수락은 이름 안 적힌 칸을 위에서부터 · 로그인 상태면 1번 자리에
  방장 닉 · [시작]은 무대 우상단(= [정산 끝내기] 좌표, 유일한 채운 버튼) · 이름이 없어도
  시작된다 · 아이디는 앞 두 글자 + 점 넷.
- **줄의 신분 표시 (2026-09-05)**: 계정 붙은 줄은 **이름 칸의 왼쪽 빈자리**에 작은 `i` 하나가
  선다(호버에 닉 · 가린 아이디, 끊긴 사람은 흐려짐 — 서버가 뷰어 소켓 접속을 `on`으로 알린다).
  (폐기 2026-09-05, 당일) 가린 아이디 글자를 그 자리에 쓰던 안 — 이름 칸을 너무 먹었다.
  이름은 오른쪽 끝에 그대로 붙는다 — n×m 탐색의 눈길이 이름에서 칸으로 바로 건너가야 해서,
  이름 뒤에 칩을 달지 않는다. 파티원 화면엔 아이디가 안 오므로(§4.3) `방장`·`나`만.
  (폐기 2026-09-05, 당일) 이름 오른쪽에 붙인 아이디 칩 — 이름의 오른쪽 정렬을 흔들었다.
- **대기실 신호 (2026-09-05 목업 확정 — 준비 상태의 표는 대기실이다)**: ① **자리 띠** — 잠긴 벌금
  칸 셋 자리에 줄마다 띠 하나: `● 앉음`(+`방장`/`나`) · `● 방금 앉았어요 · {닉} {가린 아이디}`(10초) ·
  `연결 끊김` · `직접 적은 이름 — 이 사람이 초대로 들어오면 여기 앉아요`(첫 줄만 긴 말, 파티원에겐
  `방장이 적은 이름`) · `빈 자리 — 초대 코드로 차요`(첫 빈 줄만). 항목 머리는 그대로라 항목 설정도
  그 자리에서 하고, [시작]을 누르면 띠 자리가 그대로 벌금 칸이 된다. ② **모집 카드 머리**가 초대를
  냈는지 말한다: `● 모집 중 · m분 남음` + `n/정원 앉음`(계정 붙은 자리만 센다); 초대가 없으면
  `파티원 모으기`. 마스트 상태 칩도 `시작 전 · 모집 중`. ③ **도착**: 앉는 순간 그 줄이 3초 금색으로
  밝아지고(왼쪽 금색 띠) 토스트가 줄 번호를 말한다. 도구줄 오른쪽에 요약 `앉음 n · 직접 적음 n ·
  빈 자리 n`. (폐기 2026-09-05) 표 윗줄의 방장 힌트 `초대 링크를 보내면 파티원이 빈 칸에 앉아요.
  이름을 직접 적어도 돼요.` — 띠가 그 자리에서 말한다. 목업: https://claude.ai/code/artifact/c4c275de-32d7-4709-b197-b883d708bd0e
- **표면 손질 (2026-09-05 ①②③⑤⑥)**: 모집 카드 읽는 순서 = 머리(`● 모집 중 · m분 남음 | n/정원 앉음`) →
  초대 코드 한 줄(코드·눈·[코드 복사][링크 복사]) → 조용한 링크(디코 메시지 복사·새로 발급) → 들어오려는
  사람 → 게스트 안내(맨 아래 작은 글자). 시작 전 판 이름은 아직 날짜 기본값이라 로비 카드에는 고친 이름만
  보이고 마스트에서는 흐린 자리표시로 선다(①). 기타 칸은 눌러야 열리고 편집칸이 칸 위에 떠서 줄 높이를
  밀지 않는다, 시작 전엔 잠긴다(②). 기록 없는 판의 [정산 끝내기]는 `아직 기록이 없어요. / 이 판을 접고
  로비로 갈까요? 이름과 항목은 그대로 남아요.` [접기] → 로비(⑤). 비로그인 로비는 [+ 새 판 만들기]가
  주인공이고 권유는 카드 발치 한 줄, 비로그인 방장의 시작 전 판에는 모집 카드 대신 `파티원을 부르거나
  방송에 띄우려면 방송용 주소가 필요해요. [주소 받기]` 한 줄(③). 주소 줄은 가림을 풀어도 폭이 안 변한다(④).
- **줄의 사람 버튼 (2026-09-05)**: 파티가 있으면 줄 끝 도구칸(기록·삭제 옆, 호버에 뜸)에
  사람 모양 버튼 하나(방장 줄 제외). 누르면 줄 이름이 제목인 시트 — 동사 라벨만: 계정 붙은
  줄은 `{닉} {가린 아이디} · 연결됨` + **[다른 줄로 옮기기…]**(기존 자리 고르기 창) ·
  **[파티에서 내보내기]**(확인 한 줄); 이름만 적힌 줄은 **[여기 앉히기]** — 앉아 있는
  파티원(지금 어느 줄인지 표시)과 신청 중인 사람 목록. 방장의 눈이 가 있는 줄에서 표의
  기존 문법(호버 도구)으로 푼다. `i`는 설명만 남긴다.
  (폐기 2026-09-05, 당일) `i` 밑의 팝오버에 자리 바꾸기를 두는 안 — "설명" 글자 밑에 동작을
  기대하지 않고, 계정 없는 줄엔 반대 방향 동작을 놓을 곳도 없었다.
- **준비 상태의 문법** (2026-09-05 목업 ⓑ 채택): 칸은 점선·잠김(누르면 §8 한 줄). 판 이름은
  **마스트 왼쪽**에 글자 + 연필(진행 중엔 그 옆에 [전부 비우기]가 나란히). [시작]은 마스트
  오른쫽 끝 — [정산 끝내기] 좌표. 탭(정산 장부·보낼 우편)·입력 단위·채팅 복사는 시작 전엔
  숨긴다. 도구줄은 인원(4인/8인/숫자)·[프리셋]·[+ 항목]. **모집 카드**가 표 위에 선다 —
  옛 모으기 열이 가로로 누운 것: 초대 링크 줄(마스킹·[새로 발급]·[디코용 복사], 없으면
  [초대 링크 만들기]) · 신청이 오면 금테 줄([거절][수락]). (폐기 2026-09-05, 당일) 함께한 사람
  칩(지목 초대) — §3.3. **초대는 코드가 주인공** (2026-09-05 확정): `초대 코드 •••• ••••`(눈으로
  잠깐 드러남 — 화면은 방송에 잡힌다) · `m분 남음` · [코드 복사] [링크 복사] · 조용한 링크
  `디코 메시지 복사`·`새로 발급`. 코드는 음성으로 불러 줄 수 있고 로비 입력칸이 코드를 받는다.
  (폐기 2026-09-05, 당일) 가린 주소 한 줄 + [디코용 복사] 하나. **시작하면 카드가 같은 자리의
  한 줄로 줄어든다** — `파티원 n · ● 모집 중 · m분 남음 … [신청 k] [초대 링크]`(누르면 같은
  초대 조각 + 신청 목록이 창으로). 자리가 같으니 시작 전과 후에 새로 배울 것이 없다.
  (폐기 2026-09-05, 당일) 벌금표 도구줄 안의 [초대 링크] 버튼 — 사람을 부르는 일은 표의 일이
  아니다. (폐기 2026-09-05, 당일) 파티 서랍(방 칩). 비로그인 방장에게는 모집 카드가 없다.
  (폐기 2026-09-05, 당일) ⓐ 띠 — 초대 링크 한 줄만 두고 나머지를 서랍에 넣는 안. 옛 모으기
  열의 힘(함께한 사람 지목·신청 줄이 보이는 것)이 숨었다.
  (폐기 2026-09-05) 얼린 판·[이어가기] — 자동 중단이 폐지되어 준비 상태에 얼림이 없다(§3.4).
- **파티원도 같은 화면**(§5.3) — 명단·항목이 보이고(초대받은 사람이 지금도 보는 그
  화면), 시작하면 자기 줄 칸에 불이 들어온다. [시작] 자리에는 `방장이 시작하면 함께
  시작돼요`. 신청 중(st:req)이면 그 줄이 `수락 대기` + [신청 취소].
- **이사 지도** (옛 벤토 → 어디로): 명단 카드 → 표의 이름 열 / 항목 카드 → 표의 항목
  머리 / 모으기 열 → 시작 전 초대 줄 + 파티 서랍 + 우하단 신청 카드 / 히어로 판 이름 →
  표 머리 / 판 기록 아이콘 → 로비(§3.0) / [이어가기] 카드 → [시작] 자리 / 비로그인 권유
  카드 → 없음(비로그인은 로비도 없다 — 랜딩).
- (폐기 2026-09-05) 아래 옛 본문 전체. 사유: 로비가 집 기능을 가져가자 이 화면의
  고유물이 [시작] 하나였고, 명단·항목 카드는 표와 같은 데이터를 고치는 두 번째 UI였다.
  파티원 미러·시작 시 화면 전환 장치도 함께 필요 없어졌다.

**(옛 본문 — 2026-09-04 확정, 2026-09-05 폐기)** 데스크톱 앱이다 — 폭을 쓴다. **명단이 왼쪽**(첫 할 일이 읽기 시작점에), 모으기가 오른쪽. 무대 최대 1080px 가운데(시스템 줄·마스트·판과 같은 열 — 로비 [시작]과 판의 [중단]이 같은 좌표에 서는 근거), 2열 그리드.

- **왼쪽 열 위 = 명단 카드 — 인원 수만큼의 칸** (2026-09-04 확정, 사용자설정 방 문법 — §3.7):
  칸 수 = 인원 수, 방장이 정한다 — **[4인]·[8인] 퀵 + 숫자 칸(1~16), 기본 8인**. 빈 칸은
  `(모험가N)` 플레이스홀더로 서서 "여기가 자동으로 차는구나"를 그 자리에서 말하고,
  직접 쳐도 된다. **수락된 사람은 이름 안 적힌 칸을 위에서부터 대체한다** — 이름 적힌
  칸(앱 안 쓰는 사람)은 안 밀린다. [×]는 칸을 비운다 — 줄을 지우지 않고, 칸은 남아
  자리표시로 돌아간다. 번호 + 인라인 입력(벌금표 이름 글꼴), 계정 붙은 자리는
  **아이디가 닉네임 바로 오른쪽에 붙는다**(이름 칸은 내용만큼만). 표기는 **앞 두 글자 + 점 넷**(`he••••`) — 로비가 방송에 잡혀도 아이디가 새지 않고, 점 개수가 고정이라 길이도 못 센다. **전체를 보는 장치는 없다**(호버 공개 폐지 — 이 화면은 통째로 송출된다). **로그인 상태면 1번 자리에 자기 닉이 미리 앉아 있다**([×]로 뺄 수 있고, 비로그인이면 빈 칸) — 로아 로비에서 파티장이 1번 슬롯인 것과 같다. **[시작]은 무대 우상단 모서리에 고정**(머리 한 줄의 오른쪽 끝) — 판 화면의 수명 동사 자리와 같은 좌표이고, 이 화면의 유일한 채운 버튼. **빈 칸은 [시작] 때 `(모험가N)` 이름 그대로 판에 들어간다** — 이름 고치기는 판에서 한다(벌금이 생기는 순간이 이름을 적는 순간이다). 이름이 없어도 시작된다.
- **왼쪽 열 아래 = 항목 카드**: 줄에서 **바로 고친다** — 항목마다 이름·단가 인라인 편집 +
  [×] + [+ 항목]. 요약↔편집 모드 전환은 두지 않는다(누를 이유가 없는 겹문이었다).
  채운 버튼 없는 조용한 줄들이라 [시작]과 자리를 다투지 않는다.
- **오른쪽 열 = 파티원 모으기** (상설): 위→아래 ①신청(닉·가린 아이디·[거절]·[수락],
  금테 — 자리는 앱이 §3.2 규칙으로 정한다) ②함께한 사람 칩(누르면 §8 시트) ③처음 오는 사람 — 링크(마스킹·[새로 발급]·
  [디코용 복사]). 비로그인이면 조용한 권유 카드 한 장("초대를 보내면 파티원이 이 명단에
  앉아서 자기 벌금을 직접 세요." + [로그인]).
- **히어로**: 제목 `파티 로비`, 그 아래 **판 이름**(눌러서 고치는 인라인 편집, 기본값 §8 `{날짜} 벌금 파티`), 우상단에 [시작]. 판 이름은 [시작] 때 그 판의 이름이 되어 **결과지·판 기록에 그 이름으로 남고**(찾는 열쇠가 날짜뿐이면 못 찾는다), 다음 판은 다시 그날 기본값으로 시작한다.
- **판 기록은 히어로 제목 옆의 아이콘**(시계+되돌아가는 화살표, 옆에 개수) — 누르면 목록이 **모달**로 뜬다. 줄 하나를 통째로 내주지 않는다. 헤더에서는 뺀다(판 중에 볼 일이 거의 없다 — 정산 비교는 판이 끝난 뒤, 항목·단가는 지금 판에 있다).
- 중단된 판 카드([이어가기])는 히어로 아래, 벤토 위.
- **판 중 파티 서랍(방 칩)의 내용은 모으기 열과 같은 구성·같은 순서** — 여는 자리만 다르고
  내용의 지도는 같다. 표면이 이사해도 사람이 헤매지 않게 하는 조건이다.
- 지울 것: "로비" 제목, [+ 이름 추가] 단일 입력, 카드 병렬 배치, 허공의 [시작].
  (옛 인원 스테퍼도 지웠으나, 2026-09-04 "인원 수 = 칸 수" 문법으로 숫자 칸이 부활 — §3.7)
- 인원이 덜 차도 그대로 시작한다 — 빈 칸은 `(모험가N)`으로 판에 들어간다.
- 판이 살아 있으면 앱을 열자마자 벌금표다 — 매일 혼자 쓰는 사람은 로비를 볼 일이 드물다.
  **기존 사용자의 저장된 판은 진행 중인 판으로 승격**된다. 세던 판이 강등되는 일은 없다.
- 인원 수: 기본 8, 방장이 조절(1~16 — 서버 lobby.cap 도 같은 수). 사람·이름이 앉은 칸
  수 아래로는 못 줄인다. 이름 안 적힌 칸이 없으면 수락이 안 된다 — 방장이 칸을
  비우거나 인원 수를 늘려서 푼다. 수락 대기 줄(대기실) 같은 기계는 안 만든다.

### 3.2 자리

자리 = `{ 이름, 붙은 계정(빈 값 가능), 기억된 아이디 }`. **자수 자격 = 내 계정이 붙은 자리.**
연결은 판의 행이 아니라 **자리**에 산다 — 판이 갈려도 연결이 안 끊어진다(닉네임 매칭
재연결은 폐기).

- **붙는 순간에 바로 앉는다** (2026-09-05 표준화 — 다음 빈 슬롯에 즉시 앉는 로비 문법): ① 닉과 똑같은
  이름이 적힌 계정 없는 자리가 하나면 거기 ② 아니면 첫 빈 칸(`(모험가N)`) ③ 빈 칸이 없으면 자리 없이
  들어와 — 시작 전엔 본인이 이름 적힌 줄 중에서 고르고, 진행 중엔 **반드시 방장이 배치**한다(들어오려는
  사람 목록의 [자리 만들어 앉히기] 또는 줄의 사람 버튼). 방장에겐 토스트 `{닉}님이 '{줄}' 줄에 앉았어요.`
  (폐기 2026-09-05, 당일) "이름 적힌 빈 줄이 있으면 문 앞에서 본인이 고른다" — 문 앞의 질문이었다.
  (폐기 2026-09-05) "자리가 아이디를 기억해 자동 복귀" — 나간 자리는 빈다(§3.4).
  **[수락 ▾] 자리 지정은 폐지**(2026-09-04). 틀리면 줄의 사람 버튼(§3.1)으로 옮기고 자격이 따라간다.
- **시작 전엔 본인이 자리를 옮긴다, 진행 중엔 방장만** (2026-09-05 표준화·확정): 시작 전 파티원은 계정
  없는 줄(이름 적힌 줄·빈 줄)의 띠에서 `이 줄로 옮기기`(서버 POST /seat — state.lobby 가 있을 때만 재선택
  허용). 빈 칸이 없어 자리 없이 들어온 사람에겐 `어느 줄이 나예요? 앉은 뒤에도 빈 줄로 옮길 수 있어요.`
  선택지. 진행 중에는 줄에 벌금이 붙어 있어 본인 선택·옮김이 없다 — 자리 없이 들어오면 `자리를 기다리는
  중이에요 / 방장이 줄을 정해 줘요.`, 방장이 배치한다. 계정 붙은 줄은 후보에서 빠진다(rows2 의 `a`).
  (폐기 2026-09-05) "한 번 고르면 잠긴다", "판 도중 합류는 본인이 고른다".
- **표시 이름은 방장 장부의 것.** 방장이 사람 자리의 이름도 보기 편하게 고칠 수 있다.
  파티원이 닉을 바꾸면 방장이 손대지 않은 자리만 따라 바뀐다 — 방장이 고쳐 둔 이름은
  안 건드린다. 계정 닉·아이디는 자리의 참고 정보로 따로 보인다.
- 판 도중에도 [+ 인원 추가]는 자리 추가로 동작한다(늦게 온 사람). 항목·직접 입력 행 수정도
  판 도중 그대로 된다.
- **이름 적힌 칸은 참가자다.** 벌금이 붙는 존재라 사람이 들어와도 **자동으로 밀리지
  않는다** — 사용자설정 방의 컴퓨터 슬롯 문법이다(방장이 앉힌 참가자라, 사람이 온다고
  컴퓨터가 빠지지 않는다. 워3에서 다들 쓰던 그 문법). 밀리는 것은 이름 안 적힌
  `(모험가N)` 자리표시뿐이다 — 그건 참가자가 아니라 빈 칸의 이름이다. 명단에 들어오는
  기본은 수락(초대·신청)이고, 직접 입력은 앱 안 쓰는 사람과 혼자 쓰기의 길이다.
- **빈 칸이 없으면 수락이 막힌다**(서버 409는 정원 기준, 앱은 빈 칸 기준). 방장이
  푼다 — 이름 적힌 칸을 비우거나, 인원 수를 늘린다. 어느 이름을 죽일지 앱이 짐작하지
  않는다.

### 3.3 들어오는 길과 수락 주체 (로아 파티찾기 문법)

- **초대**(방장 → 사람)는 **받은 사람이** 수락한다. — 라운드 B: 함께한 사람 지목 초대.
- **링크도 초대장이다 (2026-09-05 개정): 유효한 초대 링크로 오면 방장 수락 없이 바로
  앉는다** — 자리는 §3.2 자동 배정, 빈 칸이 없으면 지목 초대와 같은 갈래로 신청으로
  내려앉는다(`st:"req"`). 링크는 방장이 골라 보낸 10분짜리 비밀 코드고, 디코 초대 링크
  문법(누르면 들어가짐)이 이미 몸에 밴 동작이며, 로비 단계의 방장 [수락]은 100% 형식적
  클릭이었다. 새는 링크는 지금처럼 재발급·내보내기가 막는다.
  비로그인 흐름: 초대 클릭 → **초대장 화면**("{방장}네 파티에 초대받았어요", §5.3 —
  읽기 전용 벌금표를 먼저 보여 주지 않는다) → [참여하기] = 시작하기 랜딩(가입 포함) →
  자동 착석 → 판(준비 또는 진행).
  (폐기 2026-09-05) "링크는 방을 찾아오는 길이라 신청으로 이어진다: … 방장이 신청
  카드에서 [수락] → 자리에 앉는다" — 로아 파티찾기 문법을 빌렸지만, 파티찾기는 공개
  목록에 거는 신청이고 링크는 초대장이다. 초대받고 왔는데 문 앞에 세워지는 게 낯설었다.
- **신청**(사람 → 방)은 **방장이** 수락한다 — 코드 없는 **노크**(함께한 사람 시트의
  [참여 신청])가 이 길이다. 거절은 기록도 차단도 없다 — 재신청 가능.
- 신청 목록은 준비 상태의 모집 카드와 판 중 [초대 링크] 창 양쪽에 같은 모양으로 있다
  (2026-09-05 — 옛 "로비와 파티 서랍(방 칩)"). 우하단 카드는 알림만 한다 —
  카드에만 두면 동시 신청이 덮어써지고 놓치면 수락할 길이 없다(실제 있었던 결함).
- **파티 하나 규칙은 서버가 지킨다.** 어느 방에서든 st:"ok"가 되는 순간 서버가 그 계정의
  다른 방 착석을 지우고 그쪽에 통지한다(Accounts가 `rm:` 색인 유지). 클라이언트 보정에
  기대지 않는다 — 실제로 한 사람이 두 방에 동시에 앉는 구멍이 있었다.
- **방 하나 규칙 (2026-09-07 사용자 확정, 구현 완료)**: 파티원은 [나가기]로 나가고, 방장은 [정산 끝내기](진행 중)나 [해산](시작 전)으로만 판을 떠난다. 로비를 보는 것은 떠나는 게 아니다(서기 소켓이 붙어 있어 파티원은 계속 센다). 코드 사실 둘이 근거다 — 남의 파티 화면에 들어가면(뷰어 부트) 내 판의 서기 소켓이 끊겨 파티원 자수가 막히고(`방장이 자리를 비웠어요`), 로그인 계정이 어느 방의 API 든 부르면 계정의 현재 방이 그 방으로 바뀌어 방송용 주소가 남의 판을 비춘다. 그래서 **내 판이 있는 브라우저에서 남의 초대 링크를 열면 뷰어로 열지 않는다**: 부트가 초대를 보관(`goldSettlement.pendingJoin` = {room, code})하고 해시를 걷은 채 내 앱으로 들어가 바로 묻는다 — 진행 중이면 `진행 중인 내 판을 끝내야 갈 수 있어요 / [정산 끝내기]를 누르면 결과지가 기록에 남고 판이 닫혀요. 초대는 파티 허브에 보관해 둘게요.` [취소] [끝내러 가기](판으로) · 시작 전이면 `내 판을 해산하고 초대받은 파티로 갈까요? / 시작 전 판이 없어져요. 자리와 이름이 지워지고 항목과 단가만 남아요.` [해산하고 가기](해산 뒤 초대로). 보관된 초대는 허브 맨 위에 `받은 초대가 기다려요. [지우기] [참여하기]`로 서고, 판이 없어진 뒤 [참여하기]는 초대장을 거쳐 들어간다. 반대로 **남의 파티에 앉은 채 [+ 새 판 만들기]**는 `{방장}네 파티에서 나가고 내 판을 만들까요? / 그 파티의 내 자리가 비어요. (진행 중이면) 벌금은 줄에 남아요.` [나가고 만들기]. 도착 규칙도 내 판이 있으면 남의 파티로 잡아채지 않는다. 문구 전부 초안. **서버 색인(`rm:a:`)은 자리만 든다** — 방장은 색인에 없어서 이 규칙은 지금 클라이언트가 지킨다; 자리 요청 때 그 계정의 방이 진행 중이면 거절하는 서버 검사는 뒤 슬라이스(계정부가 방에 상태를 물어야 함). 규칙 이전에 생긴 상태(남의 파티 착석 + 내 판)는 허브가 내 판 얼굴 아래 파티 줄 하나로 보여 준다. (폐기, 같은 날 논의) `두고 가기`(진행 중 판을 두고 남의 파티로) — 서기 소켓 이중 유지 개조가 필요했고 방송 주소가 남의 판을 비추는 문제가 남았다.
- 한 번 수락되면 **함께한 사람** 관계가 생겨 다음부터는 링크가 필요 없다.
- 방장의 통제는 셋: 초대를 보낸다, 신청을 받는다, 내보낸다.

- **초대장은 로그인 여부와 무관하게 거친다** (2026-09-05 표준화): 링크를 열면 누구든 초대장 → [참여하기].
  로그인 상태면 `{닉} 계정으로 들어가요.` 한 줄. 이미 그 파티 멤버면 초대장 없이 판으로. 로비의 파티 참여
  칸으로 온 사람은 그 자리에서 이미 눌렀으니 건너뛴다. (폐기, 당일) 로그인 상태의 링크 즉시 착석.
- **정원이 차면 `들어오려는 사람`으로 선다**: 본인에겐 `자리가 다 찼어요 — 방장이 자리를 만들면
  들어가요.`, 방장에겐 줄 `{닉} · 자리가 없어요 [거절] [자리 만들어 앉히기]` — 인원 +1 과 착석이 한 번에.
  (폐기 2026-09-05) 이름 `신청`과 `참여를 신청했어요 — {닉}님이 수락하면 들어가요.`, "빈 칸이 없으면
  수락이 막힌다".
- **내보낸 사람은 방장 승인으로만 돌아온다** (2026-09-05 확정): 서버가 내보낸 계정을 기억해(`x:`) 같은
  링크로 와도 `들어오려는 사람`에 `내보냈던 사람이에요`로 선다. [받기]가 승인이고 그때 기록이 지워진다.
  실수로 내보낸 경우의 복구 길이다. 본인에겐 `방장이 받아 주면 들어가요.`
- **초대 링크 수명은 10분 그대로** (2026-09-05 재확정 — 서버 초대가 아니라 방 초대라 짧아도 된다).
- **막힌 사람에겐 카드 하나와 나가는 문** (2026-09-05, 사용자 제안): 초대가 없거나 만료됐거나(로그인 상태도
  `expired`로 답한다) 내보내졌으면 빈 벌금표 위의 배너 대신 카드 하나 — 까닭 한 줄(`이 파티에 들어갈 초대가
  없어요 — 방장에게 초대 링크를 받아 주세요.` / `초대가 만료됐어요 — …` / `파티에서 내보내졌어요. 방장이 다시
  받으면 들어갈 수 있어요.`) + [로비로] · 앉은 파티가 따로 있으면 [내 파티로 돌아가기] · 내보내진 사람에겐
  [다시 들어가기](= 승인 대기). (폐기 2026-09-05) 배너 `이 방을 볼 권한이 없어요 — 방장에게 초대를 받아 주세요.`
  — 볼 것도 할 것도 없는 화면에 사람을 세워 뒀다.
  (버그 기록) 파티원 앱이 `you` 통지마다 소켓을 닫았다 다시 붙어, 그 사이(백오프 최대 15초) 계정 붙은 소켓이
  없어 내보내기 통지를 놓쳤다 — 이제 닫지 않는다.

**(폐기 2026-09-05) 함께한 사람 UI — 지목 초대·노크 전부.** 칩(로비·모집 카드·파티 서랍)과
시트, 보내는 쪽 함수를 걷었다. 사유: ① 게스트 계정끼리는 관계가 애매하고 ② 링크가 초대장이
된 뒤(2026-09-05) 재합류는 멤버십 유지(정산 끝내기는 아무도 안 내보냄)와 새 링크로 충분하며
③ 한 번은 같이 해야 생기는 목록이라 첫 합류를 못 도왔다. 서버의 관계(`mated`)·지목 초대
API·받는 쪽 카드는 호환으로 남는다 — 부르는 UI 가 없을 뿐이다. 아래 세부는 기록.

**라운드 B 세부 (확정 → 2026-09-05 UI 폐기, 기록):**

- **함께한 사람**: 수락 순간 양쪽에 생긴다. 최근 함께한 순 30명, 넘치면 오래된 것부터 밀림.
  목록이 곧 방 찾기다 — 검색은 없다. 무지성 노크는 관계 문턱이 막는다.
- **지목 초대**: 로비·파티 서랍의 함께한 사람에서 지목(자리는 그때 앱이 고른다 — 이름
  일치, 아니면 첫 빈 칸). **빈 칸이 없으면 부르지 않는다** — 불러도 문 앞에 세우는 것이고
  방장은 지금 그 명단을 보고 있으니, [수락]이 막힐 때와 같은 말로 알린다(§8).
  **유효 1분** — 로아 게임 초대처럼 실시간 악수다. 만료되면 조용히
  스러지고, 다시 지목하면 그만이다. 받은 사람이 수락하면 **방장 수락 없이 바로** 자리에 앉는다.
  이미 딴 파티에 있으면 수락 = 이전 파티에서 나가고 옮김(§8 확인 한 줄).
- **초대는 자리를 예약하지 않는다 — 문 앞에서 다시 센다** (2026-09-04 확정).
  예약하게 만들면 여럿을 부른 순간 방이 잠기고 1분짜리 초대가 그동안 자리를 죽인다.
  그래서 수락하는 순간 서버가 정원을 다시 보고, 갈래가 둘이다:
  - **지정된 자리만 남에게 넘어갔고 빈 칸은 있다** → 그 빈 칸에 앉힌다. 자리 지정은
    편의였지 약속이 아니다. 그대로 앉히면 한 줄에 두 사람이 겹친다(실제 있던 구멍).
  - **자리가 하나도 없다** → 튕기지 않고 **신청으로 내려앉힌다**(`st:"req"`, `inv:1`).
    방장이 콕 집어 부른 사람이라 조용히 돌려보내면 방장은 "왜 안 오지", 받은 사람은
    "왜 안 되지"가 된다. 내려앉으면 방장 신청 칸에 **까닭이 붙은 줄**로 서고 토스트가
    한 번 알린다. 인원 수를 늘려 [수락]하면 그대로 들어온다. 받은 사람에게도 왜
    안 앉았는지 알린다(§8). 관계(함께한 사람)와 자동 중단 무장은 **앉았을 때만** —
    내려앉은 사람은 방장이 수락하는 순간에 생긴다.
  - 정원은 **모집이 열려 있을 때만** 센다(서버 lobby.open, §0-7 용어). 출발 후 합류에는 정원이 없다(대기실 정원이지
    파티 정원이 아니다) — 그쪽은 빈 줄을 본인이 고르는 길이다(§3.2).
- **전달은 디스코드가 한다.** 푸시·폴링 배관 없음 — 앱을 **열 때와 창에 초점이 돌아올 때**만
  대기 중인 초대를 확인한다("초대 쐈어, 받아" → 알트탭 → 그 순간 확인).
- **노크(참여 신청)**: 함께한 사람을 누르면 시트 — `내 판으로 초대` / `{닉}네 판에 참여 신청`.
  신청은 **본인이 취소하거나 방장이 거절할 때까지 유지**된다(초대와 수명이 다르다 — 문 앞에
  서 있는 것이니). 거절해도 재신청 가능. 방장 쪽에서는 기존 신청 칸에 똑같이 뜬다.
- (폐기 2026-09-04) "공유 자동화 보류 · 파티 중 끄면 확인창" — 토글이 자수·중계를
  죽이지 않게 되면서(§5.7) 확인할 것이 없어졌다.

### 3.4 판의 수명

| 동작 | 하는 일 |
|---|---|
| [시작] (판·준비) | 판에 불을 켠다 — 화면 전환이 아니다. 시작 시각·판 이름이 찍히고 칸이 열린다. 첫 줄은 언제나 방장. **무대 우상단 모서리** — [정산 끝내기]와 같은 좌표다. **앞 판이 얼어 있으면 먼저 판 기록으로 보낸다**(실제 있던 구멍: 조건이 '살아 있을 때'뿐이면 얼린 판을 덮어써 통째로 잃었다). 파티원 화면도 같은 판이라 넘어갈 곳이 없다 — 자기 줄 칸에 불이 들어오고 자수 탭이 열린다. "판이 시작됐어요 [들어가기]" 카드는 자기 앱에 돌아가 있던 사람에게만 |
| [전부 비우기]·셀 비우기 (판) | 판 유지, 숫자만 리셋. 결과지에 무영향, **장부 로그에 "비움"이 남는다**(변경). 파티원의 "방금 바뀐" 카드에도 표시 |
| [정산 끝내기] (판) | 결과지를 판 기록에 남기고 판을 닫는다 → **끝난 판 화면(탭 3 읽기)** → [닫기] → 로비 (셈이 하나도 없던 판은 남길 결과가 없어 바로 준비 상태로). 끝낸 직후 방장의 일이 장부 읽기·우편 보내기라 로비 직행이 아니다. 재팟 = 로비 내 판 카드 → 판(준비) → [시작]. 닫힌 판은 준비 상태로 돌아간다 — 숫자만 비우고 자리·항목·이름은 그대로. **아무도 내보내지 않는다** — 정산 직후 전광판이 꺼지면 이상하니까. 오버레이엔 끝난 판이 계속 뜬다. (개정 2026-09-05 — 옛 도착지 "로비(자리 그대로)"의 로비는 지금의 준비 상태) |
| (폐기 2026-09-05) 자동 중단 | 폐지. **계정 붙은 자리가 있는 판만 무활동 24시간이면 얼린다**는 장치였다. 근거가 사라졌다: 방장 앱이 없으면 자수는 이미 잠기고(scribeOn=false), 오버레이에 옛 판이 남는 것은 "판을 언제 닫을지는 방장이 정한다"(아래)가 이미 받아들인 일이다. 얼림 상태·[이어가기]·얼림 띠·`paused`를 걷어 판 상태를 정확히 셋(준비/진행/끝남)으로 만든다 |

**여정표 (2026-09-05 확정)** — 나가기는 **두 동사**다. 화면에서 나가는 것(내비게이션)과
파티에서 나가는 것(탈퇴)을 한 동사로 묶으면 "잘못 눌러서 못 돌아가는" 문제가 생긴다
(실제 있었다: 나가기 → 멤버십 절단 → 소켓 거절 → 10초 뒤 화면이 죽고 [다시 참여]는
만료 코드에 걸렸다).

| 장면 | 동작 | 도착지 |
|---|---|---|
| 파티원, 판(준비·진행) | 화면만 벗어남 (브랜드 = 로비 문) | 로비 — **자리 유지**, 파티 카드 복귀 줄 [돌아가기] |
| 파티원, 판(준비·진행) | [파티 나가기] (마스트 빵부스러기 줄 오른쪽 — 2026-09-05, 옛 파티 칩 서랍; 준비 상태는 배너의 [나가기]; 확인 한 줄) | 탈퇴 → 로비. **자리는 시작 전이면 완전히 빈다(이름도 지움), 진행 중이면 줄은 장부로 남고 계정만 떨어져 `나감` 태그** (2026-09-05 표준화 — (폐기) "자리가 아이디를 기억해 재수락 시 제자리"). 내보내기도 같다. 돌아오면 보통 입장이고 진행 중엔 이름이 같으면 그 줄 |
| 파티원 | 다른 파티 초대 수락 | "옮길까요?" 한 줄 → 새 파티 직행 (로비 경유 없음 — 파티 하나 규칙이 옛 자리를 걷는다) |
| 방장 | [정산 끝내기] | 끝난 판 화면(탭 3 읽기) → [닫기] → 로비 |
| 파티원 | 방장이 끝냄 | 자수 잠김 + "끝났어요" → 탭 3 결과 읽기 → [닫기] → 로비. **결과가 파티원 로컬 판 기록에도 남는다** — 방장이 오프라인이어도 자기 벌금을 다시 본다. 저장은 방장 앱이 끌 때 보내는 '끝났어요' 마지막 한 장을 그대로 굳히고, 기록 목록에는 파티 이름표(예: 퐁퐁네 파티)를 달아 내 판 기록과 가른다 |
| 방장 | 세션 이탈 동사 | 없음 — [정산 끝내기]뿐. 브라우저 닫음 = 자리 비움 표시 + 무활동 24시간 얼림(기존) |
| (폐기 2026-09-05) | 파티원 [나가기] 뒤 "대기실에서 나왔어요 + [다시 참여]" 잔류 상태 | 시체 화면이었다 — 나가기는 확인 한 줄 뒤 로비로 간다 |

**판 화면의 수명 동사 자리**: 탭(벌금표·정산 장부·보낼 우편)은 오른쪽 그룹에 그대로 두고, **그 오른쪽 끝**에 [정산 끝내기]를 잇는다 — 탭을 좌측 끝까지 보내지 않는다. 글꼴·모서리·자간은 **로비 [시작]과 한 벌**이고(같은 우상단 좌표), 탭과는 **머리 선을 맞춘다**. 다만 **발치는 구분선에서 띄운다**: 탭은 아래 카드로 이어지는 서류철이라 선에 닿는 것이 맞지만 버튼은 아니고, 모서리가 둥근 상자가 선에 얹히면 얹힌 것도 뜬 것도 아닌 모양이 된다. 그래서 탭보다 6px 낮게 만들고 그만큼 올린다. 채움은 다르다 — [시작]은 그 화면의 주 동작이라 금색, [정산 끝내기]는 유령이다(벌금표에서 할 일은 칸을 세는 것이지 판을 닫는 것이 아니다). 마스트 왼쪽에는 [전부 비우기]만 남는다. **[처음부터]는 폐지** — 판을 닫는 문이 둘이면 하나는 못 찾는다(실증됨). ResetModal(프리셋·4/8인 선택 창)도 함께 폐지하고, 프리셋은 로비 항목 카드의 [프리셋] 문으로 옮긴다.

**[중단]도 폐지** (2026-09-03 결정, 2026-09-04 실제 반영). 브라우저를 닫아도 판은 살아 있고, 파티원에게 가는 말도 '방장이 자리를 비웠어요'와 사실상 같았다. 얼리는 일은 무활동 24시간 자동 중단이 맡고, 앱이 가진 것은 [이어가기]뿐이다. (2026-09-03 커밋은 메시지에 '중단 폐지'라 적었지만 실제로는 룩만 바꾸고 버튼은 남아 있었다 — 파괴적 편집 뒤 복구할 때 되돌아간 것을 확인 없이 보고한 결과다.)

- **모드도 판의 일부다.** 방장이 판 중에 메모장↔카운터를 바꾸면 파티원 화면도 따라간다.
  계정 붙은 자리가 있을 때 메모장 전환은 §8 확인창을 거친다 — 파티원 숫자가 사라져
  보이는 일에는 예고가 따른다.
- 사람 정리는 본인 [나가기]와 방장 내보내기(개별)뿐이다. **해산(전원 킥) 동사는 없다.**
- [나가기]·내보내기는 소속만 끊는다 — 자리는 미연결로 남고(이름·아이디 기억·쌓인 벌금),
  관계도 남는다. 없어지는 것은 "나가 있는 동안 안 보인다" 하나.
- **오버레이 규칙 한 줄 (2026-09-04 확정): 판이 있으면 그 판이 나가고, 로비면 아무것도
  안 나간다.** 화면과 똑같은 규칙이라 따로 설명할 것이 없다 — 로비 화면 = 빈 오버레이,
  벌금표 화면 = 오버레이의 벌금판.
  (2026-09-05 용어 정리) 여기의 "로비"는 옛 로비 = 지금의 준비 상태다: 준비 상태에서
  **모집 중(초대 링크를 만든 뒤)**이면 오버레이는 대기실 그림(§4.4, 이름 나열), 모집 전이면
  마지막 한 장(끝난 판)이 그대로, 진행 중이면 벌금판.
  (폐기 2026-09-05, 당일 — 구현하며 되돌림) "준비 상태면 언제나 대기실 그림" — 그러면
  [정산 끝내기] 직후 오버레이가 곧장 대기실로 바뀌어, 바로 위 "정산 직후 전광판이 꺼지면
  이상하니까"와 정면으로 어긋났다. 끝난 판이 방송에 남는 것이 우선이다.
  - **판을 언제 닫을지는 방장이 정한다.** [정산 끝내기]를 누르는 순간이 곧 "이제 치워도
    된다"는 뜻이고, 결과를 놓고 이야기하는 동안은 안 누르면 된다. 그래서 **유예도
    자동 종료도 없다** — 판을 닫는 것은 결과지를 만드는 행위라, 앱이 대신 결정하면
    사용자가 하지 않은 정산을 앱이 판 기록에 넣는 셈이 된다.
  - (폐기된 안) "낡은 판은 안 비춘다"는 시간 기준 — 정한 것은 **상태 기반**("지금 어느
    세션에 있나")인데 시간 기준은 같은 결과를 노린 다른 기계다. 유예를 지키려다 모델을
    갈아엎은 것이었고, 유예 자체가 필요 없어서 폐기했다.
  - 남의 판이 끝나고 나가면 내 쪽으로 돌아온다(방송 중 오버레이가 증발하지 않게).

### 3.5 결과지

- [정산 끝내기] 때 결과지가 판 기록에 남는다: 출처·이름·기간·
  총액·**참여자 전부**(줄이지 않는다). **배지는 보는 사람 기준이다** — 방장에게는 자기
  파티 판도 `내 판`(사람은 칩으로 보인다), 파티원에게는 `{방장닉}네 파티`. 저장은 판 전체 스냅샷으로 하되(로컬이니 용량 무관),
  **화면은 결과지(정산 요약)를 먼저** 보여주고 세부는 펼쳐야 보인다.
- 파티원 쪽 결과지는 받은 스냅샷에서 만든다(§5.4 그대로).

### 3.11 게스트로 시작 (2026-09-04 확정)

계정 문은 **셋**이다 — 로그인 · 가입 · **게스트로 시작**. 스트리머에게 익숙한 표준이고,
"주소를 받으려면 가입부터"라는 문턱을 없앤다.

- **게스트 = 닉 한 줄.** 아이디·비밀번호가 없을 뿐 가입과 같은 계정이라 방송용 주소도
  파티 참여도 **파티 열기도** 그대로 된다 (2026-09-05 — 게스트에게 [아이디 만들기]를
  먼저 요구하던 문턱은 "익명 = 주소만 받는 계정" 시절의 잔재라 걷었다). 남는 걱정은 하나 —
  방장은 파티의 앵커라 브라우저 저장소를 잃으면 방을 되찾을 길이 없다 — 그건 문턱이 아니라
  모집 카드의 한 줄(§8)이 말한다.
- **게스트의 [로그아웃]은 사실상 계정 버리기다** (2026-09-05) — 아이디·비밀번호가 없어 다시
  로그인할 길이 없다. 그래서 한 번 묻고(§8: [아이디 만들기] / [그래도 로그아웃]), 확인하면
  **파티에서도 나간다**(주인이 영영 못 오는 자리를 남길 이유가 없다) → 방장 화면의 그 줄은
  미연결(이름은 남고 아이디 표시가 떨어짐), 본인은 비로그인 로비로. 정식 계정의 로그아웃은
  자리를 둔다(아이디가 기억된다, §3.2) — 방장 화면엔 아이디 표시가 흐려진다(연결 끊김). 나중에 가입하면(upgrade) 주소·방·관계가 전부 따라온다.
  닉 규칙은 가입과 같다(2~3자) — 벌금판에 오르는 이름이라 다를 이유가 없다.
  비밀번호가 없어 선해시도 없으므로 **crypto.subtle 이 없는 자리에서도 열리는 유일한 문**이다.
- **닉을 반드시 받는다.** 예전에는 [가입 없이 주소 받기]가 계정을 조용히 만들어서
  모두가 기본 닉 `방장`으로 앉았다 — 게스트도 남의 파티에 들어갈 수 있으니 명단에
  방장이 둘 앉는 그림이 됐다. 기본값은 `손님`으로 바꾸되, 앱은 언제나 닉을 보낸다.
- **대문은 랜딩이다** (2026-09-05 재확정 — 같은 날 [계정 만들기] 단일 화면안을 대체).
  초대 링크·공유 설정 창의 [내 방송용 주소 받기] 어디로 들어와도 랜딩부터다.
  제목 `시작하기` / 문 두 장(2열 카드): [게스트로 시작] `이 브라우저에 저장되는
  계정으로 바로 시작해요.` · [계정 만들기] `어느 컴퓨터에서든 로그인해 같은 주소와 파티를 그대로 써요.`
  (2026-09-05 교체 — 옛 `…파티를 열 수 있어요`는 게스트도 열게 되면서 거짓이 됐다) / 발치 `이미 계정이 있어요 · 로그인`. 게스트가 왼쪽 —
  처음 온 사람 대부분이 이 문으로 들어간다.
  서브 화면(가입·게스트·로그인)은 ← 뒤로 + 폼 + 채운 버튼 하나뿐, 서로 오가는
  링크가 없다 — 분기는 랜딩이 전담한다.
  (폐기 2026-09-05) 가입 폼 밑 게스트 상자(`계정 없이도 쓸 수 있어요. …`)와
  로그인·게스트 화면의 "또는" + 같은 폭 버튼 두 벌 — 랜딩 카드가 같은 일을 한다.
  (폐기 2026-09-05) 공유 설정 비로그인 카드의 리드 `지금 이 브라우저의 벌금판만
  방송에 띄우려면 가입 없이도 돼요.` — 랜딩의 게스트 카드가 같은 말을 한다. 구성은 위에서
  아래로: 가입을 권하는 이유 한 줄(두 컴퓨터 같은 주소·파티 열기 — 전에는 "? 로그인하면
  어떤 게 좋나요?" 링크 뒤에 숨어 있었다) → 아이디·비밀번호·닉네임 → [가입하기] →
  구분선 아래 **게스트 상자**(`계정 없이도 쓸 수 있어요` 한 문장 + [게스트로 시작] —
  이유 없이 버튼만 두면 도망칠 구멍으로 읽힌다) → 맨 아래 `이미 계정이 있어요 · 로그인`.
- **[게스트로 시작]을 누르면 닉네임 한 칸**(목업 ⓑ): `벌금판에 올라갈 이름을 정해
  주세요.` + 큰 가운데 입력 + `2~3글자 · 이 브라우저에 저장돼요` + [시작하기].
  게스트가 뭘 할 수 있는지는 대문의 상자가 이미 말했으므로 여기서는 안 되풀이한다.
  게스트 칸과 로그인의 발치에는 나머지 문 둘이 `또는` 아래 같은 폭 버튼으로 선다.
- (폐기 2026-09-05) "들어온 자리에 따라 어느 문을 먼저 펼지 정한다" — 진입점마다
  화면을 갈아끼우면 같은 화면인 걸 못 알아본다. 실제 서비스는 계정 화면이 하나고,
  문맥이 바꾸는 것은 안내 한 줄뿐이다. 초대받은 파티원도 대문을 보는 값(시선 한 번)은
  게스트 상자가 바로 아래 있어 감수할 만하다.
- **두 컴퓨터 함정을 화면이 막는다.** 게스트 계정은 브라우저에 묶여 있어서, 게임컴에서
  받고 송출컴에서 또 받으면 **다른 계정 두 개**가 되고 "링크 두 개인데 왜 안 되냐"가
  된다. 그래서 셋을 말한다:
  1. **발급 직후 한 번만** 뜨는 다음 걸음 — `주소가 나왔어요` + 어디에 붙여넣는지 +
     `송출컴이 따로 있나요?` 한 줄. 그 화면의 채운 버튼은 [복사]다.
  2. **발급 버튼 옆의 경고** — `이미 다른 컴퓨터에서 받았다면 여기서 또 받지 마세요`.
     이게 함정의 입구라 여기서 막는다.
  3. **계정 권유를 그 문제의 답으로** — `두 컴퓨터에서 같은 주소를 쓰려면 계정을 만들어
     양쪽에서 로그인하세요`. 추상적인 이득으로 두면 안 읽힌다.

### 3.6 자수 · 가입 없이 (변경 없음)

- **되돌리기 칩 (2026-09-07 사용자 확정, 구현 완료)**: 자수 카드마다 서버가 200 을 준 순간 항목별 {n, t}를 서버 규칙 그대로 적는다 — 새로 누르면 t=지금(30초가 **다시** 차고), 창 안의 −1 은 n 을 하나 줄인다. n>0 이고 창 안이면 카드 발치에 칩 `↶ +n 되돌리기 · s초`와 줄어드는 금색 막대(0.5초마다 다시 셈), 창이 지나면 사라진다. 연타했으면 +n 이 늘고 우클릭마다 하나씩 준다. 30초 초과의 −1 은 토스트(2026-09-07: 쪽지는 화면을 밀었다). (폐기 목업안) 가장 오래된 누름 기준 시계 · 누름마다 동그라미 하나 — 사용자: 새로 누를 때마다 30초로 갱신되는 것 말고는 동의.

- 자수: 자기 줄 보통 항목만 +1/−1. 파티원 → 서버 → 방장 앱이 적음 → 푸시. 낙관 갱신 없음.
  **−1 은 되돌리기다 (2026-09-05)**: 내가 방금 올린 것만 **30초 안에**, 올린 수만큼만. 서버가
  항목별로 올린 수·마지막 시각을 기억해 거른다(409 `late`) — 자기 벌금을 나중에 슬쩍 깎는
  길을 막고, 그 뒤의 조정은 방장의 일이다. 30초는 복귀 타이머와 같은 수라 배울 것이 하나다.
  방장 부재 시 버튼 비활성 + §8 문구. **모으는 중(대기실)에도 방장 부재 표시가 떠야 한다.**
- 가입 없이 주소 받기(익명 계정)·업그레이드·닉네임 2~3자·[가입하고 시작] 흐름: 기존 그대로.

### 3.7 결정 경위 (다시 파지 않기 위한 기록)

- 자동 입장 → 승인제 → (롤식 무수락 검토) → **로아 문법으로 확정**: 초대는 상대가, 신청은
  방장이 수락. 링크는 소지자 표라 신청 쪽이 맞다.
- '쉼'·'해산'·'파티 모드'라는 발명 개념을 지웠다. 로비가 홈이고, 끝내기는 킥하지 않으며,
  사람 정리는 나가기·내보내기가 한다.
- 24시간 자동 처리는 '끝내기'가 아니라 '중단(얼림)'이다 — 잃는 것이 없어야 부담 없이 걸 수 있다.
- 지목 초대는 1분짜리 실시간 악수다(로아 게임 초대처럼). 전달 배관은 안 깐다 — 디스코드
  음성이 전달이고, 앱은 열 때와 초점이 돌아올 때만 확인한다. 노크는 문 앞에 서 있는 것이라
  취소·거절 전까지 유지된다. 공유 자동화는 어려워서 보류했다 — 토글 유지 + 파티 중 확인창만.
- 2026-09-04 **명단 문법 확정 — 사용자설정 방(워3 커스텀)**. "치는 만큼 생기는 줄"을
  버리고 **인원 수만큼의 칸**으로: 슬롯 문법(수락 = 자리 차지)과 방장 직접 입력(컴퓨터
  슬롯)이 한 그림이 된다. 빈 칸 = `(모험가N)` 자리표시(괄호가 "바꿔 적는 칸"을 말한다 —
  실리안 여덟은 진짜 사람처럼 읽혀 폐기), 수락은 빈 칸을 위에서부터, [시작]이 빈 칸을
  그 이름 그대로 판에 넣고 이름은 판에서 고친다(벌금이 생기는 순간이 이름 적는 순간).
  파티원 흐름은 "가입 – 수락 – 끝"으로 끝나야 한다는 요구가 출발점. 판 도중 합류자는
  자기 줄을 **본인이** 고른다(벌금 승계, 선택 후 잠김 — Splitwise 의 claim 과 같다).
  [수락 ▾] 자리 지정·[4인]/[8인] 이름 채우기 폐지, 인원 스테퍼는 "인원 수 = 칸 수"로
  부활. 대기실(수락 대기 줄)·슬롯 열림/닫힘 상태 기계는 검토 후 안 만들기로 — 링크는
  아는 사람에게만 흘러서 넘칠 일이 드물고, 드문 가득참은 안내문이면 된다. 아이디는
  어디서나 **앞 두 글자 + 점 넷**, 호버 공개도 없다(방장 화면이 통째로 송출된다).
- 로비 홈은 네 안(한 열·2열 벤토·유령 표·화면 하나)을 같은 장면으로 그려 견줬고 **벤토를
  채택**했다 — 사용자 결정. 화면 하나(로비 화면 삭제, 시작 전 벌금표 + 상설 레일)가 구조로는
  더 순수하지만 보드 대수술이라, 배포 후 개편 후보로만 남긴다. 벤토의 결점(모으기 표면이
  시작 후 서랍으로 이사)은 서랍 내용을 모으기 열과 똑같이 구성해 절충한다.
- 2026-09-05 **층 구조 확정 — 로비 → 판.** 파티원 UX 검토에서 출발: 파티원 화면이 v1
  시청자 뷰에 참여 배관을 덧댄 것이었고, 홈(옛 로비)이 방장 준비실이라 순수 파티원에게
  집이 없었다. 3층안(로비 → 대기실 → 세션)을 거쳐, 로비가 집 기능을 갖자 대기실이
  얇아져 **위 문단의 "화면 하나" 후보를 채택** — 시작 전 벌금표 + 판 위 초대 줄. 로비의
  "파티 목록/문패"(함께한 사람의 방 + 서버 스냅샷)는 같은 날 폐기: 초대는 토스트 문법,
  앉은 사람은 판에 있어야 하고, 아는 방장 문패는 첫 합류를 못 돕는데 재합류는 멤버십
  유지가 이미 한다. 로비 입장은 초대 코드/주소 입력칸(비밀 파티 문법). 링크는 초대장으로
  격상(즉시 착석), 신청은 노크만. 나가기는 두 동사(화면 이동 / 탈퇴), 끝내기는 결과
  화면 경유, 파티원 로컬 기록 신설.

### 라운드 B 서버 추가 (§4 보충)

- Accounts DO: `f:<id>` = 함께한 사람 [{id, t}] (최근 순 30, 수락 때 양쪽 갱신. 닉은 표시할 때
  `u:` 에서 읽는다). `inv:<받는이>:<보낸이>` = {room, seat, t} — 60초 지나면 읽을 때 버림.
- `POST /api/invite` (Bearer) `{to, seat?}`: 함께한 사람에게만. 분당 10회 리밋.
- `GET /api/auth/me` 응답에 대기 중 초대(신선한 것만)와 함께한 사람 목록 동봉.
- `POST /api/r/:id/join`: (a) `{inv:1}` — 유효한 지목 초대가 있으면 **즉시 st:"ok"** + 자리 힌트를
  서기에 전달. (b) 코드 없이 — 신청자가 방장의 함께한 사람이면 **st:"req"**(노크). (c) `{j:코드}`
  — **유효한 코드면 즉시 st:"ok"** (2026-09-05 개정, §3.3 — 만석이면 지목 초대와 같은
  갈래로 st:"req" 내려앉음). 노크·신청 취소는 기존 /leave 가 st:"req" 도 지우게.
- (폐기 2026-09-05, 당일 — §3.0) 파티 목록 스냅샷 `rs:<roomId>` — 로비 파티 목록
  자체를 접으면서 설계 단계에서 폐기. 로비 입장은 초대 코드 입력칸이 맡는다.
- **초대 코드 색인 (2026-09-05, §3.0)**: 코드 8자만 치고 들어올 수 있게, Room이 초대를
  발급할 때 Accounts에 `c:<CODE>` = `{room, exp}` 한 줄을 민다(만료는 초대와 같고,
  지난 것은 읽을 때 버린다 — `inv:` 청소 문법). `GET /api/j/:code/resolve` →
  `{roomId}` 또는 404. 주소 붙여넣기는 색인 없이 된다(주소에 방·코드가 다 있다).

Cloudflare Workers + DO. `Room` 개편 + `Accounts`(idFromName("global") 하나) 신설.
`Handoff` 클래스는 마이그레이션 안전을 위해 export만 남기고 라우트를 뗀다
(이사·복구 코드 폐기 — 기기 이사 = 로그인). wrangler.jsonc에 v2 마이그레이션
`new_sqlite_classes: ["Accounts"]` 추가.

### 4.1 Accounts DO

저장: `u:<id>` = {id, nick, salt, ph, created, seen, obsToken, cur} / `t:<obsToken>` = id /
`s:<token>` = {id, exp} / 레이트리밋 가드.

| 경로 | 하는 일 |
|---|---|
| POST /api/auth/register `{id,pw,nick}` | 검증(id 4~20 영숫자, pw 64hex, nick 2~3자) → 계정+세션+OBS 토큰 → `{id,nick,token,obsToken}`. 중복 id 409 |
| POST /api/auth/login `{id,pw}` | → 같은 형태. 실패 401, 리밋 429 |
| POST /api/auth/logout `{token}` | 세션 삭제 |
| GET /api/auth/me (Bearer) | `{id,nick,obsToken,cur}` + 세션 연장 + seen 갱신 |
| POST /api/auth/nick (Bearer) `{nick}` | 닉 변경 → cur 방의 Room DO에 통지(/nick-changed) |
| POST /api/auth/obs-reissue (Bearer) | 새 OBS 토큰(옛 것 즉시 무효) → `{obsToken}`. **계정당 24시간 창 5회**, 넘으면 429 (§5.8) |
| POST /api/auth/anon `{nick?}` | 게스트 계정 생성 (§3.11) → `{id, nick, token, obsToken}`. 닉은 가입과 같은 규칙(2~3자), 어기면 400. 안 보내면 기본 `손님`. 가입과 같은 레이트리밋 |
| POST /api/auth/upgrade (Bearer) `{id,pw,nick}` | 익명 계정에 진짜 아이디·비밀번호·닉네임을 붙인다. 방·OBS 토큰·멤버십은 그대로. 이미 정식이면 409, 아이디 중복 409 |
| GET /api/o/:token/resolve | `{roomId, look}` 또는 404. seen 은 **반나절에 한 번만** 갱신한다 — 유휴 오버레이가 이 주소를 되풀이해 묻는데, 매번 쓰면 그대로 스토리지 쓰기가 된다 (§5.8) |
| POST /api/auth/look (Bearer) `{look}` | 이 계정의 오버레이 외형을 저장한다. 사람마다 주소가 하나씩이니 외형도 사람마다다. 저장하면 OBS를 다시 안 건드려도 다음 접속부터 반영된다 — "OBS는 한 번만 넣는다"는 약속을 지키려고 주소 파라미터 대신 서버에 둔다 |
| (내부) /verify `{token}` | → `{id}` — 메인 fetch가 방 API 인증에 씀 |
| (내부) /touch `{id, roomId}` | cur·seen 갱신 |

alarm: 주 1회 스캔, seen 1년 초과 계정과 그 세션·토큰 삭제(그 계정의 방 storage도 청소 시도).

### 4.2 Room DO

(2026-09-06 모델) `hasParty()` = `party` 표시(로비 열기가 켠다) 또는 열린 로비 또는 끝나지 않은 진행 중 판(state.roundId && !end).
/join 은 초대의 무효·만료를 먼저 답하고, 그 다음 판이 없으면 `{error:"no party"}` 409. `inviteOk(inv)` = 서기 소켓이 붙어 있거나 exp 가 남았을 때;
마지막 서기 소켓이 끊기면 exp = max(exp, now+10분). GET /invite 는 살아 있는 코드를 그대로 준다(없으면 null). 코드 색인(계정부)은 7일 포인터일 뿐이다.
/end = endParty: 명단 비움(각자 you:null + end:1, 색인 해제, 소켓 닫기) · `x:` 전부 삭제 · state 에 end:1 · lobby 닫기 · party 삭제. 90일 정리도 같은 함수.
코드 뷰어 소켓은 판이 없어도 붙는다(끝난 결과지가 새 판 열림을 받게). 정원은 로비가 열린 동안(시작 전)만 센다.

저장: `owner`(계정 id), `state`, `invite {code,exp}`, `lobby {open, cap, since}`,
`m:<acct>` = `{nick, rowId|null, st:"ok"|"req", t}`, 자수 가드.
WS attachment: `{k:"v"|"scribe", acct|null}`.

메인 fetch가 Bearer를 Accounts /verify로 풀어 `x-acct` 헤더로 전달한다. owner 체크는 DO가 한다.

| 경로 | 하는 일 |
|---|---|
| POST /api/my/room (Bearer) | 자기 방 get-or-create → `{roomId, invite, lobby}` |
| PUT /api/r/:id/state (owner) | 저장 + 전 구독자 브로드캐스트. 본문에 `bindings:{acct:rowId}`가 있으면 멤버 rowId 갱신. 푸시마다 rowId가 사라진 멤버는 같은 닉의 행으로 자동 재연결, 행 이름이 바뀐 멤버는 nick… 아니, 멤버 nick이 원본 — 행 이름은 방장 장부가 원본이므로 여기서 건드리지 않는다 |
| GET /api/r/:id/read (owner) | `{state}` — 새 기기 로그인 후 장부 앉히기 |
| POST /api/r/:id/invite (owner) | 새 초대 `{code, exp: now+10분}` (옛 코드 무효, 멤버 무영향) |
| POST /api/r/:id/lobby (owner) `{open, cap?}` | 모집 열기/닫기(§0-7 용어). 열면 since 기록. 브로드캐스트. (6시간 alarm 은 §1 대로 폐기) |
| GET /api/r/:id/members (owner) | 멤버·신청 목록 `[{acct,nick,rowId,st,t}]` |
| POST /api/r/:id/member (owner) `{acct, action:"approve"\|"remove"}` | 수락 / 내보내기·거절·해제(=삭제). 대상 계정의 뷰어 소켓에 `{kind:"you"}` 통지. **remove 는 통지 뒤 그 소켓을 닫는다** — 안 닫으면 브라우저가 끊김을 알아챌 때까지 2~3초 동안 못 보는 판이 화면에 남는다 |
| POST /api/r/:id/join (Bearer) `{j}` | 초대 유효 검사 → **즉시 `st:"ok"`** (2026-09-05 개정 — 정원이 남았으면 앉힌다; 만석이면 `st:"req"`로 내려앉힘, 지목 초대와 같은 갈래). 서기 소켓에 `{kind:"join"}`. 이미 멤버면 현 상태 반환. (폐기) "언제나 st:req — 방장이 수락해야 앉는다" |
| POST /api/r/:id/seat (Bearer, st=ok) `{rowId}` | 판 도중 합류자의 자리 고르기 (§3.2). rowId 없는 멤버만, rows2 에 있는 `a:0` 줄만. 이미 앉았으면 409 seated, 남의 줄이면 409 taken. 성공 시 서기 소켓에 `{kind:"seat", acct, nick, rowId}` — 서기 앱이 자리·줄 이름을 맞추고 state 를 민다 |
| POST /api/r/:id/leave (Bearer) | 본인 멤버십 삭제, 서기에 `{kind:"left"}` |
| POST /api/r/:id/confess (Bearer, st=ok) `{rowId,colId,dir}` | dir ∈ {1,−1}, 본인 rowId만. 서기 소켓으로 전달, 서기 없으면 409 `{error:"scribe-off"}` |
| WS /api/r/:id/live `?s=\|?j=\|?o=` | 구독. 접속 즉시 `{kind:"hello", you, scribeOn}` + `{kind:"state", state}`. 권한 없으면 `{kind:"denied", why:"invite"\|"member"}` 후 닫기 |
| WS /api/r/:id/scribe `?s=` (owner) | 방장 앱 채널. 접속 시 `{kind:"members", list}`. 이후 `{kind:"join"\|"left"\|"confess"\|"nick"}` 실시간. 연결 수 0↔1 전환 시 전 구독자에 `{kind:"presence", scribeOn}` |

- `you` = `{nick, rowId, st}` 또는 null. state 브로드캐스트에 `scribeOn` 동봉.
- 하이버네이션 ping→pong 자동 응답(v1 그대로). 뷰어 메시지는 전부 무시.
- state 90일 무갱신 alarm: state만 삭제, 방·멤버·owner는 유지. 로비 6시간 alarm과 공존
  (알람 하나로 다음 만료 시각을 관리).
- v1 라우트(/api/rooms, key 기반 state/kill/peek, handoff, recovery, archive) 삭제.

### 4.3 state 스키마 (앱 → 서버, 서버는 해석하지 않음)

v1 스키마(board/cols/full/fx/spin/look/name)를 유지하고 두 가지를 더한다:

```json
{ "...v1 필드 그대로...": "",
  "lobby": { "n": 4, "cap": 8, "names": [{ "n": "냥슬", "live": true, "host": true }] },
  "rows2": [{ "rowId": "r7", "n": "꿀멩", "a": 1 }] }
```

- `lobby` — 모집이 열려 있는 동안만. **오버레이**가 대기실을 그린다(§4.4). 파티원 앱은
  2026-09-05부터 대기실 카드가 아니라 판(준비)을 그리므로 이 필드를 화면에 안 쓴다 —
  `대기실 n/정원` 상태 말 한 줄에만 쓴다. 빈 칸((모험가N) 자리표시)은 안 싣는다 — 밖에서
  보면 있지도 않은 참가자로 읽힌다.
- `rows2` — rowId↔이름 목록. 파티원 앱이 자기 줄(you.rowId)을 찾는 데 쓰고, `a`(계정
  붙음, 0/1)가 판 도중 합류자가 고를 수 있는 줄을 가린다. 아이디는 절대 싣지 않는다.
- `full.log`는 v1처럼 최근 200건 (자수 반영 확인용으로도 쓰인다).
- `ovSlide` (2026-09-06) — 오버레이 슬라이드 모드. `false` 만 끔이고 없으면 켬이라 기존 사용자도 켜진 채 시작한다(사용자 확정). `ovNet`·`ovSum`·`ov.off`(끈 항목)는 그대로 실리고, 슬라이드에서는 "무엇을 돌릴지"가 된다.

### 4.4 페이지 (worker/src/page.js)

- **연출 순서와 룰렛의 우선권 (2026-09-05 검증·수정)**: 카드(알림) → 금액 스와이프(줄·총액) →
  순위 이동의 순서는 그대로. **룰렛은 큐를 기다리지 않는다** — 카드가 떠 있거나 금액이 굴러가는
  중이어도 그 위에 즉시 뜬다(버그 기록: 카드·스와이프 뒤로 밀려 STOP 무렵에야 떴다).
  **STOP** 은 자유 회전의 각도를 이어받아 감속한다(버그 기록: 클래스를 먼저 벗겨 0도로 튄 뒤
  다시 돌았다 — "STOP 을 누르면 다시 돈다"). **건너뛰기**(서기의 skipAt·fast)를 방송도 따른다 —
  도는 중이면 240ms 에 목표 자리로 세우고, fast 면 남은 박자를 320ms 로 줄인다.
  (버그 기록) 스와이프가 줄을 옛 이름 열쇠로 찾아 금액이 안 굴렀다 — 줄 열쇠(rowKey)로 통일.
  빼기·취소 카드는 `정정 · {항목} −{금액}`.
- **원판 급정거 (2026-09-07 사용자 지적, 수치로 확인).** 멈춤 곡선 `cubic-bezier(x1, s0·x1, x2, 1)` 에서 x2 는 "꼬리 길이"가 아니라 반대다 — 크면 끝까지 속도를 유지하다 마지막 0.1초에 선다. 느긋하게 x2=.9 는 남은 시간 2% 에 315°/s, 보통 .72 는 36°/s. 고침: 보통 .55(14°/s), 느긋하게 .40(14°/s, aim 1.4 로 바퀴는 적고 뒤에서 오래 미끄러짐). 서기 원판·방송 원판 같은 값. 사용자: 보통이든 느긋이든 급정거는 절대 안 됨.
- **외형 기본값 강제 적용 (2026-09-07 사용자 확정).** 다음 배포에 모든 사용자에게 한 번: 슬라이드 켬 · 알림 켬 · 감속 느긋하게 · 원판 · 새틴 (`lookMig2` 표시, 그 뒤 고른 것은 그대로; 끈 항목·합계·순액은 안 건드림).
- **감속 슬라이더 (2026-09-07 사용자 확정: 슬라이더 + 프리셋 보통·느긋하게·직접).** `감속` 줄에 [보통][느긋하게][직접]과 `짧게 ─●─ 길게` 슬라이더(0~100, 1 단위, `relay.spinGlide`, 판에는 `spin.glide`). 슬라이더를 만지면 [직접]이 켜진다([직접]은 표시일 뿐 값을 안 바꿈). 멈춤 곡선 x2 는 **지수**로 — 0 → .62, 50 → .30, 100 → .15 (사용자: 선형 말고 로그·지수 느낌, 극단도 되게). 보통 10(x2 .53) · 느긋하게 30(.40), 기본 30(강제 적용에 포함). 바퀴 수(aim 1.7)는 고정. 어느 값이든 급정거 없음(x2 ≤ .62 → 남은 2% 에 20°/s 이하). 안내 `끝에서 미끄러지는 길이예요. 다음 판부터.`(초안). (폐기, 같은 날) 선형 .62→.32 + aim 연동, 프리셋 25/75, 5 단위 · 그 전의 두 단 보통/느긋하게(`spinEase`).
- **감속 '느긋하게'** (2026-09-05): 룰렛 외형의 선택지 `감속 보통 / 느긋하게`. **기본은 느긋하게**(2026-09-07 사용자 확정, 외형 기본값 정리: 슬라이드·알림 켬·느긋하게·원판·새틴; 보통은 `spinEase:"normal"` 로만, 옛 저장값 `gentle` 은 기본과 같아 비움; (폐기) 기본 보통). 판(spin.ease)에
  실려 서기 원판과 방송 원판이 같은 곡선을 쓴다 — 바퀴 수 목표 1.7→1.4, 꼬리 0.72→0.9(끝에서
  오래 미적인다). 속도(spd)는 느리게 고정 그대로.
- **미리보기의 예시 판** (2026-09-05, ①B 확정): 미리보기 창(`?fit=1`)인데 그릴 판이 없으면
  (시작 전·모집 전이라 서버에 판이 없거나 dead) 체커보드만 두지 않고 **예시 명단(예시 방과
  같은 여덟 명)을 지금 외형으로** 그리고 발치에 리본 `예시 화면이에요 — 지금은 방송에 나가는
  게 없어요. [시작]을 누르면 현황판이, 파티원을 모으면 대기실이 여기 떠요.` 이 창은 옛
  [테마 미리보기]를 대신하는 자리라 첫 판 전에도 테마를 판단할 수 있어야 한다. 앱은 미리보기
  주소에 지금 외형(`t`·`bg`)을 실어 보낸다 — 판을 민 적 없는 계정은 서버에 look 이 없다.
  OBS 안(isPreview 거짓)에서는 절대 안 그린다. (폐기) ⓐ 안내 한 줄만 — 테마를 볼 수 없었다 /
  ⓒ 시작 전 판(0점)을 실제로 송출 — "끝난 판은 새 판 시작까지 유지"와 충돌.

- **오버레이 줄의 열쇠는 줄 고유번호다** (2026-09-05): 앱이 board 줄마다 `k`(줄 id)를
  싣고, 페이지는 번쩍임·이동표시·이전값 비교·룰렛 대상 찾기를 전부 이 열쇠로 한다.
  (버그 기록) 이름을 열쇠로 쓰던 시절, 닉이 겹치면 두 줄이 한 열쇠를 나눠 써서
  남의 벌금 효과·이동표시가 내 줄에 붙었다. 옛 스냅샷(k 없음)은 이름으로 물러난다.

- `/r/:id` — 두 얼굴 유지. 브라우저 → 앱 `#live=ID&j=CODE`(해시의 j를 그대로 넘김).
  OBS(방송 판별은 v1 로직) → 오버레이, WS는 `?j=` 인증. 코드 없음·만료 → 침묵 +
  (obsstudio.getStatus로 방송 중 아님이 확인될 때만) §8 안내.
- `/o/:token` — **판별 없이 언제나 오버레이.** 방송 프로그램은 종류가 많고(프리즘·vMix·
  트위치 스튜디오…), 판별에 실패하면 방송에 앱 화면이 통째로 뜬다. 이 주소의 임무는
  하나(방송 소스)이므로 그것을 무조건 실행한다. `?mode=page`만 예외로 남긴다.
  `/api/o/:token/resolve`로 방을 찾고 그 방 WS(`?o=`)를 구독한다.
  resolve 404·denied → 침묵 + (방송 중 아닐 때만) §8 안내. WS가 denied로 닫히면
  60초마다 resolve부터 다시(방이 바뀌었을 수 있다).
  **브라우저로 열렸다고 판단될 때만**(기존 판별을 거꾸로 씀) 화면 아래에 작은 한 줄을
  얹는다(§8). 판별이 틀려도 방송에 새는 것은 그 한 줄뿐이다 — 실패의 대가를 뒤집는 것이
  요점이다. resolve 응답의 `look`이 있으면 URL 파라미터가 없을 때 그것을 쓴다(§4.1).
- 오버레이가 `state.lobby`를 받으면 순위표 대신 대기실을 그린다: 제목 `대기실 n/정원`,
  닉네임을 `·`로 이어 나열, 아래 §8 문구 한 줄 (목업 부속 참고, 기존 오버레이 톤 유지).
- **옛 주소 안내.** 지금 배포된 릴레이(계정 이전)의 주소가 실제로 살아 있고, 그 주소가
  OBS에 들어가 있는 사람이 있다. 배포하면 그 소스는 조용히 검은 화면이 된다.
  방에 `owner`가 없으면(= 없던 방이거나 계정 이전의 방) WS가 `{kind:"denied", why:"gone"}`을
  주고, 페이지는 침묵 + (방송 중 아님이 확인될 때만) §8 의 옛 주소 안내를 그린다.
  초대 코드가 없어서 막힌 경우(`why:"invite"`)와 문구가 다르다 — 옛 사용자에게는
  "새 주소를 받아 넣어라"가 정답이고, 초대가 없는 사람에게는 그게 오답이다.
- **슬라이드 모드** (2026-09-06 사용자 확정, 기본값): 항목 열과 순액을 열로 늘어놓지 않고 **합계 자리에서 번갈아** 보여 준다.
  순서는 합계 → 항목들(판의 열 순서) → 순액 → 합계, 합계 8초·나머지 4초. 아무도 안 쓴 항목과 전부 0인 순액은 건너뛰고, 돌릴 것이 없으면 합계만 선다.
  머리줄의 총액 자리가 라벨 자리다 — 합계일 땐 총액, 항목일 땐 항목 이름(룰렛은 ◎), 순액일 땐 `순액`(사용자 확정: 이름만). 라벨이 칸보다 길면 글자만 줄인다(2.0~3.4vw).
  값은 왼쪽으로 나가고 오른쪽에서 들어온다(표의 자연스러운 순서 항목 → 합계 → 순액 방향 — 사용자), 260ms, 줄마다 30ms 늦게.
  **값이 바뀌면**: 판이 앉기 전에 먼저 합계로 미끄러져 돌아오고, 그다음 스와이프·순위 이동이 붙는다 — "움직일 땐 합계, 조용할 땐 디테일". 앉으면 합계부터 8초 다시 센다.
  카드·스와이프·룰렛이 떠 있거나 아직 안 앉은 판이 있으면 돌지 않는다(0.7초마다 다시 본다). 칸 폭은 횟수·순액까지 처음부터 재서 지표가 바뀌어도 판이 숨 쉬지 않는다. 칸 폭 변수(--goldw·--netw)는 vw 로 적는다 — px 로 두면 창 크기가 바뀔 때 vw 글자만 커져 값이 판 밖으로 넘쳤다(2026-09-06 사용자 지적, 순액 단계). **0회는 0으로 표시한다, 빈칸으로 두지 않는다**(사용자 확정; 슬라이드 칸과 나란히 열 모두, 흐리게만).
  근거: 판 너비가 항목 3열 기준 70vw → 35vw 로 절반이 되어, 같은 면적에서 글자를 두 배로 키울 수 있다(오버레이는 작게, 시야를 안 가리게 — 사용자). 대신 판의 가로세로 비가 바뀌어 켜면 OBS 소스 크기를 한 번 다시 잡아야 한다(앱 안내 한 줄).
  앱: OBS 공유 설정의 '방송 화면에 넣을 열' 머리에 `항목 표시 방식`(사용자 지정 라벨) 세그먼트 [슬라이드] [나란히](초안) — 슬라이드가 기본. 미리보기는 슬라이드일 때 합계 자리가 2초 간격으로 도는 그림이고(값이 오른쪽에서 14px 들어오며 상자는 가로로 안 굴린다 — 2026-09-07 사용자: 스크롤바가 번쩍여 정신 사납다), 눈은 값 칸 위에 이름과 함께 선다(합계는 늘 도는 자리라 눈이 없다). 안내(초안): `합계 8초, 항목과 순액 4초씩 번갈아 나와요. 판이 좁아지니 OBS에서 소스 크기를 한 번 다시 맞춰 주세요.`
- **헤어라인** (2026-09-06 사용자 확정): 판 테두리 한 줄과 줄 사이 실선. 어두운 판엔 밝은 선(금 .55), 밝은 판엔 어두운 선(먹 .5). 그 이상은 없다 — 사용자: 과한 건 별로. look 에 `line: 1`(판 테마에만), 주소 파라미터 `line=1` 이 우선, 판 없는 테마엔 안 그린다. 테마 선택지 `어두운 판 · 테두리` / `밝은 판 · 테두리`(이름 초안)가 '다른 테마와 투명도' 아래에 선다. 경쟁 오버레이 비교(2026-09-06)에서 나온 마감 후보 중 고른 것 — 순위 색·배지·상단 띠·줄 스크림·유리는 목업까지만.
- **알림 카드 전환 (2026-09-07 사용자 확정: 카드는 한 장, 대신 다른 사건임을 알린다).** 카드가 이어지면 상자는 그대로 두고 옛 글이 0.07초 사라진 뒤 새 글이 0.14초 나타나며(두 글이 겹치지 않고 움직이지도 않음), 그 순간 카드가 4% 부풀고 테두리가 금색으로 번쩍한다(0.28초). 첫 카드는 번져 나오고 마지막 카드는 번져 사라진다. (폐기, 같은 날) 비웠다 채우기(사이에 판이 비침) · 새 카드를 투명에서 겹치기(두 장이 겹쳐 보임) · 글이 위로 흐르기(사용자: 이유 없음) · 불투명 카드를 위에 내려앉히기(여전히 이상).
- 나머지 렌더·테마(t/bg/s)·룰렛 재생·FLIP은 v1 그대로.

### 5.7 송출 — 스위치는 없고 계기판만 있다 (2026-09-04 확정)

**송출 토글은 폐지한다. 앱이 방송용 주소에 대해 갖는 문은 [주소 새로 발급] 하나다.**

토글이 하려던 일은 셋인데 전부 다른 것이 더 잘 한다:

1. *잠깐 방송에 안 띄우고 싶다* → **OBS 에 이미 소스 눈알이 있고 씬까지 나눠 쓴다.**
   핫키·스트림덱까지 걸려 있어서 앱으로 알트탭하는 것과 비교가 안 된다.
2. *낡은 판을 치우고 싶다* → 그건 스위치가 아니라 **판의 수명** 문제다([정산 끝내기]·유예).
3. *세되 시청자에겐 안 보여주고 싶다* → 1과 같다.

그리고 **있으면 해롭다.** 같은 일을 하는 스위치가 앱과 OBS 두 군데 생기면, 판이 안 뜰 때
스트리머가 원인을 두 군데서 찾는다. 게다가 앱 스위치는 OBS 밖에 있어 **OBS 만 보고 있으면
원인이 보이지도 않는다** — 방송 사고 중에 제일 나쁜 성질이다.

**남는 것은 계기판이다.** 스트리머가 실제로 묻는 것은 "끌 수 있나"가 아니라
**"지금 내 링크에 뭐가 나가고 있나"**다. 그 답이 있으면 안 보일 때 원인이 OBS 쪽인지
앱 쪽인지가 한 줄로 갈린다.

자리는 **헤더의 [오버레이 공유 설정] 버튼**이다 — 비로그인이든 로그인이든 이 버튼
하나가 창을 연다 (2026-09-05 확정):

- 버튼 얼굴은 **고정 라벨 "오버레이 공유 설정" + 송출 점**. 상태와 고치는 법은 창의
  주소 밑 문장과 버튼 툴팁이 말한다. 버튼 이름이 상태 따라 바뀌면 같은 버튼인 걸
  못 알아본다.
- **창의 구조** (2026-09-05 목업 확정 — 전면 재설계): 위에서 아래로
  1. **계정 줄** — 아바타(첫 글자)·닉·`게스트` 뱃지(정식 계정은 가린 아이디 `ak····`)·
     [닉 바꾸기]·[로그아웃]. 게스트면 그 밑에 `가입 없이 쓰는 중이에요 — …` 한 줄과
     [아이디 만들기], `아이디를 만들면 뭐가 달라져요?` 링크(게스트·아이디 차이 창 — 2026-09-06 오후; (폐기) `방송 주소, 한 번만 넣으면 돼요`, `로그인하면 어떤 게 좋나요?`). 맨 아래 두면 아무도 못 본다 —
     닉이 곧 벌금판의 내 이름이라 "내가 누구로 있는지"가 먼저다.
  2. **주소 카드** (금테 상자 — 창의 주인공): **주소와 주소의 속성이 전부 이 안에 산다.**
     고르는 것이 먼저, 주소는 그 결과다: **소스 나누기 그림 카드 두 장**(미니
     현황판·원판 그림이 "한 소스/나눈 소스"의 뜻을 말한다; 나눈 소스면 배치 안내
     한 단락. 머리말은 없다 — (폐기 2026-09-05) 접이문 라벨 `룰렛을 화면 전체에
     크게 띄우려면 — 소스 나누기`: 접기가 사라졌고, 같은 말을 나눈 소스 카드의
     문구가 한다) → 주소(가림)·눈·[복사] 한 줄(나눈 소스면
     `현황판 …?type=board`·`룰렛 …?type=spin` 두 줄 — 가려도 꼬리는 보인다:
     안 보이면 두 주소가 똑같아 보인다) → 상태 문장 한 줄(주소의 캡션) →
     발치에 조용한 `주소 새로 발급` 링크(설명은 누르면 뜨는 확인 창이 한다).
     **창 안 실시간 미리보기** (2026-09-05 확정): 주소 카드 첫 줄에 이 주소의 오버레이(`?fit=1`)를
     iframe 으로 그대로 띄운다 — 지금 방송에 나가는 그림이 그 자리에서 보이고, 테마도 그 자리에서
     고른다. 캡션 `지금 이 주소에 나가는 그림이에요 — 판이 없으면 예시가 떠요.`(초안). 창이 열린
     동안만 소켓 하나를 더 쓴다. (폐기 2026-09-05, 당일) [↗ 오버레이 미리보기] 새 탭 — 새 탭·
     체커보드·예시 판이 전부 "이게 방송에 나가는 건가"를 묻게 했다.
     **비로그인**은 같은 카드에 발급이 든다: 리드 한 줄 + [내 방송용 주소 받기] +
     `이 브라우저에 저장돼요. · 이미 계정이 있어요 · 로그인`, 카드 밑에 두 컴퓨터 경고.
     발급 버튼은 대문(§3.11)을 열고, 어느 문으로 끝내든 그 자리에 주소가 뜬다
     (신규는 "주소가 나왔어요" 카드, 로그인으로 끝냈으면 카드 없이).
  3. **카드 아래 줄 하나**: 넣는 법 문장 + `OBS에 넣는 방법` 링크(도움말은 링크
     무게 — 버튼으로 세우면 조작과 같은 소리를 낸다).
     - (폐기 2026-09-05) 파티원 줄 `파티원도 자기 방송에 띄우려면 이 주소를 그대로
       보내면 돼요.` + [파티원에게 보낼 메시지 복사].
  - **창 머리(제목·×)는 스크롤을 따라온다** — 긴 창 어디서든 닫혀야 해서.
    (폐기 2026-09-05) 발치의 [닫기] — §9-6 의 "손이 위로 올라가야" 문제를 머리
    고정이 대신 푼다.
     - (폐기 2026-09-05, 당일) 그림 카드를 글자 단추 둘([한 소스|나눈 소스])로 줄인 안 —
       글자 두 개로는 뜻이 안 통했다. 그림은 살리고 자리만 옮긴다.
     - (폐기 2026-09-05) 소스 나누기 접기 — 나눠 쓰는 사람의 주소 두 줄이 "~하려면"
       안내문 뒤 접기에 숨어 있었다. 주소도 선택도 카드 안으로.
  4. **오버레이 외형 설정** — 그대로 펼침. **비로그인은 어둡게 + 조작 잠금**:
     주소를 받으면 꾸밀 화면이라는 예고다. (폐기: "예시 방이라 로그인 없이도 조작" —
     발급 전 조작은 저장될 곳이 없어 헛손질이었다.)
- **계정은 이 창 안이다**: 헤더의 계정 드롭다운(로그인 버튼·계정 서랍)은 폐지하고,
  닉 바꾸기·로그아웃·게스트의 아이디 만들기를 창의 계정 줄로 옮겼다.
  "주소는 어디, 닉은 어디"가 갈라져 있던 것을 한 창으로 모은 것이 이번 통합이다.

**파티 칩에서는 점을 뗀다**: 그 칩은 파티 서랍 문이라, 성격이 다른 둘을 한 얼굴에 붙이면
점이 무엇을 말하는지 알 길이 없다.

**(폐기 2026-09-05, 당일)** 헤더 버튼을 갈아끼우는 통합 — 비로그인 [방송용 주소 받기]
CTA + 로그인 아바타 버튼("내 방송과 계정" 패널). 구현까지 갔으나 지시를 잘못 읽은
것이었다: 통합은 "밖의 버튼을 바꾸기"가 아니라 "계정 기능을 창 안으로 넣기"다. 밖의
문은 기존 그대로 두고, 발급이 대문을 여는 자리는 창 안으로 되돌렸다.
**(폐기 2026-09-05)** 버튼 얼굴에 상태어를 직접 쓰던 안(2026-09-04) — 위의 고정 라벨로
바꿨다.
**(폐기 2026-09-05, 당일)** 계정 섹션을 창 맨 아래에 두던 배치 — "저기에 계정이 있으면
누가 보냐". 위의 계정 줄로 올렸다.

네 갈래이고, **앱이 아는 만큼만** 말한다. 얼굴 라벨(`주소 없음`·`판 없음`·`방송 중`·
`연결 끊김`)은 폐기 (2026-09-05) — 지어낸 두 글자 상태어는 아무 서비스에도 없는 말이고,
특히 `판 없음`은 뜻도 안 통했다. 주소는 **내가 있는 판을 그대로 비추는 것**이라, 상태는
그 관점의 문장으로 말한다 ([시작] 같은 방장 전용 동사 금지 — 파티원도 이 창을 본다):

| | 문장 (§8) |
|---|---|
| `none` | 방송용 주소를 아직 안 받았어요. 받으면 이 자리에서 지금 뭐가 나가는지 알려줘요. |
| `down` | 서버와 연결이 끊겨서 갱신이 멈췄어요. 마지막으로 보낸 판이 그대로 떠 있어요. |
| `idle` | 아직 시작 전이에요 — 지난 판이 있으면 그게 떠 있고, [시작]을 누르면 이 판이 나가요. (초안 2026-09-05) |
| `recruit` | 대기실이 나가고 있어요 — 모이는 사람이 방송에 보여요. [시작]을 누르면 이 판이 나가요. (초안 2026-09-05, 모집 중) |
| (폐기 2026-09-05) `idle` | 이 주소에는 내가 있는 판이 그대로 떠요. 지금은 판에 있지 않아요. — 방장이 제 시작 전 판을 보며 읽으면 틀린 말. 이 표의 문장은 이제 **방장만** 본다(파티원 툴팁은 §5.3의 제 문장) — [시작] 금지 조건이 풀렸다 |
| `on` | 이번 판이 이 주소에 나오고 있어요. 방송에 안 보이면 OBS 쪽 소스를 확인해 주세요. |

**"OBS 가 실제로 받고 있나"는 세지 않는다.** 붙어 있는 것이 본인 OBS 인지 브라우저 탭인지
새어 나간 링크인지 앱이 구분할 수 없어서, 세어 봐야 틀린 말을 하게 된다.

**(폐기된 시도)** 토글을 남기려 했을 때 스냅샷에 `cast` 를 실어 **오버레이에게 "그리지
말라"고 부탁**하는 구현을 했었다. 판을 링크로 그대로 보내 놓고 브라우저에게 맡기는 것이라
링크를 쥔 쪽에서는 그냥 보였다 — **"꺼짐"이라 써 놓고 실제로는 안 꺼진** 것이다.
링크의 속성을 판의 스냅샷에 실은 것이 잘못이었다.

### 5.8 서버 사용량 (2026-09-04)

- **떠 있는 오버레이 자체는 거의 공짜다.** 하이버네이션 + `setWebSocketAutoResponse` 라
  50초 keepalive 를 런타임이 대신 답하고 DO 를 안 깨운다. 방장이 손댈 때만 깨어난다.
- **비싼 쪽은 "볼 것이 없을 때"다.** 오버레이는 죽은 상태(방 없음·자격 없음)에서 주소
  해석을 다시 묻는데, 1분 고정이면 켜 둔 채 방치된 OBS 소스 **하나당 하루 1,440번**이고
  그게 전부 계정부(전역 DO 하나)로 간다. 그래서 **처음 세 번만 1분, 그 뒤 물러나 5분 상한**.
  아예 끄지는 못한다 — 이 폴링이 유일한 복귀 수단이다.
- **`seen` 은 반나절에 한 번만 쓴다.** 용도가 "1년 미사용 계정 삭제"라 분 단위 정밀도가
  필요 없는데, 해석 때마다 쓰면 위 폴링이 그대로 스토리지 쓰기가 된다.
- **재발급은 계정당 하루 5회.** 이 라우트만 리밋이 비어 있었다(초대·로그인·자수는 다
  걸려 있다). 사용량보다 **전역 DO 점유**가 문제다 — 한 사람이 반복 호출하면 다른 사람의
  로그인·참여까지 느려진다. 주소가 샜을 때 쓰는 문이라 평생 몇 번이면 충분하다.

## 5. 앱 (src/GoldSettlement.jsx)

### 5.1 저장·부트

- **화면 = 주소 (2026-09-05 확정 — 뒤로가기 표준화)**: `#lobby` 로비 / `#board` 내 벌금판 /
  `#gen=이름` 판 기록 / `#live=…` 파티 판. 화면을 바꾸는 문(브랜드·`‹ 로비`·헤더 칩·[벌금판 열기]·
  [벌금판으로 돌아가기]·기록 열기·[정산 끝내기]·[닫기]·[지금 판으로]·[돌아가기]·[참여하기]·초대 수락)은
  전부 history 에 한 장 얹고(push), hashchange 하나가 화면을 정한다 — 뒤로와 앞으로가 대칭이다.
  **새로고침은 그 화면을 지킨다.** 도착 규칙(앉은 파티 → 살아 있는 내 판 → 내 판 → 백지면 로비)은
  **주소가 비어 있을 때만** 돌고, 결과를 replaceState 로 주소에 적는다. 첫 화면에서 뒤로가기는 사이트
  밖으로 나간다(가두지 않는다). 파티 판 ↔ 로비는 뷰어 여부를 부트가 정하므로 리로드로 갈아탄다 —
  뒤로·앞으로도 같은 길이라 사용자는 차이를 못 느낀다. 자리를 버리는 길(나가기·끝난 판 닫기·로그아웃)은
  replace — 없어진 파티로 뒤로가기가 돌아가지 않게. **모달·탭·확인창은 history 밖**(데스크톱 앱 —
  Esc 와 ×가 닫는다; 뒤로가기로 창을 닫는 건 모바일 문법).
  (폐기 2026-09-05) sessionStorage `gs-home` 표시(브랜드로 온 로비에서 자동 입장을 건너뛰던 장치) —
  주소 `#lobby` 가 그 뜻을 말한다. (폐기 2026-09-05) 판 기록의 `pushState({gsGen})` + popstate —
  주소 없는 한 장이라 새로고침이 화면을 못 지켰다.

- `goldSettlement.auth` = `{id, nick, token, obsToken}`.
- `goldSettlement.relay` — 외형 취향(look/ov/fx/spd/spinLook/wheelTheme/ovsrc)과 `on`,
  `room`(자기 방 id), `invite {code,exp}`, `lobbyDraft`(로비 초안 cols, 열려 있는 동안만).
  v1의 `rooms`/`active`/`rcode`는 읽어서 버린다(외형 취향만 이주).
- 해시 진입: `#live=ID`(&`j=CODE`), `#o=TOKEN`, 기존 `#live=CAFE22` 데모 유지.
  `#o=`는 앱이 resolve로 방을 찾아 뷰어로 연다.
- 선해시·API 층: `authApi`(register/login/logout/me/nick/obsReissue), `roomApi`(위 §4.2).
- **도착 규칙과 백지 판정 (2026-09-05, §3.0)**: 부트는 앉은 파티(서버 `cur`) → 살아 있는
  내 판(벌금표) → 내 판(준비) → 로비 순으로 고른다. **백지** = 내 판을 만든 적이 없음 —
  휴리스틱: `roundLive || log.length || 내 판 기록(참여한 판 기록은 제외) || 판 이름을
  고친 적` 중 하나라도 참이면 만든 것. 참여한 판 기록만 있는 순수 파티원은 백지로 남아
  로비가 홈이다. 로비는 라우트 상태(`atLobby`)로 서고, 브랜드 클릭·[나가기]·끝난 판
  [닫기]가 그 문이다. 뷰어(#live)에서 로비로 가는 것은 해시를 걷고 로비 표시를 남긴 채
  다시 여는 것 — 멤버십은 건드리지 않는다. 그 표시가 있으면 부트의 "앉은 파티 → 그 파티"
  자동 입장을 이 세션에서는 건너뛴다 — 안 그러면 브랜드 클릭이 도로 파티로 튕기는 고리가 된다.
- **첫 방문 온보딩** (2026-09-05): 백지 → 로비 → [+ 새 판 만들기] → 판(준비)에서 코스가
  시작된다. 옛 첫 방문 관문(튜토리얼 물음)은 판 진입 시점으로 옮긴다.

### 5.2 방장 흐름

(2026-09-05 정리) 옛 첫 네 항목은 2026-09-02~03의 초기 구현 메모였고 §3·§5.7 이 대체했다.
살아 있는 규칙만 다시 적는다:

- **모집**: 준비 상태의 판 위 초대 줄에서 [초대 링크 만들기] → `POST /api/my/room` →
  `POST lobby {open:true}` → 초대 발급. 모집 중 300ms 디바운스로 `lobby` 붙은 state 푸시
  (오버레이의 대기실 그림용, §4.3). 판 중에도 초대는 발급된다(§3.3 — 모집은 상설).
- **[시작]**: 판에 불을 켠다 — 앞 판이 얼어 있으면 판 기록으로 보내고, roundId·시작 시각을
  찍고, `PUT state`(bindings 포함, lobby 없이) → 모집 표시 닫기(open:false). 표는 이미 서
  있으므로 rows 를 새로 만들지 않는다.
- (폐기) [파티 모드 시작하기] 버튼 · 로비 화면(목업 ②) · 상시 위젯(목업 ③) · [출발]("로비
  초안 → 판 변환")/[로비 닫기] — '파티 모드'는 §3.7 에서 지운 개념이고, 로비 화면은 §3.1
  로 폐기됐다.
- **서기 WS**: 공유 켬(또는 로비 열림) 동안 상시 연결. join(로비 중)=위젯 갱신,
  join(출발 후)=수락 카드(목업 ④, 수락 시 줄 만들고 bindings 푸시), confess=장부 반영,
  nick=줄 이름 변경, left=위젯 갱신.
- **자수 반영**: pressCell과 같은 계산으로 bump + 기록 `{kind:"confess", ...press와 같은 필드}`,
  기록 표시는 "자수 — {항목} ±{금액}". 취소(역분개)는 기존 그대로 동작해야 한다.
- **오버레이 공유 설정 창 개편** (→ §5.7 이 최신; 이 항목의 "토글 유지"는 폐기됨): 비로그인 → 계정 섹션부터. 로그인 후: (폐기) 공유 켜기 토글,
  **내 방송용 주소**(가림+복사+재발급 — v1의 '주소 새로 발급' 대체), 초대 링크(로비 밖에서도
  발급 가능), 멤버 목록(닉↔id, [내보내기]), LookBody·소스 나누기(주소만 `/o/` 기반으로) 유지.
  **로그아웃 상태에서는 주 버튼이 하나다 — [내 방송용 주소 받기].** 누르면 익명 계정이
  조용히 생기고 주소가 나온다(§3-11). 결과가 어느 쪽이든 "내 링크"인데 문을 둘로 나누면
  "나는 어느 쪽인가"를 또 묻는 셈이라, 주소는 마찰 없이 주고 계정은 그 아래에서 이득을
  설명해 권한다(§8) — [계정 만들기]·[로그인], 지금 주소를 그대로 옮긴다는 말과 함께.
- **가이드 창** (문패는 §8 "주소와 계정, 어떻게 돌아가요?"로 교체됨) — 머리줄의 [OBS에 넣는 방법] 옆에 (옛 문패) **[로그인하면 어떤 게 좋나요?]**.
  내용은 두 칸 비교(`가입 없이` / `계정을 만들면`)와 `한 번만 / 팟마다` 두 장.
  목업: https://claude.ai/code/artifact/c0e25f63-7930-4485-b0d3-017b5559712c
  '옛 방식/새 방식'이라 부르지 않는다 — 사용자는 옛 방식이 있었다는 걸 알 필요가 없다.
  가입 없이 쪽의 첫 줄은 "링크를 받은 사람은 누구나 자기 방송에 띄울 수 있다"이다
  (못 하는 것은 자수뿐이라는 게 정확한 차이다). 맨 아래 줄은 파티원이 할 일이
  초대 누르기 하나뿐임을 말한다 — 몇 명만 이해해도 굴러가게 하는 문장이다.
- **주소 발급 화면의 안내** — 주소를 복사하는 순간이 사람이 가장 집중하는 지점이다.
  (a) `OBS·XSplit·프리즘 등 어떤 방송 프로그램이든` 된다고 적는다(§8). 지금 문구는 OBS만
  말해서 다른 걸 쓰는 사람이 "나는 안 되나"부터 떠올린다. (b) **[미리보기]** — 이미 있는
  `?fit=1` 장치로 팝업을 열어 방송에 뜰 그림을 넣기 전에 보여 준다. (c) "브라우저로 열면
  자세한 현황을 볼 수 있어요"는 §4.4 개편 이후 거짓이므로 지운다 — 읽기 화면은 앱이다.
- **나눈 소스를 고른 방장의 파티원 메시지에는 주소를 두 줄 담는다.** 가입 없이 방식에서
  파티원은 앱 화면을 안 거치니 `?type=board`/`?type=spin`을 스스로 붙일 방법이 없다.
- **오버레이 외형은 계정마다 따로다.** 앱에서 고르면 `/api/auth/look`으로 저장하고,
  그 사람의 `/o/` 오버레이가 그것을 쓴다. 방장의 외형이 전원에게 적용되던 것을 끝낸다
  (state 의 `look`은 외형을 저장하지 않은 계정의 기본값으로만 남긴다).
- (폐기 — [처음부터] 폐지, §3.4) '처음부터'는 방·링크·멤버십을 건드리지 않는다 (endShare 결합 삭제).

### 5.3 파티원 흐름 (2026-09-05 개편)

파티원 화면의 뿌리 문제는 v1의 "OBS 시청자 뷰"에 참여 배관을 덧댄 구조였다 — 파티원은
읽기 전용 벌금표를 보고, 신분(초대·신청·앉음·나감·끊김)은 표 위의 배너 한 줄이 중얼거렸다.
개편의 축은 **파티원에게도 장소를 주는 것**: 방장과 같은 화면(로비는 자기 것, 판은 방장의 것)을 권한만 좁혀 본다.

- **초대장 화면**: 코드 붙은 링크(`#live&j`)로 온 비로그인에게는 읽기 전용 벌금표 대신
  초대장 — "{방장}네 파티에 초대받았어요 — {판 이름} · {n}명 모임" + [참여하기] 하나.
  누르면 시작하기 랜딩(§3.11), 끝나면 자동 착석(§3.3 개정)으로 이어진다.
  코드 없는 시청자 링크(`#live=`)만 기존 읽기 전용 뷰 — 그 화면의 본업은 구경이다.
- **같은 판** (2026-09-05 — 대기실 미러안을 당일 대체): 착석한 파티원은 방장의 판을 그대로
  본다. 준비 상태면 명단·항목이 보이고 칸은 잠겨 있으며 [시작] 자리에 `방장이 시작하면
  함께 시작돼요`; 시작하면 자기 줄 칸에 불이 들어오고 자수 탭이 열린다 — 화면 전환 없음.
  판 이름은 글자만(연필 없음), 명단에 방장 뱃지·나 표시. 신청 중(st:req)도 같은 화면 —
  내 줄이 `수락 대기` + [신청 취소]. 판 도중 합류는 §3.2(자리 고르기) 그대로.
- **자수 UI**: st=ok && 자기 rowId 줄의 보통 항목 칸만 활성(금색 강조).
  클릭/우클릭 → `POST confess`. scribeOn=false → 비활성 + §8 문구. 409 응답도 같은 처리.
- **주소 표면은 하나** — 방장과 똑같이 [오버레이 공유 설정] 창 안뿐이다. (폐기 2026-09-05)
  화면 맨 위 "내 방송용 주소" 배너, 파티 칩 서랍의 주소 줄 — 파티원만 주소가 세 군데
  떠 있었다. (폐기 2026-09-05, 당일) 칩 서랍 자체 — 나가기는 마스트 빵부스러기 줄의
  [파티 나가기]로, 인원은 표가 말한다.
- **헤더 [오버레이 공유 설정] 버튼도 방장과 한 벌** — 파티원에게도 송출 점: 앉아 있고
  연결이 살아 있으면 on 계열, 끊기면 down. 문장은 CAST_WHY 그대로.
- [나가기]·화면 이동·끝남: §3.4 여정표. 데모(#live=CAFE22)는 기존 그대로.

### 5.6 자수 탭 · 방 표시 · 자수 알림

목업: https://claude.ai/code/artifact/55235237-baf2-4d6e-abff-0873db6469a4

- **자수 탭** — 탭 줄 맨 왼쪽에 [자수]. 파티원(st=ok)에게만 보이고 **기본 화면**이다.
  (폐기 2026-09-06 — 카드 B: 항목명 옆에 `1회 {단가}G` 또렷이 · 회수가 큰 숫자 · 밑에 칸의 굳힌 금액 · 룩은 벌금표 칸의 결) 원문: 보통 항목마다 큰 카드 하나: 항목명 · **내 벌금(횟수 × 단가)이 큰 숫자** · 그 밑에 `n회 · 1회 단가`
  (2026-09-05 — 사람이 묻는 건 "얼마 물었나"라서 횟수 대신 금액을 주인공으로). 카드를 누르면 +1회,
  **우클릭하면 −1회** — 벌금표와 같은 문법이라 규칙을 새로 배울 것이 없다.
  룰렛 항목은 흐리게 잠그고 `룰렛은 방장이 돌려요`. 카드 위에 내 닉네임과 내 벌금 총액.
  방장이 없으면(scribeOn=false) 카드 전체가 잠기고 위에 §8 문구.
  카드 안의 − 버튼과 '방금 … 취소' 줄은 두지 않는다 — 규칙을 하나로 줄인 결과다.
  (폰은 이번 범위 밖. 우클릭만 지원한다.)
- **30초 복귀** — 다른 탭에서 30초 동안 **아무 조작이 없으면** 자수 탭으로 돌아온다.
  스크롤·클릭 등 어떤 조작에나 타이머가 처음으로 돌아간다. **남은 초를 글자로 보인다**
  (2026-09-05 — `{n}초 뒤 자수 화면으로 돌아가요 — 누르면 다시 30초.`, 초안): 말없이 화면이
  바뀌면 "왜 바뀌었지"가 된다.
  안내는 **카드 맨 위 한 줄**에 둔다(표 아래에 두면 표가 길 때 화면 밖으로 밀린다).
  벌금표에서는 읽기 전용 문구와 한 줄로 합치고, 장부·우편에서는 복귀 안내만 남긴다.
  왼쪽에 도는 고리를 달아 글자를 안 읽어도 흐르는 것이 보이게 한다.
- **읽기 전용 표시를 벌금표 카드 안쪽 위로** 옮긴다. 바깥 배너는 어느 탭에서나 봐야 하는
  것만 맡는다 — 대기·거절·방장 부재·내 방송용 주소.
- **(폐기 2026-09-05) 방 표시 칩·파티 서랍** — 같은 날 유지 확정했다가 폐기. 사람 일은 줄의
  사람 버튼(§3.1)이, 판 중 초대 링크·신청은 도구줄 [초대 링크] 창이, 파티원의 나가기는
  빵부스러기 줄이, "진행 중인가"는 헤더 칩·마스트 상태 칩(§3.0)이, 연결 상태는 송출 점과
  줄의 `i`(끊기면 흐림)가 맡는다. 서랍은 표와 떨어져 있어 "어느 줄"을 이름으로만 고르게
  했다. 아래는 기록 — 머리줄에 하나. "어느 방인가"와 "잘 붙어 있나"를 같이 답한다.
  파티원 `{방장닉}네 파티 · N명 ●`, 방장은 계정 붙은 자리가 있으면 `내 파티 · N명 ●`,
  없으면 **`파티원 모으기`** — 혼자 판에 '파티'라는 말을 쓰지 않는다. 누르면 패널
  (파티원: 방장·인원·[나가기] — (폐기 2026-09-05) 내 방송용 주소 줄 / 방장: §3.1의 모으기 열과 같은 구성 +
  파티원 목록([자리 바꾸기]·[내보내기])). 수명 동사는 서랍이 아니라 판 우상단에 있다(§3.4).
  끊기면 표시 — 방장 `연결 끊김`, 파티원 `방장 없음` (안내이므로 빨강 금지, §9-4).
  지금은 방 이름 기본값이 `벌금 현황판`이라 어느 방인지 알 수 없다. 방장 닉으로 부른다.
  **서버가 hello 에 방장 닉을 실어야 한다**(저장은 이미 `ownerNick`으로 하고 있고
  내보내지 않을 뿐이다). 인원 수는 판의 줄 수로 센다.
- **자수는 방장의 '방금 누른' 카드에 섞는다.** 지금 자수는 숫자만 조용히 바뀌어서
  방장이 놓친다. 그 카드가 이미 되돌리는 자리이므로 새 장치를 만들지 않는다.
  자수 줄에는 **`자수` 표시**를 붙여 방장이 누른 것과 갈라 보이게 한다 — 안 그러면
  "내가 저걸 눌렀나?"가 된다. 머리글은 `방금 바뀐 N건`(누른 것만이 아니게 됐다).
  ✕(취소)는 그대로 동작한다. 덧붙여 자수로 바뀐 **칸이 잠깐 금색으로 번쩍이고**
  `{닉} 자수` 말풍선이 2초쯤 뜬다 — 방장의 눈은 판에 있으니 판에서 알려야 한다.
- **'방금 바뀐' 카드 줄의 색 띠** (2026-09-07 사용자: 정정 칩과 부호 색만으로는 시각적 구분이 약하다 — 증감도 마찬가지): 줄마다 왼쪽에 3px 띠, 늘면 붉게(--red) 줄면(정정·자수 정정) 푸르게(--blue). 칩 `자수`/`자수 정정`은 그대로.
- **자수 정정은 정정으로 읽힌다** (2026-09-05): 되돌리기 창(30초) 안의 −1 은 장부 줄 `자수 정정 —
  {항목} −{금액}`, 방금 바뀐 카드의 태그 `자수 정정`, 방장 판의 말풍선 `{닉} 자수 정정`,
  오버레이 카드 `정정 · {항목} −{금액}`, 되돌린 본인에게 토스트 `자수를 정정했어요 — 방금 것을
  되돌렸어요.`(초안). 창 밖의 −1 은 서버가 거른다(§3.6).
- **자수 잠그기는 만들지 않는다.** 트롤은 내보내기로 해결한다.
- **끊김 대응** — 서기 소켓 재시도 간격 상한을 15초에서 **3~5초**로 줄인다.
  탭이 열려 있어도 끊기는 경우가 있다(네트워크 튐, 브라우저의 절전형 탭, 메모리 부족).
  방장이 자기 화면에서 알아야 고칠 수 있으므로 방 칩이 그 상태를 보여 준다.

### 5.4 판 기록

목업: https://claude.ai/code/artifact/1dd38898-d04e-4282-84f4-a50ff58bca56

명칭 "지난 회차" → "지난 판" → **"판 기록"**. 자동 보관(출발·처음부터·파티 종료),
읽기 전용. 잠금·복원(교대) 삭제, 최근 20판, 수동 삭제는 확인창 유지.
서버 archive 관련 코드는 삭제 — 로컬 전용이다.

- **끝난 파티에서 튕겨 내지 않는다.** 지금은 방장이 공유를 끄거나 새 판을 시작하면
  파티원 화면이 그냥 빈다. 그런데 정산은 판이 끝난 뒤에 한다 — 그 순간 파티원 전원이
  자기가 얼마 보내는지 못 보게 된다. 마지막으로 받은 판을 로컬에 담아 두고,
  연결이 끊겨도 **정산 장부와 보낼 우편을 계속 볼 수 있게** 한다. 자수 탭만 사라진다
  (끝난 판이라 더 셀 것이 없다). 기본 탭은 정산 장부.
- **판의 신분증** — 판마다 출처(`파티`/`로컬`), 이름(파티면 `{방장닉}네 파티`),
  기간, 총액, **파티원 전부**를 함께 담는다. 파티원은 줄이지 않는다("외 4명" 금지) —
  날짜는 잘 기억 안 나도 누구랑 했는지는 기억하니, 그게 찾는 열쇠다.
  칩에서 방장은 금색, 나는 파랑으로 갈라 표시한다.
- **신분증은 탭 위에 띠로.** 카드(탭 내용) 안에 넣으면 판 전체에 걸리는 정보가 탭 하나에
  속한 것처럼 보이고 탭마다 반복된다. 끝난 판·기록에서는 마스트 왼쪽의
  [프리셋]·[처음부터]·[파티 모드 시작하기]가 어차피 쓸모없으므로 **그 자리를 띠가 쓴다**
  — 새 자리를 만들지 않고 세로도 안 늘어난다. 띠에 지금 상태(§8)와 나가는 문
  ([닫기] / [지금 판으로])까지 같은 줄에 둔다. 기존의 읽기 전용 배너를 이 띠가 흡수한다.
- **목록의 집은 로비**(§3.0, 2026-09-05 — 옛 "앱 이름 옆"·"히어로 아이콘" 자리는 폐기). 줄마다 배지·이름·날짜·파티원 전부·총액. 판 화면에서도 같은 목록을 여는 문 하나(마스트의 기록 아이콘).
- **기록에서 나가는 길 셋** (2026-09-05): 띠의 버튼(로비에서 열었거나 막 끝낸 판이면 [닫기] → 로비, 판에서 들춰봤으면 [지금 판으로]) · 헤더 브랜드(기록을 접고 로비) · **브라우저 뒤로가기**(기록은 주소 `#gen=이름` 을 가진 화면이다 — §5.1 화면 = 주소). 헤더 명칭은 `판 기록`.
- 파티원 쪽 기록은 받은 스냅샷에서 만든다 — 방장이 끝낼 때 보내는 '끝났어요' 마지막 한 장을
  그대로 굳혀 **파티원 로컬 판 기록에도 남긴다**(2026-09-05 확정, §3.4 여정표). 배지는
  `{방장닉}네 파티`(§3.5). 서버는 건드리지 않는다.

### 5.5 지우는 것

방 열쇠 체계(rooms/key/kill/재발급), handoff·recovery 전부(RECOVERY_ON 코드째),
endShare와 회차-주소 결합, ResetModal의 주소 닫힘 문구,
seatFromBundle의 서버 복구 경로(→ 로그인 후 `GET read`로 앉히는 단순한 경로로 대체),
지난 회차 잠금·복원 UI.

**파일 백업도 통째로 없앤다** — `KeyShare` 컴포넌트, 헤더의 [백업] 버튼, `exportPartyFile`·
`importPartyFile`, 관련 상태와 CSS까지. 계정이 그 일을 대신한다: 다른 기기에서 로그인하면
`GET read`로 장부가 돌아오고, 표 자체는 URL 공유가 링크 하나에 담고, 최근 20판은 지난 판에
남는다. 남는 구멍은 하나 — 계정과 브라우저를 동시에 잃으면 기록까지 담긴 사본이 없다.
비밀번호 재설정이 없으니 실제로 일어날 수 있는 일이지만, 그 대가로 지키는 것(개념 하나,
헤더 버튼 하나, 창 하나)이 더 크다고 보고 뺀다.

## 6. 검증 (배포 전, 전부 로컬)

1. `cd worker && npx.cmd wrangler dev --port 8787` (PowerShell은 npx.cmd).
2. **API 스크립트**(worker/test-api.mjs, node 22 전역 WebSocket): 가입(잘못된 닉/id 400,
   중복 409) → 로그인(오답 401, 30회 리밋 429) → me → my/room → invite → 로비 열기 →
   두 번째 계정 join(초대 없이 403, 만료 403, 정원 초과 409, 정상 ok) → 뷰어 WS hello/you →
   출발 시나리오(state PUT + bindings) → 출발 후 join=ok(정원 없음, 2026-09-05) → member approve/remove →
   confess(서기 없음 409 → 서기 WS 연결 후 전달 확인, 남의 rowId 403, 리밋 429) →
   presence 브로드캐스트 → obs resolve(활동 갱신) → obs-reissue 후 옛 토큰 404 →
   nick 변경 통지 → leave. **전부 PASS를 출력으로 증명.**
3. **페이지**: /r/(j 유무·만료)·/o/ 각각 오버레이/리다이렉트 분기, 대기실 오버레이 렌더.
4. **앱 E2E**: `node server.js`(5175). 방장 = http://localhost:5175, 파티원 = http://127.0.0.1:5175
   (localStorage가 갈라진다). 가입 2건 → 초대 링크 만들기(모집) → 초대 → 파티원 진입·즉시 착석 →
   준비 상태 실시간 → [시작] → 자수 왕복(기록 "자수" 확인, 취소 역분개) → scribe 끊고 비활성 확인 →
   내보내기 → 침묵 확인 → 재초대.
5. **회귀**: 로그인 없이도 카운터·메모장·정산·우편·프리셋·처음부터·지난 판(보기)·룰렛이
   v1과 동일하게 동작. 데모(#live=CAFE22) 정상.

## 7. v2에서 하지 않는 것

디스코드 OAuth·봇 / 비밀번호 재설정 / 영구 차단·조용히(mute) / 자수 큐(방장 오프라인 보관) /
파티원의 기타·합계·룰렛 조작 / 계정당 방 여러 개 / OBS 화면의 방 전환 버튼 /
구경용 공개 링크 / 지난 판 복원·잠금·서버 보관. — 전부 논의로 뺐다. 임의로 추가하지 않는다.

## 8. 문구 (그대로 쓴다)

- 가입 창: `비밀번호를 잊으면 되찾을 방법이 없어요. 다른 곳에서 쓰는 비밀번호는 쓰지 마세요.`
- 가입 창(만료 안내 — 이메일이 없어 예고를 못 하므로 미리 적는다): `1년 넘게 한 번도 안 쓰면 계정이 지워질 수 있어요.`
- 닉네임 칸 안내: `2~3글자 — 벌금판에 이 이름으로 올라요`
- 초대(디코용 복사, 마스크드 링크 한 줄):
  `[🔔 {방장닉}네 벌금 현황판 — 눌러서 참여](<{초대URL}>)` + 둘째 줄 `방송에 띄우려면 로그인해서 내 방송 주소를 OBS에 한 번만 넣어요.` (2026-09-06 오후 사용자 확정 — 파티원이 링크를 열기 전에 봄)
- (폐기 2026-09-05) 파티원 읽기 배너 `읽기 전용 화면이에요 — 참여하려면 로그인이 필요해요.` — 코드 붙은 초대 링크에는 초대장 화면(§8 끝의 초안)이 대신 선다. 코드 없는 시청자 링크(#live=)에는 남는다.
- (폐기) 대기 배너 — 승인제 전환으로 없어졌다. 신청 중은 "참여를 신청했어요…",
  수락됨은 "자리에 앉았어요…"를 쓴다.
- 무효 초대 코드: `이 초대는 쓸 수 없어요 — 방장에게 새 초대를 받아 주세요.`
- 만료된 초대 링크: `초대가 만료됐어요 — 방장에게 새 초대를 받아 주세요.`
  (둘 다 빈 벌금표 위가 아니라 안내 화면 단독으로)
- 판 중 메모장 전환 확인창(계정 붙은 자리가 있을 때): `메모장으로 바꾸면 파티원 화면도 메모장으로 바뀌어요.`
- (폐기 2026-09-04) 로비 시작 안내 `시작하면 이 사람들로 새 판이 열려요. 지금 판은 판 기록에 남아요.` /
  `혼자서도 시작할 수 있어요.` — 로비에 있다는 것은 판이 이미 닫혔다는 뜻이라 뒷문장이
  참이 되는 순간이 없었고, 앞문장은 [시작]이라는 버튼 이름이 이미 하는 말이었다.
  그 자리는 히어로(제목 + 판 이름)가 쓴다.
- 수락됐을 때: `자리에 앉았어요 — 방장이 시작하면 함께 시작돼요.`
- 판이 시작/재개됐을 때 카드: `판이 시작됐어요.` + [들어가기] — 자기 앱에 돌아가 있던 사람에게만(2026-09-05; 판에 서 있던 파티원은 화면 전환 없이 칸에 불이 들어온다)
- 중단 띠(파티원): `잠깐 멈췄어요 — 방장이 이어가면 다시 움직여요.`
- 자동 중단 카드(방장 로비): `한동안 조용해서 잠깐 멈춰 뒀어요.` + [이어가기]
- 정산 끝내기 확인창: `이 판을 마감할까요?` / `결과지가 판 기록에 남아요.` (킥 없음을 굳이 말하지 않는다 — "파티원은 그대로"도 "바로 다시 시작"도 뜻이 안 통했다)
- (폐기 — [처음부터] 폐지) 파티원 수 상기는 [정산 끝내기] 확인창의 `파티원은 그대로 있어요.`가 맡는다.
- 비움의 장부 로그 표기: `비움`
- (폐기 2026-09-05) 로비 히어로 제목 `파티 로비` — 헤더 명칭 `로비`/`벌금판`으로. / 판 이름 기본값: `{날짜} 벌금 파티` (예: `9월 3일 벌금 파티`)
- 초대가 아직 없을 때 그 자리의 버튼: `초대 링크 만들기` — 누르면 무엇이 나오는지 그대로 말한다(옛 `파티원 모으기`는 카드 제목과 겹치고 결과를 안 말했다)
- 빈 칸 자리표시: `(모험가1)`~`(모험가N)` — 괄호까지가 이름이다(2026-09-04 사용자 지정,
  "교체하라"가 읽히게). 실리안 여덟은 폐기 — 진짜 사람처럼 읽혔다. [시작] 때 이 이름
  그대로 판에 들어간다.
- (폐기 2026-09-05) 명단 칸 힌트 `오른쪽 파티원 모으기로 부르면 빈 칸에 앉아요. 이름을 직접 적어도 돼요.` — 모으기 열이 없어졌다. 준비 상태 표의 힌트는 §8 끝의 초안
- 명단 가득(방장이 수락 못 할 때): `명단이 가득 찼어요. 이름 적힌 칸을 비우거나 인원 수를 늘리면 앉힐 수 있어요.`
- (폐기 2026-09-05) 판 도중 합류자의 자리 고르기: `어느 줄이 나예요? 한 번 고르면 못 바꿔요 — 그 줄에 쌓인 벌금도 같이 이어받아요.` — 방장이 옮길 수 있게 되면서 겁만 주는 말. 새 문구는 §8 끝 초안
  / 늦었을 때: `그 줄은 방금 다른 사람이 가져갔어요 — 다른 줄을 골라 주세요.`
- 빈 칸이 없어 지목 초대를 못 보낼 때: `명단이 가득 찼어요. 이름 적힌 칸을 비우거나 인원 수를 늘리면 부를 수 있어요.`
- 초대를 수락했는데 그 사이 자리가 찼을 때 — 받은 사람: `자리가 다 차서 문 앞에서 기다려요 — 방장이 자리를 만들면 들어가요.`
  / 방장 토스트: `{닉}님이 초대를 받고 왔는데 자리가 없어요 — 인원 수를 늘리면 앉아요.`
  / 방장 신청 줄에 붙는 까닭: `초대받고 왔는데 자리가 없었어요`
- **§8 안의 `{id}`는 어디서나 가린 아이디로 그린다** — 앞 두 글자 + 점 넷(`he••••`, §3.1).
  점은 굵은 점(•)이다 (2026-09-05) — 가운뎃점(·)은 작아서 줄표로 읽혔다.
  문구는 그대로고 채워 넣는 값만 가린 것이다: 방장 화면도 파티원 화면도 통째로 방송에
  잡히므로, 아이디가 온전히 그려지는 자리가 앱 안에 하나도 없어야 한다.
- 지목 초대 카드: `{닉}({id})님이 파티에 초대했어요` + [거절] [수락하고 들어가기]
- 초대가 만료됐을 때: `초대 시간이 지났어요. 다시 초대해 달라고 해주세요.`
- 딴 파티에 있는데 초대를 수락할 때: `지금 파티에서 나가고 옮겨요.`
- (폐기 2026-09-05) 함께한 사람 시트: `내 판으로 초대` / `{닉}네 판에 참여 신청` — UI 폐기 (§3.3)
- 노크 뒤 상태: `참여를 신청했어요 — {닉}님이 수락하면 들어가요.` + [신청 취소]
- (폐기 2026-09-04) 파티 중 공유 끄기 확인창 `공유를 끄면 파티원이 판을 못 보고 자수도 멈춰요.`
  — 토글이 그런 일을 안 하게 됐으므로 확인창째로 없앴다 (§5.7)
- 송출 상태 — 주소 밑 문장 한 줄 = 헤더 버튼 툴팁 (§5.7, 2026-09-05 교체):
  `방송용 주소를 아직 안 받았어요. 받으면 이 자리에서 지금 뭐가 나가는지 알려줘요.`
  `서버와 연결이 끊겨서 갱신이 멈췄어요. 마지막으로 보낸 판이 그대로 떠 있어요.`
  (폐기 2026-09-05) `이 주소에는 내가 있는 판이 그대로 떠요. 지금은 판에 있지 않아요.` → §5.7 표의 idle·recruit 초안
  `이번 판이 이 주소에 나오고 있어요. 방송에 안 보이면 OBS 쪽 소스를 확인해 주세요.`
  - (폐기 2026-09-05, 당일) `이 주소에 지금 이 판이 …` — "이 판"은 사람이 안 쓰는
    지시어. 게이머의 말인 "이번 판"으로
  - (폐기 2026-09-05, 당일) `지금 이 판이 그대로 나가는 중이에요. …` — "그대로"가 군더더기,
    `지금 이 판이 방송에 나가고 있어요` — 앱은 OBS 가 실제로 받는지 모르므로 "방송에
    나간다"는 과장. 앱이 아는 사실(이 주소가 이 판을 내보낸다)만 말한다
  - (폐기 2026-09-05) 라벨 두 벌 `주소 없음`·`연결 끊김`·`판 없음`·`방송 중` — 지어낸
    상태어라 걷었다. 설명 중 `지금은 새 판이 안 나가요. [시작]하면 그 판이 방송에
    떠요. (…)`도 폐기 — [시작]은 방장 전용 동사라 파티원에게는 남의 말이었다.
- 오버레이 미리보기 버튼: `오버레이 미리보기` (↗ 새 탭 아이콘, 새 탭으로 연다)
  — (폐기 2026-09-05) `미리보기`: 눈(주소 보기)과 나란히 있으면 뭘 미리 보는지 안 갈렸다
- 주소 새로 발급 — 카드 발치 링크 `주소 새로 발급`, 확인 창: `내 방송용 주소를 새로
  발급할까요?` / `주소가 새어 나갔을 때 써요. 지금 주소는 바로 못 쓰게 되고, OBS 소스의
  주소를 새것으로 바꿔야 해요.` / [새로 발급]
  — (이동 2026-09-05) 창에 서 있던 설명 줄 `주소가 새어 나갔을 때 써요. 옛 주소는 바로
  못 쓰게 되고, …`는 확인 창 본문으로 들어갔다
- 계정 줄(공유 설정 창 맨 위, 2026-09-05): 게스트 뱃지 `게스트` / 게스트 밑줄
  `가입 없이 쓰는 중이에요 — 아이디를 정하면 다른 컴퓨터에서도 같은 주소를 쓸 수 있어요.`
  + `아이디를 만들면 뭐가 달라져요?` 링크(2026-09-06 오후; (폐기) `방송 주소, 한 번만 넣으면 돼요`, `로그인하면 어떤 게 좋나요?`) + [아이디 만들기](창 제목도 `아이디 만들기`) / 닉 편집 중 안내
  - (폐기 2026-09-05) [아이디 정하기]와 같은 이름의 창 제목 — "정하다"는 한국 서비스에서
    안 쓰는 말. 문장 속 "아이디를 정하면"은 사용자 확정 그대로 둔다
  `닉네임은 벌금판에 올라가는 이름이에요.`
  - (폐기 2026-09-05) `아이디 없음` — 게스트를 "없음"으로 부르지 않는다. 뱃지 `게스트`로.
  - (갱신 2026-09-05) `가입 없이 받은 주소예요. 아이디를 정하면 다른 컴퓨터에서도 같은
    주소를 쓰고, 파티원을 모을 수 있어요.` → 위의 밑줄 문장으로 (계정 줄이 주소보다
    먼저 서게 되어 "받은 주소예요"가 안 맞게 됐다)
- 자수 안내: `내 줄만 누를 수 있어요 — 칸 클릭 +1회 · 우클릭 −1회. 나머지는 읽기 전용이에요.`
- 방장 부재: `방장이 자리를 비웠어요 — 돌아오면 다시 누를 수 있어요.`
- (폐기 2026-09-05, 당일 — 로비 층 신설로 비로그인의 홈이 랜딩이 됐다) 파티원 모으기 자리(비로그인, 2026-09-05 재확정): **빈 상태 판의 문법** — 아이콘 ·
  헤드라인 `로그인하고 파티를 열어 보세요` · 세 줄(`초대 링크로 파티원을 모아요` /
  `파티원이 자기 벌금을 직접 세요` / `내 방송용 주소로 방송에 띄워요`) · 가운데 채운
  [로그인] · 밑에 `로그인하면 어떤 게 좋나요?` 링크. "파티원 모으기" 머리도 접는다 —
  아직 모을 수 없는 사람의 화면이다.
  (폐기 2026-09-05, 당일) 리드 + 구석의 작은 [로그인] — 구석의 작은 버튼과 글줄로는
  로그인을 못 부른다. 리드 `초대를 보내면 파티원이 이 명단에 앉아서 자기 벌금을 직접
  세요.`는 로그인 상태에서도 안 쓰게 됐다
- 헤더 화면 명칭 (2026-09-05, 같은 날 갱신): 브랜드 옆 `로비` / `벌금판` — (폐기, 당일)
  `파티 로비`. 판 이름은 표 머리에 **글자 + 연필**로 서고, 누르면 입력칸이 된다(표준
  문법; 상시 입력칸은 제목으로만 읽혔다). [시작]은 [정산 끝내기]와 같은 몸·같은 y
  (mastverbs 한 벌 공유) — 이 정합은 옛 로비 머리줄에서 판 화면으로 그대로 이사한다
- 수락 카드: `{닉}({id})님이 참여하려 해요` + [수락] [거절]
- (폐기 2026-09-05) 파티원 화면 맨 위 내 방송용 주소 카드 `내 방송용 주소 — OBS 브라우저 소스에 넣으면 이 판이 방송에 떠요` — 주소 표면은 창 하나(§5.3). 로비의 주소 줄 라벨은 §8 끝의 초안
- 대기실 오버레이 아래 한 줄: `모이는 중이에요…`
- 옛 주소 안내(방송 중 아닐 때만): `주소 체계가 바뀌었어요. 앱에서 새 주소를 받아 넣어주세요.`
- 대문(§3.11) 제목 `계정 만들기` / 이유 한 줄 `계정이 있으면 두 컴퓨터에서 같은 방송 주소를 쓰고, 파티를 열어 파티원이 자기 벌금을 직접 세게 할 수 있어요.`
  / 게스트 상자 `계정 없이도 쓸 수 있어요. 닉네임만 정하면 방송 주소가 나오고 파티에도 들어가요 — 나중에 가입해도 주소도 파티도 그대로예요.` + [게스트로 시작]
  / 맨 아래 `이미 계정이 있어요 · 로그인`
- 게스트 칸(§3.11) 제목 `게스트로 시작` / 물음 `벌금판에 올라갈 이름을 정해 주세요.`
  / 입력 밑 `2~3글자 · 이 브라우저에 저장돼요` / 버튼 `시작하기` / 게스트 기본 닉 `손님`
- 로그인 창 안내: `계정은 파티와 내 방송용 주소에 써요. 벌금을 세고 정산하는 데는 계정이 필요 없어요.`
  — 헤더에서 스스로 연 사람의 "로그인을 왜 해야 하는데?"에 대한 답이다. 대문의 권유
  (만들면 얻는 것)와 역할이 다르다: 용도 설명 + 안심. (옛 문구의 `파티 모드`는 §3.7에서
  지운 발명 개념이라 `파티`로 맞췄다)
- 주소를 방금 받았을 때(발급 직후 한 번만): `주소가 나왔어요.` + `이 주소를 복사해서 방송 프로그램(OBS·프리즘 등)의 브라우저 소스에 붙여넣으면 벌금판이 방송에 떠요.`
  + `송출컴이 따로 있나요? 여기서 복사해서 옮기세요 — 거기서 새로 받으면 다른 주소가 나와요.`
- 발급 카드 밑 안내 (2026-09-05 교체): `주소는 벌금판을 쓸 브라우저에서 받으세요 — 게스트 주소는 받은 브라우저에 묶여 있어요. 송출컴에는 주소만 복사해 넣으면 되고, 계정을 만들면 어느 컴퓨터에서든 같은 주소를 써요.`
  - (폐기 2026-09-05) `이미 다른 컴퓨터에서 받았다면 여기서 또 받지 마세요 — …` — 금지형이라 "주소를 두 개 받으면 안 되나?"로 읽혔고, 가입자에게는 해당도 없었다
  - (폐기 2026-09-05) 발급 버튼 밑 `이 브라우저에 저장돼요.` — 발급 문이 랜딩(가입 포함)으로 가면서 게스트 전용 안내는 오안내가 됐다. 브라우저 저장 이야기는 랜딩의 게스트 카드가 한다
- (폐기 2026-09-05) 그 아래 계정 권유 `두 컴퓨터에서 같은 주소를 쓰려면 계정을 만들어 양쪽에서 로그인하세요. 파티원이 자기 벌금을 직접 세는 것도 계정이 있어야 해요.` — 대문(§3.11)의 이유 한 줄이 같은 말을 하게 되어 걷었다. 패널에는 [내 방송용 주소 받기]와 경고, `이미 계정이 있어요 · 로그인` 한 줄만 남는다
- 가입 없이 주소 받기(오버레이 공유 설정): `지금 이 브라우저의 벌금판만 방송에 띄우려면 가입 없이도 돼요.`
  / (폐기 2026-09-05) 옆줄 `나중에 계정을 만들면 이 주소를 그대로 옮겨요` — 대문의 게스트 상자가 같은 말을 한다
- (폐기 2026-09-05) 익명 계정으로 파티 모드를 열려 할 때 `파티원을 모으려면 아이디와 비밀번호를 정해야 해요. 지금 쓰는 방송용 주소는 그대로예요.` — 게스트도 파티를 연다(§3.11)
  - (초안 2026-09-05) 자수 되돌리기 거절 `자수는 30초 안에만 되돌릴 수 있어요 — 그 뒤는 방장에게 말해 주세요.` / 복귀 안내 `{n}초 뒤 자수 화면으로 돌아가요 — 누르면 다시 30초.` / 로비 오른쪽 카드 제목 `파티 참여`
  - (초안 2026-09-05) 게스트 로그아웃 확인창 `게스트 계정은 로그아웃하면 다시 못 들어와요` / `아이디를 정하면 어디서든 로그인할 수 있어요. 지금 로그아웃하면 이 계정과 파티 자리를 버려요.` / [아이디 만들기] [그래도 로그아웃]
  - (초안 2026-09-05) 모집 카드의 게스트 한 줄 `게스트로 연 파티는 이 브라우저에 묶여 있어요 — 아이디를 정하면 어디서든 이어요.` / 아이디 만들기 창 이유 한 줄 `아이디를 정하면 다른 컴퓨터에서도 로그인해 같은 주소와 파티를 그대로 써요. 지금 주소는 바뀌지 않아요.`
- 가이드 맨 아래 줄: `파티원이 할 일은 초대 링크를 누르는 것뿐이에요. OBS를 안 써도 벌금은 세어지고 자수도 돼요.`
- 주소 발급 화면: `OBS·XSplit·프리즘 등 어떤 방송 프로그램이든, 브라우저 소스에 이 주소를 넣으면 돼요.`
- `/o/` 를 브라우저로 열었을 때 아래 한 줄(**마우스가 움직인 뒤에만** 만든다 —
  송출 프로그램의 소스 화면에는 마우스가 없어서, UA 로 못 거르는 마이너 송출
  프로그램에도 안 샌다; 2026-09-05): `이 주소는 방송 프로그램에 넣는 주소예요.`
  + 그 줄 안의 누를 수 있는 문 `현황판으로 보기` (누르면 `?mode=page` — 읽기 화면을 없애면
  가입 없이 쓰는 사람이 정산 장부·보낼 우편을 못 보게 된다)
- 자수 탭 룰렛 카드: `룰렛은 방장이 돌려요`
- (폐기 2026-09-05) 외형 설정의 [테마 미리보기] 버튼 — 카드 머리의 [오버레이 미리보기]와
  미리보기 문이 둘이 되어, 뭐가 다른지부터 묻게 했다
- 안내 창 문패 (2026-09-06 사용자 확정): `방송 주소, 한 번만 넣으면 돼요` — 창 안의 링크 문구도 같은 말. **2026-09-06 오후 창을 둘로** (사용자: 주소 하나의 장점은 게스트도 누리니 게스트·아이디 차이는 따로): 이 창은 쓰는 방식 비교와 "한 번만 하는 일과 팟마다 하는 일"만 남기고, 문은 OBS 공유 설정 창의 주소 상자 아래 새 줄 `파티원도 각자 로그인해서 자기 주소를 받아 넣어요. 방장 주소 하나를 다 같이 넣던 방식과 뭐가 다른지는 여기에.` [방송 주소, 한 번만 넣으면 돼요] — 로그인·주소 유무와 상관없이 늘 (구 방식 방장이 주소를 받은 직후 보는 자리). 게스트·아이디 차이는 새 창 `아이디를 만들면 뭐가 달라져요?`(제목 초안; 머리말 `게스트든 아이디든 내 방송용 주소는 하나씩 나오고, 자수·참여·정산도 똑같아요.` — (폐기, 같은 날) 뒷문장 `다른 건 어디서 이어 쓸 수 있느냐예요.` 사용자: 빼자; 옛 창 끝 절을 옮김)로, 문은 게스트 계정 줄의 링크와 랜딩(게스트/가입 고르기)의 링크. (폐기 2026-09-06) `주소와 계정, 어떻게 돌아가요?` — 사용자: 더 나은 문구를. 첫 부분의 그림은 글자 상자·화살표 대신 선으로 그린 세 장면(방장 주소 하나를 모두의 OBS에 → 방장이 바뀌면 전원이 주소를 갈아요 / 파티가 바뀌어도 선은 그대로), 이름은 예시 실리안·니나브·웨이(사용자 지정). 뒤 두 부분은 그대로
- 안내 창 "주소와 계정, 어떻게 돌아가요?" (2026-09-05 개편): 본업은 **쓰는 방식 두 가지의
  비교**다 — `주소 하나 나눠쓰기(기존 방식)`(방장 주소를 전원이 넣음 — 지금도 되는 방식; 방장이
  바뀌면 전원이 소스 주소를 갈아야 하고, 그 주소엔 그 방장의 판만 뜬다) vs `사람마다
  자기 주소`(한 번 넣으면 끝 — 누가 방장이든 내가 들어간 파티가 내 주소에 뜬다).
  예전 축("가입 없이 vs 계정")은 주소 발급 = 로그인이 되면서 죽었지만, 나눠쓰기라는
  **방식**은 살아 있으므로 비교에서 지우지 않는다. 이어서 —
  머리 `게스트든 아이디든, 로그인하면 내 방송용 주소가 나와요 — 주소는 사람마다
  하나씩이에요. 내 주소에는 내가 있는 판이 떠서, OBS에 한 번만 넣으면 파티가 바뀌어도
  그대로예요. 벌금을 세고 정산하는 데는 계정이 필요 없어요.`
  여는 링크(게스트 계정 줄·비로그인 파티 자리)도 같은 문패다.
  (폐기 2026-09-05, 당일) 문패 `로그인하면 어떤 게 좋나요?` — 내용의 질문이 바뀌었는데
  문패만 옛 질문이었다
  → "한 번만/팟마다" 두 장(유지) → 비교는 **게스트 vs 아이디를 만들면**.
  (폐기) "가입 없이(주소 하나를 만들어 나눠 씁니다)" 열 — 게스트도 자기 계정·자기
  주소를 받게 되면서 존재하지 않는 방식이 됐다
- 룰렛 항목 칩 (2026-09-05): 로비 `◎ 룰렛 · {n}면 ⚙` / 판 머리 `◎ 룰렛 · {n}면 · 나온
  숫자 × {단가} ⚙` — 요약(면 수)과 채운 톱니, 실선 테두리. 점선은 자리표시로 읽혀서
  실선으로 세웠다. (폐기) 라벨만 있던 `◎ 룰렛` 점선 칩, ▾ 화살표만 달던 안 —
  커스텀 문이 안 보였다
- 벌금표 카드 맨 위(파티원): `읽기 전용이에요 — 내 벌금은 자수 탭에서 세요. 30초 동안 안 누르면 자수 화면으로 돌아가요.`
- 장부·우편 카드 맨 위(파티원): `30초 동안 안 누르면 자수 화면으로 돌아가요.`
- 오버레이 공유 설정, 로그아웃 상태의 주 버튼: `내 방송용 주소 받기` / 그 아래 한 줄로
  `이 브라우저에 저장돼요. · 이미 계정이 있어요 · 로그인` (2026-09-05 두 줄을 합침 —
  같은 카드 발치에 각자 서 있을 만큼의 말이 아니다)
  / (폐기 2026-09-04) 계정 권유 `계정을 만들면 다른 컴퓨터에서도 같은 주소를 쓰고, 파티원이 자기 벌금을 직접 셀 수 있어요.`
- 오버레이 침묵 예외(방송 중 아닐 때만): `/r/`: `이 주소만으로는 판을 볼 수 없어요. 자수 화면의 '내 방송용 주소'를 넣어주세요.`
  / `/o/`: `지금 들어가 있는 파티가 없어요. 초대를 받아 참여하면 다시 보여요.`
- 정원 참: `대기실이 가득 찼어요. 방장에게 정원을 늘려 달라고 해주세요.`
- 판 기록 창: `끝난 판이 여기 남아요. 보기만 할 수 있고, 최근 20판까지 남습니다.`
- 끝난 파티의 띠: `끝났어요.` + `정산 결과는 계속 볼 수 있고, 판 기록에도 남았어요.`
- 기록을 열었을 때의 띠: `판 기록을 보는 중이에요 — 여기서는 못 고쳐요.`
- **2026-09-05 로비·여정 개편의 새 문구 — 초안.** 구현 때 자리표시로 넣고 스크린샷 검증
  때 함께 확정한다(2026-09-05 결정 — 문구는 화면에 얹혀야 판단된다). 확정되면 이 줄들을
  확정본으로 바꾼다.
  - 로비 내 판 카드: 백지 버튼 `+ 새 판 만들기` / 있을 때 버튼 `벌금판 열기` (당일 교체 — `들어가기`는 파티 참여의 동사와 겹쳤다) / 자리 수 칩 `자리 3/8`
  - 비로그인 로비 권유 줄: `로그인하면 파티에 들어가고 내 방송용 주소가 나와요 — 게스트로도 돼요.` + [시작하기]
  - 로비 주소 줄 라벨: `내 방송용 주소`
  - 로비 파티 참여 카드 제목 `파티 참여` / 입력칸 자리표시 `초대 코드나 초대 주소를 붙여넣어요` /
    버튼 `참여하기` / 틀린 코드는 기존 `이 초대는 쓸 수 없어요 — …` 재사용
  - 복귀 줄: `{방장닉}네 파티에 앉아 있어요` + [돌아가기]
  - (폐기 2026-09-05, 당일) 빈 파티 카드 `초대를 받으면 여기서 들어가요. 함께한 파티원이 생기면 그 사람들도 여기 떠요.` → `초대 링크나 코드를 받으면 여기 붙여넣어 들어가요.`
  - **2026-09-05 여정 표지판·자리 규칙의 새 문구 — 초안** (화면 검증 뒤 확정):
    - 로비 내 판 카드 진행 중 얼굴: 제목 `진행 중 · {판 이름}` / 한 줄 `{n}명 · 벌금 {총액} · {m}분째`(1분 미만 `방금 시작`) / 부제 `판을 두고 나온 상태예요 — 파티원은 그대로 셀 수 있어요.`(혼자 판 `판을 두고 나온 상태예요 — 숫자는 그대로 남아 있어요.`) / 버튼 `벌금판으로 돌아가기`
    - 로비 내 판 카드 시작 전 얼굴: 제목 `시작 전 · {판 이름}` / 부제 `열어서 [시작]을 누르면 세기 시작해요.` + `지난 판 '{이름}'은 끝났어요 — 결과는 판 기록에.` / 버튼 `벌금판 열기`
    - 헤더 칩 `● 진행 중 · 돌아가기` / 마스트 빵부스러기 `‹ 로비` / 상태 칩 `시작 전` · `진행 중` / 파티원 `파티 나가기`
    - 자리 고르기: `어느 줄이 나예요? 그 줄에 쌓인 벌금도 같이 이어받아요 — 잘못 골랐으면 방장이 옮겨 줘요.`(준비 상태는 벌금 구절 없이) / 선택지 `새 자리` + `{n}칸 남음` / 기다리는 동안 `자리를 잡는 중이에요…` + `방장 앱이 내 줄을 찾고 있어요.`
    - 방장 토스트: `{닉}님이 '{줄 이름}' 줄에 앉았어요.` / `{닉}님이 {n}번 줄에 앉았어요.` / `{닉}님이 들어왔어요 — 어느 줄인지 본인이 고르는 중이에요.`
    - 줄의 사람 시트: 계정 줄 `{닉} {가린 아이디} · 연결됨|연결 끊김` + `다른 줄로 옮기기…` · `파티에서 내보내기`(확인 `{닉}님을 내보낼까요?` / `줄과 벌금은 그대로 남고, 다시 초대하면 돌아와요.` / [내보내기]) / 이름 줄 `이 줄에 앉힐 사람을 골라요 — 옮겨 오는 사람의 자수 자격이 이 줄로 따라와요.` + 후보 `지금 '{줄}' 줄`·`자리 없음`·`신청 중` / 없으면 `아직 앉힐 사람이 없어요 — 초대 링크로 부르면 여기 떠요.`
    - 판 중 [초대 링크] 창: 머리 한 줄 `판 도중에도 들어올 수 있어요 — 들어온 사람은 이름 적힌 빈 줄 중에서 자기 줄을 고르거나 새 자리에 앉아요.` / 신청 카드 버튼 `신청 보기`(옛 `파티 서랍 열기`)
    - 미리보기 리본: `예시 화면이에요 — 지금은 방송에 나가는 게 없어요. [시작]을 누르면 현황판이, 파티원을 모으면 대기실이 여기 떠요.`
  - 초대장 화면: `{방장닉}님네 파티에 초대받았어요` / 밑줄: 모집 중 `{n}/{정원} 모임`, 진행 중
    `진행 중 · {n}명` (2026-09-05 — 정원을 모르는 판에서 `1/1 모임`이 떴다) / 버튼 `참여하기`
  - 룰렛 외형의 감속: 라벨 `감속` / `보통` / `느긋하게` / 설명 `느긋하게는 끝에서 오래 미적여요.` (초안 2026-09-05)
  - 초대 조각(⑥): `초대 코드` / [코드 복사] [링크 복사] / `디코 메시지 복사` / `새로 발급` / `m분 남음` — (폐기) `디코용 복사`
  - 판 중 파티 줄(⑤): `파티원 n` · `모집 중 · m분 남음` · [신청 k] [초대 링크] (초안 2026-09-05)
  - 창 안 미리보기 캡션(①): `지금 이 주소에 나가는 그림이에요 — 판이 없으면 예시가 떠요.` (초안 2026-09-05)
  - 표준화(초안 2026-09-05): 초대장 로그인 상태 `{닉} 계정으로 들어가요.` / 인원 `{n}명 앉음 · 빈 자리 {k}` / 들어오려는 사람 `자리가 없어요` `내보냈던 사람이에요` `들어왔는데 빈 줄이 없어요` [자리 만들어 앉히기] [받기] [내보내기] / 본인 `자리가 다 찼어요 — 방장이 자리를 만들면 들어가요.` `방장이 받아 주면 들어가요.` / 시작 전 옮기기 `이 줄로 옮기기` / 고르기 `어느 줄이 나예요? 앉은 뒤에도 빈 줄로 옮길 수 있어요.` / 진행 중 대기 `자리를 기다리는 중이에요` `방장이 줄을 정해 줘요.` / 나감 태그 `나감` / 방장 토스트 `{닉}님이 들어왔는데 빈 줄이 없어요 — 줄의 사람 버튼으로 앉혀 주세요.` `{닉}님이 다시 들어오려 해요 — 받을지 정해 주세요.` / 카드 `들어오려는 사람이 있어요` [보기] / 빈 판 끝내기 `아직 기록이 없어요.` `이 판을 접고 로비로 갈까요? 이름과 항목은 그대로 남아요.` [접기] / 비로그인 판 위 `파티원을 부르거나 방송에 띄우려면 방송용 주소가 필요해요.` [주소 받기] / 로비 비로그인 `파티원을 부르거나 방송에 띄우려면 로그인이 필요해요 — 게스트로도 돼요.` [로그인]
  - 대기실 신호(초안 2026-09-05): 자리 띠 `앉음` / `방금 앉았어요` / `연결 끊김` / `직접 적은 이름 — 이 사람이 초대로 들어오면 여기 앉아요` / `직접 적은 이름` / `방장이 적은 이름` / `빈 자리 — 초대 코드로 차요` / `빈 자리` · 모집 머리 `모집 중 · m분 남음` `n/8 앉음` · 상태 칩 `시작 전 · 모집 중` · 요약 `앉음 n · 직접 적음 n · 빈 자리 n`
  - 자수 정정: `자수 정정` / `{닉} 자수 정정` / 오버레이 `정정 · {항목} −{금액}` / 토스트 `자수를 정정했어요 — 방금 것을 되돌렸어요.` (초안 2026-09-05)
  - 준비 상태 표 힌트(방장): `초대 링크를 보내면 파티원이 빈 칸에 앉아요. 이름을 직접
    적어도 돼요.` / 잠긴 칸을 눌렀을 때(방장) `[시작]을 누르면 세기 시작해요.` /
    (파티원) `방장이 시작하면 세어져요.`
  - 파티원 [나가기] 확인창: `파티에서 나갈까요?` / `다시 들어오려면 초대나 신청이
    필요해요.` / [나가기] — 나간 뒤 로비 토스트 `파티에서 나왔어요.`
  - 파티가 끝났을 때 파티원이 로비에 닿으면 토스트: `{방장닉}네 파티가 끝났어요 — 결과는
    판 기록에 남았어요.`
  - 로비 누적 한 줄(미확정): `여태 {n}판 · 벌금 {총액} · 최다 벌금왕 {닉}`

### 8.x 파티 허브 · 방 하나 규칙 · 되돌리기 칩 (2026-09-07) — 전부 초안
- 헤더 칩: `파티 없음` / `시작 전 · 내 판` / `● 모집 중 · 내 판 n/정원` / `● 진행 중 · 내 판` / `● {방장}네 파티 · 진행 중`·`시작 전`. 허브 팝오버 aria `파티 허브`. 로비 2열 카드 제목 `계정` · `기록` · `파티`.
- 허브: `받은 초대가 기다려요.` [지우기] [참여하기] · 남의 파티 상자 `파티가 진행 중이에요.`/`방장이 시작하길 기다려요.` `돌아가기 ›` [나가기] · 파티원 허브 `{n}명 · 내 벌금 {금액}` [파티 나가기] · 기록 없음 `아직 기록이 없어요. 판을 끝내면 결과지가 여기 남아요.` · 게스트 안내 `게스트 계정은 이 브라우저에 묶여 있어요 — 아이디를 정하면 어디서든 이어요.` · 상자 사실 줄 `대기실 n/정원`.
- 방 하나 규칙 확인창: `진행 중인 내 판을 끝내야 갈 수 있어요 / [정산 끝내기]를 누르면 결과지가 기록에 남고 판이 닫혀요. 초대는 파티 허브에 보관해 둘게요.` [끝내러 가기] · `내 판을 해산하고 초대받은 파티로 갈까요? / 시작 전 판이 없어져요. 자리와 이름이 지워지고 항목과 단가만 남아요.` [해산하고 가기] · `{방장}네 파티에서 나가고 내 판을 만들까요? / 그 파티의 내 자리가 비어요. 벌금은 줄에 남아요.` [나가고 만들기].
- 되돌리기 칩: `↶ +n 되돌리기 · s초`.
- (폐기 2026-09-07) 로비 진행 중 얼굴 `판을 두고 나온 상태예요 — 파티원은 그대로 셀 수 있어요.` / `숫자는 그대로 남아 있어요.` — 로비를 볼 뿐 나간 게 아니다(사용자). 헤더 칩 `● 진행 중 · 돌아가기` · `{방장}네 파티 · 돌아가기` · `모집 중 · 대기실로` · `시작 전 · 대기실로` — 칩 하나로. 헤더 [초대 코드]. 로비 카드 `파티 참여`·`판 없음`(허브에선 `파티 없음`). 모집 카드 `n / 정원 앉음`·라벨 `초대 코드`. 빵부스러기 [파티 나가기].

### 8.x 물건 셋·상태 셋 (2026-09-06) — 새 문구는 초안, 폐기는 원문 보존

- 로비 백지 얼굴: `내 판이 없어요.` + [+ 새 판 만들기] (초안) — (폐기 2026-09-06) `아직 내 판이 없어요.` — 끝낸 뒤엔 "아직"이 안 맞는다
- 로비 시작 전 얼굴: `● 모집 중` / `시작 전` + 상자(사실 줄 `대기실 n / m 앉음` · 앉은 사람 이름 `·` 나열 / `아직 아무도 없어요` · 발치 `대기실로 ›`) + 상자 안 우상단 `× 해산` (2026-09-07; (폐기) `● 모집 중 · n / m 앉음` 제목 칩, [해산] [대기실로] 버튼 줄)
- 로비 파티 참여 카드: 제목 `파티 참여` + 입력칸(테두리·바탕이 있는 칸, 안내문 `초대 코드나 초대 주소를 붙여넣어요`) + [참여하기] (2026-09-07 사용자: 같은 말이 세 번, 텍스트 박스가 안 보임 — (폐기) 라벨 `초대 코드나 초대 주소` · 안내 줄 `초대 링크나 코드를 받으면 여기 붙여넣어 들어가요.`)
  — (폐기 2026-09-06) `열어서 [시작]을 누르면 세기 시작해요.` / `지난 판 '{이름}'은 끝났어요 — 결과는 판 기록에.` / [벌금판 열기]
- 헤더 칩: `● 모집 중 · 대기실로` / `시작 전 · 대기실로`(비로그인) (초안). `● 진행 중 · 돌아가기`는 그대로
- 대기실 마스트: [해산] [시작]. 상태 칩 `시작 전` — (폐기 2026-09-06) `시작 전 · 모집 중` — 시작 전 판은 언제나 모집 중이라 겹말
- 해산 확인창: `파티를 해산할까요?` / `앉아 있는 파티원이 나가요.` + [해산] (초안)
- 기록 없는 끝내기: `기록이 없어요 — 해산할까요?` / `판이 없어지고 로비로 가요. 항목·단가·인원은 그대로예요.` + [해산] (초안)
  — (폐기 2026-09-06) `아직 기록이 없어요.` / `이 판을 접고 로비로 갈까요? 이름과 항목은 그대로 남아요.` [접기] — 접기라는 세 번째 동사
- 모집 카드 머리: `● 모집 중` + `n / m 앉음` — (폐기 2026-09-06) `모집 중 · m분 남음` · `파티원 모으기` · `초대 링크 만들기`(→ 발급 실패 때만 `초대 코드 발급`)
- 시작 전 이름 칸을 누르면: `이름은 [시작] 뒤에 적어요.` (초안)
- 합류 토스트(8초): `{닉}님이 들어왔어요 — 표 아래에서 앉혀 주세요.` / `{닉}님이 다시 들어오려 해요 — 표 아래에서 받아 주세요.` / `{닉}님이 들어오려는데 자리가 없어요 — 표 아래에서 받아 주세요.` (초안)
  — (폐기 2026-09-06) `{닉}님이 들어왔는데 빈 줄이 없어요 — 줄의 사람 버튼으로 앉혀 주세요.` / `{닉}님이 다시 들어오려 해요 — 받을지 정해 주세요.`
- 표 아래 줄 (개정 2026-09-06 당일, 같은 날 다시): `{닉} {아이디 앞 두 글자}•••• · 자리가 다 찼어요`(시작 전 정원 참) / `들어왔어요`(진행 중에 처음 온 사람 — 되살림) + [거절] [받기](시작 전) · [거절] [자리 정하기](진행 중, 2026-09-06 재정정 — 시트 문구는 §3.2)
  — (폐기 2026-09-06 당일) `내보냈던 사람이에요`(→ 자기 줄 밑) / `초대받고 왔는데 자리가 없었어요` / `자리가 없어요` / [자리 정하기]
- 기다리는 파티원의 카드: `자리를 기다리는 중이에요` / `방장이 받아 주면 앉아요.` (초안) — (폐기 2026-09-06) `방장이 줄을 정해 줘요.` — 방장은 줄을 고르지 않는다
- 자기 줄 밑의 요청 줄: `↳ {닉}이(가) 돌아오려 해요` / `↳ 내보냈던 {닉}이(가) 돌아오려 해요` + [거절] [받기] (초안)
- (폐기 2026-09-06 당일) 자리 정하기 시트: 제목 `{닉} · 앉힐 줄`, `새 줄 만들기`, 빈 줄엔 `빈 줄` 꼬리 — 앱이 앉힌다(§3.8 앉히기 규칙 하나)
- 사람 시트: 제목 `{닉}` · `{닉} {아이디 앞 두 글자}•••• · 연결됨` · [다른 줄로 옮기기…] [파티에서 내보내기] [닫기] (초안). 옮기기 창은 기존 `자리 바꾸기` / `{닉} 님을 어느 자리에 앉힐까요? 자리를 옮기면 자수 자격도 따라가요.` / `{이름} 자리에`
- 진행 중 합류 토스트(8초): `{닉}님이 새 줄에 앉았어요.` / `{닉}님이 n번 줄에 앉았어요.` / `{닉}님이 '{이름}' 줄에 앉았어요.` (초안)
- 이어가기: 대기실 버튼 `지난 판 이어서…` · 피커 제목 `지난 판 이어서` · `[시작]을 누르면 고른 판의 줄·숫자·기록이 그대로 열려요. 앉은 사람은 자기 줄로 돌아가요.` · 항목 `{판 이름} {n}명 · {합계}` · [닫기] (초안)
- 시작 전 칸 안내: `[시작]을 누르면 세기 시작해요.` — (폐기 2026-09-06) `이름을 적고 [시작]을 누르면 세기 시작해요.` — 시작 전 이름 칸은 글자라
- 비로그인 대기실 모집 카드 자리: `파티원을 부르거나 방송에 띄우려면 방송용 주소가 필요해요.` + [주소 받기] (기존, 검증 확인)
- 판이 없어 초대가 안 열릴 때(코드는 맞음): `이 파티에 들어갈 초대가 없어요 — 방장에게 초대 링크를 받아 주세요.` + [로비로] (기존 문구가 그대로 쓰임 — 재발급된 코드로 온 경우와 같은 말)
- 진행 중에 나간 줄의 태그: `퇴장` — (폐기 2026-09-06) `나감` — 사용자: 더 좋은 말을 찾자
- 파티원 끝남 띠: `판이 끝났어요` + [로비로]; 새 판이 열리면 `● 새 판이 열렸어요` + [로비로] [들어가기] (초안)
  — (폐기 2026-09-06) `끝났어요. 정산 결과는 계속 볼 수 있고, 판 기록에도 남았어요.` + [닫기] — 결과지가 이미 말한다
- 판이 없을 때 링크: `지금은 열린 판이 없어요.` + [로비로] (초안)
- 시작 전에 해산당한 쪽지(로비 파티 카드): `{방장}네 파티가 해산됐어요.` + [×] (초안)
- 진행 중 나가기 확인창: `파티에서 나갈까요?` / `벌금 기록은 남아요. 다시 들어오려면 방장이 앉혀 줘야 해요.` + [나가기] (초안). 시작 전은 묻지 않는다
  — (폐기 2026-09-06) `다시 들어오려면 초대나 신청이 필요해요.`
- 로비 복귀 줄: `● {방장}네 파티 · 시작 전/진행 중` + [나가기] [돌아가기] (초안) — (폐기 2026-09-06) `{파티}에 앉아 있어요` + [돌아가기]
- (폐기 2026-09-06) 대기실 슬립 `자리에 앉았어요 — 방장이 시작하면 함께 시작돼요.` — 칩 `시작 전`과 자리 띠가 말한다. `대기실 n/m 모임`도 폐기(`● 실시간`만)
- 방장 부재: `방장이 자리를 비웠어요.` — (폐기 2026-09-06) 뒷절 `— 돌아오면 다시 누를 수 있어요.`
- (폐기 2026-09-06) 중단 띠 `잠깐 멈췄어요 — 방장이 이어가면 다시 움직여요.` · 자동 중단 카드 — 중단이라는 상태가 모델에 없다
- 헤더 버튼: `OBS 공유 설정` (2026-09-06 사용자 지정) — (폐기 2026-09-06) `오버레이 공유 설정`. 창 제목도 같다. `오버레이 외형` 섹션 이름은 그대로
- 파티원 코치마크: `방송하시면 이 판을 OBS에도 띄울 수 있어요. 여기서요.` + [알겠어요] (초안)
- 방장 코치마크 (2026-09-06 사용자 지적 — 방장은 당연히 안다고 여겨 빠뜨렸던 자리): 새 판을 만들어 대기실에 온 1.5초 뒤 헤더 [OBS 공유 설정]에 `방송에 띄우는 주소는 여기 있어요. OBS에 한 번만 넣으면 돼요.` + [알겠어요] (초안). 파티원 것과 같은 규칙(브라우저당 1회, 주소 복사한 적 있으면 생략). 비로그인 방장은 생략 — [주소 받기] 카드가 말한다
- (폐기 2026-09-06 당일) 공유 창 미리보기 캡션 `지금 이 주소에 나가는 그림이에요.` — 사용자: 미리보기가 스크롤을 너무 잡아먹고 주소가 잘 안 보인다. 나가는 게 뭔지는 상태 문장이 말한다. 외형 섹션의 룰렛 면 미리보기는 다른 것이라 남긴다
- 사람 아이콘 호버·시트의 이름은 계정 닉(2026-09-06 사용자 지적) — 줄 이름(방장이 고친 이름)은 안 쓴다. 자리가 앉을 때의 닉을 기억해 명단이 오기 전과 방장 자기 줄에서도 계정 닉이 뜬다
- 초대장은 한 화면(스크롤 없음)이고, [참여하기]와 앉는 순간 맨 위로 간다 (2026-09-06 사용자 지적)
- 자수 카드 B (2026-09-06 사용자 확정): 항목명 옆에 `1회 {단가}G`(13.5px, 본문색) · 회수가 큰 숫자(34px) `n회` · 밑에 금액(17px 금색) — 금액은 칸에 굳힌 금액(sums)이라 단가를 '이제부터만' 바꾼 뒤에도 방장 표와 같다(3만×3 뒤 5만×2 = 19만; 전엔 횟수 × 지금 단가로 25만이 보였다 — 버그). 룰렛 카드는 `나온 숫자 × {단가}G` + 굳힌 금액 + `룰렛은 방장이 돌려요`. 룩은 벌금표 칸의 결(가는 테두리, 둥근 금테 없음) — (폐기 2026-09-05 안) 금액 46px 주인공 · 회수·단가 11.5px 한 줄
- 자수 안내 둘째 줄 (2026-09-06, 같은 날 고침): `누른 건 방장 벌금판에 바로 올라가요. 되돌리기는 30초 안에만 돼요.` (초안) — 30초는 서버의 되돌리기 창(§3.6)과 같은 숫자. (폐기 2026-09-06 당일, 사용자 지정 원문) `여기서 누르면 방장의 벌금판에 반영돼요. 30초 이내에 우클릭하면 취소할 수 있어요.` — 사용자: 첫 줄의 우클릭과 겹친다
- 자수 카드의 단가 표기 (2026-09-06 같은 날 고침): `1회` 작은 회색 · 금액은 모노 15px 금색 · `G` 작은 회색. 밑줄은 없다(사용자: 밑줄이 있으니 고칠 수 있는 것처럼 보인다).
- 화면별 사용법 문구 (2026-09-06, 전부 초안) — 로비: `판은 여기서 만들어요. 파티는 판 안에 있어요.` / `초대 코드나 링크를 받았으면 여기 붙여요.` / `끝난 판은 여기 남아요. 결과지를 다시 볼 수 있어요.` · 대기실: `링크를 복사해서 디코에 붙여요. 파티원은 누르면 바로 앉아요.` / `인원과 항목은 시작 전에 정해요. 이름은 시작 뒤에 적어요.` / `다 모이면 시작. 안 온 사람 자리는 비워 두고 시작해도 돼요.` · 벌금판: `칸을 🖱 누르면 1회가 쌓여요.` / `🖱 우클릭하면 1회가 빠져요.` / `단가는 머리의 숫자예요. 누르면 고칠 수 있고, 이미 센 것까지 바꿀지 그때 물어요.` / `이름 옆 사람 아이콘. 줄을 옮기거나 파티에서 내보내요.` / `정산 장부 탭엔 누른 게 정산돼 있고, 보낼 우편엔 누가 누구에게 얼마 보낼지 있어요.` / `다 끝나면 정산 끝내기. 결과지가 기록에 남아요.` · 자수: `내 줄이에요. 🖱 누르면 방장 표에 바로 올라가요. 잘못 눌렀으면 30초 안에 🖱 우클릭으로 되돌려요.` / `방장 표는 여기서 봐요.` · [?] 메뉴: `사용법` · `지금 이 화면` · `n걸음` · `봤어요` · `그 화면에서` · [보기] [다시] · 발치 `OBS에 넣는 방법과 방송 주소 안내는 [OBS 공유 설정] 창에 있어요.`
- 자수 안내 첫 줄 (2026-09-06 사용자): `칸을 🖱 누르면 1회 쌓이고, 🖱 우클릭하면 되돌려요.` — 둘째 줄의 '되돌리기'와 같은 말. (폐기 당일) `…우클릭하면 1회 빠져요.` 벌금표 도구줄의 문장은 그대로. (폐기 당일) 13.5px 평문 `1회 3만G` — 사용자: 구분이 안 되고 작다
- 자수 안내 (2026-09-06 사용자 지적): 벌금표와 같은 문장·같은 마우스 아이콘 `칸을 🖱 누르면 1회 쌓이고, 🖱 우클릭하면 1회 빠져요.` — (폐기 2026-09-06) `내 줄만 누를 수 있어요 — 칸 클릭 +1회 · 우클릭 −1회. 나머지는 읽기 전용이에요.`
- 감속 (2026-09-06 사용자 지적): 자리는 룰렛 외형 머리 바로 아래(원판 고르기 위). 안내 `느긋하게는 끝에서 천천히 멈춰요.` (초안) — (폐기 2026-09-06) `느긋하게는 끝에서 오래 미적여요.` — 사용자: 워딩이 별로
- 공유 창 주소 상자 (2026-09-06 사용자 확정): 주소 카드 안에서 주소를 금테 상자에 넣어 강조 — 모노 17px 금색, [복사]는 채운 버튼(작은 크기 아님). 나눈 소스는 줄마다 상자(14px). 계정 줄은 그대로 창 맨 위(사용자: 아바타는 위가 좋다 — 목업의 "주소 카드를 위로"는 폐기). 소스 고르기 자리(두 장 그대로 / 카드 머리 오른쪽 두 칸)는 미정 — 지금은 그대로
- 미리보기 캡션: `지금 이 주소에 나가는 그림이에요.` — (폐기 2026-09-06) `— 판이 없으면 예시가 떠요.` · 예시 화면 리본 `예시 화면이에요 — 지금은 방송에 나가는 게 없어요. …`
- (폐기 2026-09-06) 대기실 도구줄 요약 `앉음 n · 직접 적음 n · 빈 자리 n` · 판 중 파티 줄 `파티원 n · 모집 중 · m분 남음 [들어오려는 사람 k] [초대 링크]` · 카드 `들어오려는 사람이 있어요 [보기]` · 창 `초대 링크`의 안내문

### 8.x (폐기 2026-09-06 밤 → §3.9 튜토리얼) 처음부터 같이 해보기 열 걸음 — 원문 보존

- 로비 권유 줄(로비 헤더 아래·두 카드 위 띠; (폐기 2026-09-06 낮) 내 판 카드 [+ 새 판 만들기] 바로 위): `리뉴얼됐어요. 4인 파티를 예시로 처음부터 같이 열어 봐요.` [됐어요] [같이 해보기]
- [?] 메뉴 첫 행(어느 화면에서든): `처음부터 같이 해보기` · `4인 파티 예시 · 10걸음 · 지금 판엔 영향 없음` · [시작] (본 뒤엔 `봤어요` [다시]) — (폐기 2026-09-06) `… · 저장 안 됨`, `로비에서`/`판이 없을 때 로비에서`(예시가 진짜 화면을 빌리던 때의 조건)
- 예시 앱 띠: `예시예요. 실제 판엔 아무 영향 없어요.` [그만두기] · 부모의 창이 뜨는 동안 `튜토리얼 시작 중`(사용자 지정 2026-09-06; 예시 앱이 첫 그림을 그릴 때까지 iframe 은 투명 — 흰 화면 점멸 없음) — (폐기 2026-09-06) `예시를 불러오는 중…`
- 예시 판 이름 `예시 파티`, 정원 4, 초대 코드 `TUTORIAL`(가짜, 복사 안 함)
- 걸음(번호는 화면 표시, 기다리는 말풍선은 앞 걸음과 같은 번호; 2026-09-06 여덟 걸음으로):
  1. [+ 새 판 만들기] `판부터 만들어 볼게요. 파티는 판 안에 있어요.`
  2. 첫 항목의 단가 `1회 단가는 여기를 누르면 고쳐요. 항목 이름은 바로 위 글자를 누르면 되고요.` [다음] — (폐기 2026-09-06, 같은 날) 표 머리 통째 `항목과 1회 단가는 표 머리에서 고쳐요. 이름을 누르면 이름, 단가를 누르면 값이에요. 항목을 늘리려면 [+ 항목].`
  3. [+ 항목] `항목은 여기서 늘려요. 지우는 건 항목 이름 옆 ×.` [다음]
  4. [링크 복사] `초대 링크를 복사해서 디코에 붙이면 돼요. 보내는 건 이번엔 저희가 대신할게요.` → 모집 카드 `보냈어요. 사람들이 들어올 거예요…`(잠김) → 실리안·니나브가 앉음(평소의 `방금 앉았어요`·`{닉}님이 n번 줄에 앉았어요.` 그대로)
  5. [시작] `두 명 왔어요. 한 명은… 안 들어오네요. 그냥 시작해 보죠.`
  6. 표 `칸을 🖱 누르면 1회, 🖱 우클릭하면 되돌려요. 한번 눌러 보세요.` → 누르면 `올라갔죠? 파티원은 자기 줄을 자수 탭에서 직접 눌러요. 실리안이 지금 누르는 중…`(잠김) → 실리안 자수(토스트 `실리안이 자수했어요 — 파티원이 누른 건 이렇게 올라와요.`) → 표 `실리안이 자수했어요. 파티원이 누른 건 이렇게 올라와요.`(잠김) — (폐기 2026-09-06) `칸을 🖱 누르면 1회예요. 파티원은 자수 탭에서 자기 줄을 직접 눌러요.` · `칸을 🖱 누르면 1회예요. 한번 눌러 보세요.`
  7. 표 아래 웨이 줄(평소의 `들어왔어요` [거절] [자리 정하기]·토스트 `웨이님이 들어왔어요 — 표 아래에서 받아 주세요.`) `웨이가 늦게 왔어요. 표 아래에 서 있죠? [자리 정하기]로 줄을 골라 앉혀요.` → 시트 `빈 줄, 퇴장한 사람 줄, 새 줄 중에 골라요. (모험가4) 줄을 눌러 볼까요?` — (폐기 2026-09-06) `… [받기]를 누르면 빈 줄에 앉아요.`
  8. [정산 끝내기] `다 끝나면 여기예요. 결과지가 기록에 남아요. 이제 들어온 파티원이 보는 화면도 볼게요.` [파티원 화면 보기] — (폐기 2026-09-06) `… 다시 보려면 [?]에서요.` [알겠어요]
  9. (파티원 예시 앱) 자수 카드 `들어온 파티원은 이 화면을 봐요. 자기 줄만 있고, 칸을 🖱 누르면 1회, 🖱 우클릭하면 되돌려요. 한번 눌러 보세요.`
  10. 자수 카드 `방장 벌금판에 바로 올라갔어요. 되돌리기는 30초 안에만 돼요. 다시 보려면 [?]에서요.` [알겠어요]
- 파티원 화면의 자수 사용법(통합 뒤 2걸음, 초안): `내 줄이에요. 칸을 🖱 누르면 1회, 🖱 우클릭하면 되돌려요.` [다음] · `누른 건 방장 벌금판에 바로 올라가요. 되돌리기는 30초 안에만 돼요. 방장 표는 [벌금표] 탭에서 봐요.` [알겠어요] — (폐기 2026-09-06) `내 줄이에요. 🖱 누르면 방장 표에 바로 올라가요. 잘못 눌렀으면 30초 안에 🖱 우클릭으로 되돌려요.` · `방장 표는 여기서 봐요.`
- (폐기 2026-09-06 사용자 확정, 통합) 로비 사용법 `판은 여기서 만들어요. 파티는 판 안에 있어요.` · `초대 코드나 링크를 받았으면 여기 붙여요.` · `끝난 판은 여기 남아요. 결과지를 다시 볼 수 있어요.` / 대기실 사용법 `링크를 복사해서 디코에 붙여요. 파티원은 누르면 바로 앉아요.` · `인원과 항목은 시작 전에 정해요. 이름은 시작 뒤에 적어요.` · `다 모이면 시작. 안 온 사람 자리는 비워 두고 시작해도 돼요.` / 벌금판 사용법 `칸을 🖱 누르면 1회가 쌓여요.` · `🖱 우클릭하면 1회가 빠져요.` · `단가는 머리의 숫자예요. 누르면 고칠 수 있고, 이미 센 것까지 바꿀지 그때 물어요.` · `이름 옆 사람 아이콘. 줄을 옮기거나 파티에서 내보내요.` · `정산 장부 탭엔 누른 게 정산돼 있고, 보낼 우편엔 누가 누구에게 얼마 보낼지 있어요.` · `다 끝나면 정산 끝내기. 결과지가 기록에 남아요.`

### 8.x 튜토리얼 (2026-09-06 밤, §3.9) — 걸음 문구는 §3.9 표가 원본. 그 밖의 문구, 전부 초안

- [?] 팝오버: `튜토리얼을 볼까요?` / 첫 방문 `처음이시죠? 튜토리얼을 볼까요?` · 행 `방장 튜토리얼` `판을 열고 파티원을 부르는 사람` (초안) `8장 · 판 만들기부터 끝내기까지` · `파티원 튜토리얼` `초대를 받은 사람` (사용자 지정) `8걸음 · 자수와 내 방송 주소` · 칩 `추천`·`봤어요` · [보기]/[다시 보기] ((폐기 2026-09-06 낮) [다시]) · 발치 `OBS에 넣는 방법과 방송 주소 안내는 [OBS 공유 설정] 창에 있어요.`
- 띠: `{n}장 {장 이름} · {i}/{m}` / `파티원 튜토리얼 · {i}/6` — (폐기) `예시예요. 실제 판엔 아무 영향 없어요.` [그만두기]
- 불러오는 동안: `튜토리얼을 시작하는 중이에요.`(사용자 지정) · 8장으로 갈 때 옛 화면 위 칩 `실리안의 화면을 불러오는 중이에요.` — (폐기 2026-09-06) `튜토리얼 시작 중` · `파티원 화면으로 넘어가는 중`
- 장 이름: 판 만들기 · 항목과 단가 · 파티원 모으기 · 벌금 세기 · 정산 보기 · 방송에 띄우기 · 끝내기와 기록 · 파티원 화면
- (폐기 2026-09-06 밤) 자수 사용법 `내 줄이에요. 칸을 🖱 누르면 1회, 🖱 우클릭하면 되돌려요.` · `누른 건 방장 벌금판에 바로 올라가요. 되돌리기는 30초 안에만 돼요. 방장 표는 [벌금표] 탭에서 봐요.` / 파티원 OBS 코치 `방송하시면 이 판을 OBS에도 띄울 수 있어요. 여기서요.` / 방장 OBS 코치 `방송에 띄우는 주소는 여기 있어요. OBS에 한 번만 넣으면 돼요.` / 사용법 모달 제목 `사용법`·`지금 이 화면`·`그 화면에서` / 옛 코스 힌트 `안내는 여기서 언제든 다시 볼 수 있어요.`

## 9. 버튼 배치 규칙

그때그때 놓아서 규칙이 없었다. 앞으로 만드는 화면은 이걸 따른다.

**창이 1120px보다 좁으면 가로 스크롤. 화면(로비·결과지·판·탭)이 바뀌면 스크롤은 맨 위로(2026-09-06 낮 사용자: 시작하기·파티원 화면 복귀 등 전환 전부)(2026-09-06).** 무대 폭 1080은 그대로. 좁은 창에서 표의 오른쪽 열(합계·×)이 카드 가장자리 뒤로 잘려 들어가는 것을 막는다. 좁은 화면용 재배치 규칙(520~900px)은 걷어냈다. (폐기, 같은 날) 무대 폭 1200 고정 — 무대를 넓히자 다른 데가 이상해졌다(사용자).

1. **한 줄 안에서 읽는 것은 왼쪽, 하는 것은 오른쪽 끝.** 값·설명은 왼쪽, 버튼은 오른쪽으로
   몰아 같은 세로선에 세운다. 눈이 한 군데만 보면 된다.
2. **섹션마다 채운 버튼은 하나.** 그것이 그 섹션에서 할 일이다. 나머지는 유령 버튼이나
   밑줄 문(`gs-swaplink`)으로 내린다.
3. **주 동작이 오른쪽 끝에 앉는다.** 거기가 손이 가는 자리다. 초대 줄이면 [디코용 복사]가
   오른쪽 끝이고 [새로 발급]이 그 왼쪽으로 물러난다.
4. **빨강은 되돌릴 수 없는 것에만.** 그것도 채운 버튼이 아니라 낮춘 무게로
   (`gs-swaplink-mute`) 둔다 — 위험은 확인창이 말한다. 물음·안내에는 빨강을 쓰지 않는다.
5. **도움말은 그 물음이 생기는 자리 옆에.** 머리줄에 몰면 제목이 밀린다.
   [OBS에 넣는 방법]은 주소 섹션 머리, [로그인하면 어떤 게 좋나요?]는 계정이 없는
   사람에게만.
6. **모달의 마무리(닫기·확인)는 오른쪽 하단.**
7. **자주 안 쓰는 입력칸은 접어 둔다.** 닉 바꾸기 칸이 늘 열려 있으면 계정 줄이 입력칸과
   버튼으로 차서, 정작 자주 쓰는 것과 무게가 같아 보인다.

**초대 링크의 만료** — 복사는 복사만 하고, 새로 발급은 눌러야 한다(표준). 죽은 링크를
복사하는 사고는 **창을 열 때 만료돼 있으면 그때 새로 내는 것**으로 막는다. 화면에 죽은
링크가 떠 있는 상태(`0분 남음`)가 아예 생기지 않는다. 기간은 **10분**이다. 창을 열 때 갱신되므로
방장은 늘 살아 있는 링크를 보고, 파티는 링크를 올린 뒤 몇 분 안에 꾸려진다 — 30분 뒤에
들어오는 사람은 이미 파티가 아니다.
