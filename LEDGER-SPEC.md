# 장부 동기화 v3 — 파티 판의 원본을 서버로

> v2(OBS-SPEC.md)는 계정·로비·자수까지를 정한 문서다. 이 문서는 그 위에서 **장부의 원본이
> 어디에 사는가** 하나만 바꾼다. 화면·문법·문구는 v2 가 그대로 원본이고, 여기서 폐기한다고
> 적은 것만 폐기된다.
>
> 출발점: 자수 한 번이 화면에 돌아오기까지 1초 안팎이 걸리고, 방장이 브라우저를 끄면 자수가
> 아예 안 된다. 두 문제의 뿌리가 같다 — **파티원의 자수가 방장 앱을 거쳐야 장부에 적힌다.**
>
> 이 문서는 설계 논의가 끝난 결과물이다. 여기 적힌 결정은 재논의 없이 그대로 구현한다.
> 배포는 사용자 지시가 있을 때만 하며, 워커와 앱(docs/)을 **같이** 배포한다.

---

## 0. 원칙

1. **쓰는 사람이 둘 이상인 판만 서버가 원본이다.** 계정 붙은 파티원이 하나라도 있으면 서버가
   장부를 갖는다. 없으면 지금 그대로 방장 로컬이 원본이고 서버로는 보여 주기만 한다.
2. **혼자 쓰는 판은 아무것도 안 바뀐다.** 인터넷이 끊겨도 계속 세고, 설치형 앱의 오프라인
   약속도 그대로다. v2 §0-1 "로컬이 원본이다"는 폐기가 아니라 **범위 축소**다.
3. **보내는 것은 계산 결과가 아니라 뜻이다.** 금액·차액·합계는 절대 싣지 않는다. 서버가
   접을 때 계산한다. 이유는 §2.2.
4. **순서는 방 하나가 정한다.** Room DO 가 유일한 순서 결정점이다. 어떤 조합이 동시에
   들어와도 답은 하나다.
5. **접는 함수는 한 벌이다.** 앱과 워커가 같은 순수 모듈을 나눠 쓴다. 두 벌이 되는 순간
   언젠가 어긋나고, 어긋난 것을 알아채기가 가장 어렵다.
6. **받는 쪽 계약은 안 바뀐다.** 오버레이가 받는 판의 모양이 지금과 같다. page.js 는 손대지
   않는다.
7. 방송 화면에 에러를 그리지 않는다 (v2 §0-5 유지).

---

## 1. 세 갈래 — 무엇이 어디에 사는가

지금 "판"이라 부르는 한 덩어리를 셋으로 가른다. 가르는 기준은 **쓰는 사람이 몇 명인가**다.

| 갈래 | 내용 | 쓰는 사람 | 사는 곳 |
|---|---|---|---|
| **표** | 줄·항목·횟수·열 합계·기타·기록 | 방장 + 파티원 | **서버** (이번에 옮기는 것) |
| **판 설정** | 정산 방식·우편 수수료·입력 단위·판 이름·룰렛 외형과 감속 | 방장 하나 | 방장이 밀어 올림 (지금 그대로) |
| **보는 사람 외형** | 판 테마·투명도·헤어라인·글자 크기·슬라이드·알림·합계·순액·끈 열 | 각자 자기 것 | 계정 (`/api/auth/look`, 지금 그대로) |

### 1.1 셋째 갈래는 이번 개편이 안 건드린다

오버레이 외형은 **사람마다 다르다**. 계정마다 방송용 주소가 하나씩이고, 그 주소가 자기 계정에
저장된 외형을 쓴다 (v2 §4.1, §5.2, 2026-09-07 확정). `lookOut()` 이 계정에 저장하고,
page.js 의 `viewOf` 가 서버에서 받은 판에 자기 설정을 입혀서 그린다. 끈 열은 열 머리와 각 줄의
숫자를 같은 자리에서 같이 뺀다.

그리는 시점에 각자 입히는 층이라 **순서를 정할 일이 아예 없다.** 연산 목록에 들어가지 않는다.

방장 것으로 얼려지는 외형은 룰렛뿐이다 — 원판/숫자 릴, 새틴/베가스, 감속. 판이 시작될 때
판에 실리고 모든 화면이 그 값을 쓴다 (v2 §5.2).

### 1.2 그래서 서버가 지켜야 할 것 하나

**서버가 내보내는 판에는 열이 전부 실려야 한다.** 방장이 자기 화면에서 끈 열이라도 빼면 안
된다. 무엇을 뺄지는 보는 계정이 자기 오버레이에서 정한다. 알림을 꺼도 연출거리를 그대로
보낸다. 지금 앱이 이미 그렇게 하고 있고, 그 이유가 켜 둔 파티원의 오버레이까지 같이
조용해지던 문제였다 (v2 §5.2, 2026-09-07).

---

## 2. 연산 — 표를 바꾸는 모든 것

### 2.1 목록

표를 바꾸는 것은 **전부** 연산이다. 방장의 구성 조작도 포함된다 — 단가가 언제 바뀌었는지가
그 앞뒤 누름의 금액을 정하기 때문에, 순서 밖에 둘 수 없다.

**방장이 보내는 것**

| op | 실리는 것 | 뜻 |
|---|---|---|
| `press` | `rowId, colId, dir` | 그 칸을 `dir` 만큼 (±1) |
| `roulette` | `rowId, colId, sid, num, mult, faces, from` | 룰렛 한 판의 결과. 금액은 안 싣는다 |
| `edit` | `rowId, target` | 그 줄 합계를 이 값으로 |
| `extra` | `rowId, reason, amount` | 기타 한 건 |
| `extra-del` | `rowId, exId` | 기타 줄 거두기 |
| `cancel` | `refId` | 그 기록 줄 역분개 |
| `price` | `colId, to, mode` | 단가 변경. `mode` 는 `retro` 또는 `forward` |
| `col-add` / `col-del` / `col-patch` | 항목 한 벌 / `colId` / `colId, key, value` | 항목 |
| `row-add` / `row-del` / `row-patch` | 자리 / `rowId` / `rowId, key, value` | 줄 |

**파티원이 보내는 것**

| op | 실리는 것 | 뜻 |
|---|---|---|
| `confess` | `rowId, colId, dir` | 자기 줄의 보통 항목만 |

`confess` 를 `press` 와 가르는 이유는 계산이 달라서가 아니라 **읽히는 말이 달라서**다. 기록 줄이
`자수`·`자수 정정`으로 뜨고, 방금 바뀐 카드에 `자수` 칩이 붙고, 방장 판에 말풍선이 뜬다
(v2 §5.6). 접는 계산은 `press` 와 똑같다.

### 2.2 뜻만 보낸다

지금 앱은 누를 때 **금액까지 계산해서** 기록에 적는다. 합계 직접 수정은 지금 합계를 읽어
차액을 낸다. 취소는 그 줄의 변화량을 반대로 건다. 전부 "지금 상태를 읽어 만든 값"이다.

이걸 그대로 서버로 보내면, 둘이 동시에 보냈을 때 **나중 것의 계산이 이미 틀린 값**이 된다.

그래서 연산에는 다음을 **싣지 않는다**: `delta`, `after`, `gold`, `name`, `item`.
전부 접을 때 계산하거나 그 시점 표에서 뜬다.

가장 또렷한 예가 `edit` 이다. "차액 −3만"이 아니라 **"합계를 47만으로"** 를 보낸다. 그러면
그사이 자수가 하나 끼어들어도 결과가 정확히 47만이 된다. 그게 합계 직접 수정이 뜻하는 바다.

### 2.3 멱등성

모든 연산에 보내는 쪽이 만든 `opId` 를 붙인다. 서버는 들고 있는 연산 목록 안에 같은 `opId` 가
있으면 조용히 버린다. 소켓이 끊겼다 붙는 사이 같은 것을 다시 보낼 수 있어서다.

룰렛은 `sid`(스핀 id)가 그 몫을 겸한다 — 한 스핀은 한 번만 적힌다.

---

## 3. 순서와 확정

- 방의 모든 연산이 그 방의 Room DO 하나로 들어간다. Durable Object 는 한 번에 하나씩
  처리하고, **저장을 기다리는 동안에는 다른 요청을 받지 않는다.** 그래서 연산 처리기가
  저장만 기다리면 통째로 원자적이다.
- **연산 처리기 안에서 다른 서비스로 fetch 를 하면 안 된다.** 그 순간 문이 열려 끼어들 수
  있다. 계정 확인은 소켓이 붙을 때 이미 끝나 있으므로 처리기 안에서 할 일이 없다.
- 서버가 `seq` 를 하나씩 매긴다. 이 번호가 유일한 진실이다.

### 3.1 동시에 들어왔을 때

| 상황 | 결과 |
|---|---|
| A의 자수와 방장의 같은 칸 클릭 | 둘 다 적힌다. 2 오른다 |
| A의 자수와 방장의 다른 칸 클릭 | 둘 다 적힌다. 서로 무관 |
| A의 자수와 방장의 그 항목 단가 변경 | 번호 순서대로. 자수가 앞이면 옛 단가, 뒤면 새 단가 |
| A의 자수와 방장의 그 줄 취소 | 번호 순서대로. 취소는 그때 존재하는 기록만 가리킨다 |
| A의 자수와 방장의 합계 직접 수정 | 수정이 뒤면 합계는 정확히 그 값. 자수는 차액에 녹는다 |
| A와 B의 자수 | 둘 다 적힌다 |
| 방장 기기 두 대 | 둘 다 보내는 쪽일 뿐이다. 표는 하나 |

마지막 줄이 지금 열려 있는 구멍을 닫는다. 지금은 기기 두 대가 각자 장부를 들고 각자 판
전체를 밀어 올려서, 한쪽에서만 누른 것을 나중에 민 쪽이 덮는다.

---

## 4. 접기 — 공유 순수 모듈

`shared/ledger.js` 하나를 만들고 앱과 워커가 나눠 쓴다. `worker/src/round.js` 가 워커와
시험이 나눠 쓰는 선례다.

```
apply(표, 연산) → { 표, 기록줄 }
```

**재생이 아니라 증분이다.** 서버는 접힌 표 한 장과 최근 기록 200줄만 들고 있으면 된다.
연산이 오면 표에 한 번 적용하고 목록에 붙인다. 이건 지금 앱이 하는 일과 정확히 같다 —
`pressCell` 이 `bump` 를 부르고 `appendLog` 를 부르는 그 구조를 그대로 옮기는 것이다.

옮겨야 할 계산은 [`src/GoldSettlement.jsx`](src/GoldSettlement.jsx) 안의 이것들이다.

| 함수 | 줄 | 옮기는 이유 |
|---|---|---|
| `pressCell` | 6609 | 누름의 금액 계산과 감소분의 마지막 건 되짚기 |
| `bump` | 6244 | 횟수와 열 합계를 같이 움직이는 규칙 |
| `editTotal` | 6719 | 차액을 기타 '직접 수정'으로 보내는 규칙 |
| `cancelEntry` | 6739 | 역분개. 횟수로 되돌릴 만큼은 횟수로, 나머지는 기타로 |
| `mergeExtra` | 6695 | 같은 사유의 기타 한 줄에 누적, 0이면 줄 삭제 |
| `applyPrice`·`freezeCol`·`thawCol` | 6961 | 소급/동결 두 갈래 |
| 룰렛 결과 적용 | 6480 | 나온 숫자 × 단가, 빼기 면의 `clampCut` |
| `boardOf`·`ovCols` | 4680·4763 | 표에서 방송용 판을 만드는 부분 |

**앱도 같은 모듈로 자기 화면을 먼저 그린다.** 방장이 누르면 화면은 지금처럼 즉시 반응하고,
서버 판이 도착하면 그것으로 맞춘다. 같은 함수라 어긋나는 경우는 진짜 경합뿐이고, 그때는
서버가 이긴다.

파티원 화면에는 이 예측을 걸지 않는다 (§11).

---

## 5. 오프라인

규칙 하나로 떨어진다. **오프라인 조작은 나 말고 쓰는 사람이 없을 때만 허용한다.**

| 판 | 서버에 못 닿을 때 |
|---|---|
| 혼자 판 (계정 붙은 파티원 0명) | 지금 그대로 계속 센다. 붙으면 쌓인 연산을 순서대로 민다 |
| 파티 판 (파티원 1명 이상) | 아무도 못 쓴다. **방장 화면도 잠근다** |

혼자일 때 replay 가 항상 맞는 이유는 충돌할 상대가 없어서다. 파티원이 있으면 아예 막으니
충돌이 생길 자리가 없다. 증명이 필요 없는 규칙이다.

방장 화면을 잠글 때는 이미 있는 **얼어 있는 판** 표시를 쓴다 (v2 §3.4). 새 상태를 만들지
않는다.

### 5.1 남는 틈 하나

방장이 혼자 판을 끊긴 채 세는 동안, 살아 있는 초대 코드로 누가 들어와 자수하는 경우.
초대 코드는 부른 때부터 10분이라 창이 좁고, 그 안에 이 조합이 겹칠 일은 드물다.

그대로 둔다. 누름은 더하기라 순서가 밀려도 맞고, 어긋나는 것은 합계 직접 수정 하나뿐이다.
**나중에 붙는 쪽이 이긴다** — 방장이 마지막 판단자라는 지금 감각과 맞는다.

---

## 6. 서버 (Room DO 변경)

### 6.1 저장

추가되는 것: `sheet` (접힌 표), `ops` (최근 200, `seq` 순), `seq`.

`state` 는 남는다. 담는 것이 **판 설정만**으로 줄어든다. 표는 서버가 만든다.

### 6.2 통로

연산은 **이미 붙어 있는 소켓으로만** 오간다. 소켓은 붙을 때 자격 확인이 끝나 있어서
계정부 왕복이 없다. 판을 보고 있는 사람은 소켓이 이미 붙어 있다.

| 방향 | 메시지 |
|---|---|
| 앱 → 서버 | `{kind:"op", opId, op:{...}}` — 뷰어 소켓(파티원)·서기 소켓(방장) 양쪽에서 |
| 서버 → 전원 | `{kind:"state", state}` — **지금과 같은 모양** |
| 서버 → 보낸 사람 | `{kind:"nope", opId, why}` — 거절 |

거절 사유는 지금 것을 그대로 쓴다: `late`(30초 지난 되돌리기), `slow down`(분당 20회),
`forbidden`(남의 줄), `paused`. `scribe-off` 는 없어진다.

### 6.3 폐기되는 라우트

- `POST /api/r/:id/confess` — 소켓이 유일한 통로가 된다.
- 서기 소켓의 `{kind:"confess"}` 중계 — 방장 앱이 자수를 대신 적을 일이 없다.

`PUT /api/r/:id/state` 는 남는다. 판 설정 푸시가 그 몫이다.

### 6.4 권한

방이 직접 본다. 지금 `/confess` 가 하는 검사를 그대로 옮긴다.

- 파티원: `m:<acct>` 가 `st:"ok"` 이고, `op.rowId === m.rowId` 이고, 보통 항목일 때만.
- 방장: `owner` 일 때 전부.
- 분당 20회 상한, 30초 되돌리기 창, 얼어 있는 판 검사 그대로.

---

## 7. 앱 변경

- 표를 바꾸는 모든 자리가 **연산을 만들어 소켓으로 보내는** 것으로 바뀐다. 계산은 안 한다.
- 파티 판이면 서버 판을 받아 그린다. 혼자 판이면 지금 그대로 로컬에서 접는다.
- 방장 화면은 §4 의 예측으로 즉시 반응한다.
- 파티원 화면의 `방장이 자리를 비웠어요` 계열이 필요 없어진다. 방장이 없어도 자수가 된다.
- 300ms 디바운스 푸시는 판 설정에만 남는다. 표는 연산이 즉시 나간다.

---

## 8. 오버레이 — 안 바뀝니다

`worker/src/page.js` 는 손대지 않는다. 받는 판의 모양이 같고, `viewOf` 가 하는 일도 같다.

이게 서버가 접기로 한 이유다. 클라이언트가 접는 설계였다면 접는 모듈을 page.js 안으로도
넣어야 했고, 새로 붙는 오버레이마다 연산 목록을 재생해야 했다.

---

## 9. 비용

| 항목 | 무료 한도 | 동작 하나당 |
|---|---|---|
| DO 요청 | 100,000 / 일 | 0.05 (소켓 수신 20건이 요청 1건) |
| 쓰기 | 100,000 / 일 | 2 (표 1 + 연산 목록 1) |
| 읽기 | 5,000,000 / 일 | 소켓 붙을 때 표 한 장 |
| 저장 | 5 GB | 판마다 표 한 장 + 연산 200 |
| 시간 | 13,000 GB-s / 일 | 하이버네이션 중인 소켓은 안 셈 |

쓰기가 병목이고, 하루 5만 동작까지 들어간다. **지금보다 싸다** — 지금은 자수 한 번에 워커
요청 2회와 DO 요청 4회가 든다. 계정부를 두 번 거치기 때문이다. 소켓으로 옮기면 그 왕복이
전부 사라진다.

한도에 가까워질 요인은 여전히 자수가 아니라 방치된 오버레이의 주소 재조회다 (v2 §5.8).

---

## 10. OBS-SPEC.md 에서 폐기·수정되는 것

| 조항 | 처분 |
|---|---|
| §0-1 "로컬이 원본이다" | **범위 축소.** 혼자 판은 그대로, 파티 판은 서버 |
| §4.3 "서버는 해석하지 않는다" | **폐기.** 서버가 접는다 |
| §4.2 `/confess` 의 `scribe-off` 409 | **폐기.** 방장 없이도 된다 |
| §7 "자수 큐(방장 오프라인 보관) 안 만듦" | **폐기.** 자연히 생긴다 |
| §5.2 서기 소켓의 자수 중계 | **폐기** |
| §3.6 "낙관 갱신 없음" | **유지.** §11 |
| §5.6 자수 탭·되돌리기 창·자수 알림 | **유지.** 화면은 그대로 |
| §5.2 "룰렛 말고는 전부 계정 것" | **유지.** §1 이 그 위에 선다 |

---

## 11. v3 에서 하지 않는 것

**파티원 화면의 낙관 갱신.** 누른 즉시 내 숫자를 먼저 올려 두는 방식이다. 셋이 걸린다.
① 거절당하면 숫자가 올랐다 내려간다 — 분당 상한·30초 창·얼어 있는 판이 실제 거절 사유다.
② 남의 조작과 섞이면 잠깐 틀린 숫자를 보여준다. 방장이 같은 칸을 동시에 눌렀다면 내 화면은
하나인데 실제는 둘이다. ③ 예상값과 확정값을 따로 들고 되맞추는 규칙이 는다 — 앱은 이미
연타를 받아내려고 그림자 값을 쓰고 있어서 그것과도 겹친다.
왕복이 0.1~0.2초가 되면 칸이 번쩍이는 지금 신호로 충분하다. 지금 답답한 원인은 낙관 갱신이
없어서가 아니라 왕복이 길어서다.

그 밖에: 파티원의 구성 조작(항목·단가·인원) / 지난 판 서버 보관 / 연산 목록 무한 보관 /
계정당 방 여러 개 — 전부 v2 §7 그대로.

---

## 12. 결정 경위 (다시 파지 않기 위한 기록)

- **2026-09-08 개편 발의.** 자수 지연과 방장 오프라인 자수 불가가 같은 뿌리라는 것이 출발점.
  당시 "로컬이 원본"의 이유를 무료 플랜 절약으로 기억하고 있었으나, 첫 릴레이 커밋(3c21f5d,
  2026-08-28)의 원칙문을 확인한 결과 **비용은 이유가 아니었다.** 적혀 있던 이유는 셋이다 —
  앱이 원래 서버 없는 계산기였고 릴레이는 얹은 층이라는 것, 계정이 없어 서버가 권한을 몰랐다는
  것, 방장 인터넷이 끊겨도 기록이 이어져야 한다는 것. 둘째는 v2 의 계정으로 사라졌고, 셋째는
  §5 의 규칙으로 혼자 판에만 남긴다.
- **검토하고 뺀 안 둘.** (a) 배관만 고치기 — 자수를 소켓으로 옮기고 디바운스를 없앤다. 지연은
  잡히지만 방장 오프라인을 못 푼다. (b) 자수 큐만 만들기 — 방장 부재 중 접수만 하고 판은 안
  움직인다. 파티원이 자기 방송에 오버레이를 띄우고 있으면 멈춘 판이 그대로 보인다. 반쪽이다.
- **서버가 접는가, 클라이언트가 접는가.** 서버로 정했다. page.js 를 안 건드리고, 새로 붙는
  오버레이가 연산을 재생하지 않아도 되고, 압축이 저절로 된다. 접는 모듈을 뽑아내는 비용은
  어느 쪽이든 똑같이 든다.
- **테마의 주인 (2026-09-08 사용자 정정).** 초안에서 판 테마를 "방장 설정"으로 묶었으나
  틀렸다. 테마를 포함한 오버레이 외형은 **계정마다 따로**이고, 사람마다 자기 방송용 주소가
  하나씩 있어서 각자 고른다 (v2 §5.2, 2026-09-07 확정). 룰렛만 방장 것이다. 그래서 갈래가
  둘이 아니라 셋이 됐고, 셋째 갈래는 이번 개편의 대상이 아니다 — 그리는 시점에 각자 입히는
  층이라 순서를 정할 일이 없다.
- **오프라인 규칙 (2026-09-08 사용자 확정).** 방장 인터넷이 끊긴 파티 판은 세지 않는다.
  이걸 §0-1 의 범위 축소와 합치니 "나 말고 쓰는 사람이 없을 때만 오프라인 조작"이라는 규칙
  하나로 떨어졌다.
- **낙관 갱신은 안 넣는다 (2026-09-08).** 왕복이 짧아지면 얻는 것이 적고 §11 의 셋을 그대로
  떠안는다.
- **한 번에 간다 (2026-09-08 사용자 확정).** 디바운스만 먼저 줄이는 중간 배포는 하지 않는다.

---

## 13. 검증 (배포 전, 전부 로컬)

1. **접기 모듈이 지금 앱과 같은 표를 내는가.** 기존 판의 기록을 연산으로 바꿔 재생하고 표를
   견준다. 이게 이번 개편의 주 위험이다.
2. **단가 소급/동결 두 갈래**가 재생 뒤에도 같은 금액을 내는가.
3. **동시 입력 넷**: 같은 칸, 다른 칸, 자수와 취소, 자수와 합계 직접 수정.
4. **방장 기기 두 대**에서 각각 누르고 표가 하나로 남는가.
5. **방장 브라우저를 끈 채** 파티원이 자수하고, 오버레이가 따라 움직이는가.
6. **혼자 판을 오프라인**에서 세고, 붙었을 때 그대로 올라가는가.
7. **파티 판에서 방장이 끊겼을 때** 방장 화면이 잠기는가.
8. **오버레이 외형이 사람마다 다른가** — 끈 열·슬라이드·테마가 각자 것으로 남는가. page.js 를
   안 건드렸으니 회귀 확인이다.
9. 룰렛 한 판이 네 화면에서 같은 장면으로 보이고, 결과가 한 번만 적히는가.

---

## 14. 문구 (전부 초안)

새로 필요한 것은 하나뿐이다. 파티 판에서 방장이 끊겼을 때 방장 화면에 뜨는 줄이다.

- `인터넷이 끊겼어요 — 다시 이어지면 그때부터 세요.`

없어지는 것: 파티원 화면의 `방장이 자리를 비웠어요 — 돌아오면 다시 누를 수 있어요.` 계열.
방장이 없어도 자수가 되므로 쓸 자리가 사라진다.

그 밖의 문구는 v2 §8 이 그대로 원본이다.
