# 실시간 경로 v3 — 자수와 푸시를 소켓으로

> v2(OBS-SPEC.md)의 구조는 그대로 둔다. **장부의 원본은 방장 브라우저다.** 이 문서는
> 그 구조 안에서 **말이 오가는 길만** 바꾼다.
>
> 출발점 셋. ① 파티원의 자수가 화면에 돌아오기까지 1초 안팎이 걸린다. ② 룰렛 연출이
> 같은 지연을 탄다. ③ 방장이 탭을 닫으면 파티원에게 바로 알려지지만, 방장의 인터넷이나
> PC 가 그냥 죽으면 **아무도 모른 채 자수가 허공으로 간다.**
>
> 이 문서는 설계 논의가 끝난 결과물이다. 여기 적힌 결정은 재논의 없이 그대로 구현한다.
> 배포는 사용자 지시가 있을 때만 하며, 워커와 앱(docs/)을 **같이** 배포한다.
>
> **LEDGER-SPEC.md 와의 관계**: 그 문서는 장부의 원본을 서버로 옮기는 큰 안이다.
> **보류.** 그 안이 풀던 문제 중 "방장이 꺼져도 자수가 된다"를 요구에서 뺐고(2026-09-08
> 사용자), 그러자 서버가 장부를 가질 이유가 사라졌다. 그 문서의 §2.4(기록에 안 남는 셋)만
> 이 개편과 무관하게 살아 있다 — §6.2.

---

## 0. 원칙

v2 §0 을 그대로 잇는다. 이 개편이 더하는 것은 하나뿐이다.

8. **자수의 왕복이 방장 생사의 증거다.** 소켓이 열려 있다는 사실은 방장이 살아 있다는
   증거가 못 된다. 자수를 넘겼는데 판이 안 돌아오면 방장은 없는 것으로 본다.

바뀌지 않는 것을 못 박아 둔다. 원본은 방장 로컬이고(v2 §0-1), 서버는 판을 해석하지 않으며
(v2 §4.3), 파티원 화면에 낙관 갱신은 없다(v2 §3.6).

---

## 1. 지금 무엇이 느린가

자수 한 번이 파티원 화면에 돌아오기까지의 길이다.

| 단계 | 경로 | 비용 |
|---|---|---|
| 1 | 파티원 `POST /confess` → 워커 → 계정부 DO(세션 확인) → 방 DO | HTTP 왕복 |
| 2 | 방 DO → 서기 소켓 → 방장 앱 | 수십 ms |
| 3 | 방장 앱이 장부에 적고 **300ms 디바운스** | 300ms |
| 4 | 방장 `PUT /state` → 워커 → 계정부 DO → 방 DO | HTTP 왕복 |
| 5 | 방 DO → 뷰어 소켓 → 파티원 화면 | 수십 ms |

측정(2026-09-08, 개발 PC → 배포된 워커):

| 요청 | 걸린 시간 |
|---|---|
| 워커만 (`/`) | 0.15초 |
| Bearer 를 붙인 방 API | 0.18~0.78초 |
| Bearer 없는 방 API | 0.17~0.22초 |

계정부 DO 를 거치는 왕복이 비싼 쪽이다. 1·4단계가 그것이고, 3단계의 300ms 가 더해진다.
**룰렛 국면(`sid`·`phase`·`rolling`·`skipAt`·`fast`·`out`)도 같은 4단계를 탄다** — 그래서
파티원 화면과 방송의 원판이 방장 화면보다 늦는다.

방장 탭이 가려져 있으면 3단계가 더 늘어날 수 있다. 브라우저가 가려진 탭의 타이머를 늦추고
`setTimeout` 이 거기 걸린다. 사용자 환경에서만 잴 수 있어 수치는 없다.

---

## 2. 바꾸는 것 넷

### 2.1 자수를 뷰어 소켓으로

파티원은 판을 보고 있으므로 뷰어 소켓이 **이미 붙어 있고, 붙을 때 자격 확인이 끝나 있다.**
그 길로 보내면 HTTP 왕복과 계정부 조회가 통째로 빠진다.

- 앱 → 서버: `{kind:"confess", cid, rowId, colId, dir}`.
  `cid` 는 파티원이 매기는 이 자수의 번호다. 왕복 감시(§2.4)와 중복 방지에 쓴다.
- 서버는 지금 `POST /confess` 가 하는 검사를 **그대로** 한다: 멤버십 `st:"ok"`,
  본인 `rowId`, 얼어 있는 판, 분당 20회, 30초 되돌리기 창, 서기 유무.
- 통과하면 서기 소켓으로 넘긴다. 거절이면 **보낸 소켓에만** `{kind:"nope", cid, why}`.
  `why` 는 지금 쓰는 값 그대로다: `late` · `slow down` · `forbidden` · `paused` · `scribe-off`.
- **뷰어가 보낼 수 있는 것은 자수 하나뿐이다.** v2 §4.2 의 "뷰어 메시지는 전부 무시"가
  여기서만 열린다. 나머지 메시지는 그대로 무시한다.

`POST /api/r/:id/confess` 는 **남긴다**. 소켓이 안 붙어 있을 때의 길이고, 배포 직후 옛 앱이
떠 있는 브라우저가 계속 도는 길이다. 이것 하나로 배포 전환에 별도 장치가 필요 없어진다.

### 2.2 방장 푸시를 서기 소켓으로

- 앱 → 서버: `{kind:"state", state, bindings?}`. 서버는 `PUT /state` 가 하던 일을 그대로
  한다 — 저장, `bindings` 반영, 전 구독자 브로드캐스트, 알람 재설정.
- **소켓이 안 열려 있으면 지금의 HTTP PUT 으로 떨어진다.** 두 길이 같은 함수를 부른다.
- 소켓 전송에는 응답이 없다. 보내는 도중에 끊기면 그 한 장이 조용히 사라진다. 그래서
  **서기 소켓이 붙는 즉시 판을 한 장 민다.** 마지막 변경이 영영 안 올라가는 길을 막는다.

### 2.3 즉시 미는 것과 미루는 것

300ms 디바운스는 **연타를 묶으려고** 있는 것이다. 남이 기다리는 사건에는 쓸 이유가 없다.

| 무엇 | 언제 |
|---|---|
| 자수 반영 | **즉시** |
| 룰렛 국면 변화 | **즉시** |
| 방장 자신의 누름·이름·항목·단가·기타 | 300ms 유지 |

즉시 밀 때는 **걸려 있던 디바운스 타이머를 함께 끈다.** 안 끄면 같은 판이 곧바로 한 번 더
나간다. 판 전체를 싣는 푸시라 그냥 낭비다.

### 2.4 자수 왕복 감시 — 이 개편의 핵심

**소켓이 열려 있다는 것은 방장이 살아 있다는 증거가 아니다.** 방장이 탭을 닫으면 닫힘
신호가 와서 서버가 즉시 안다. 그러나 인터넷이 끊기거나 PC 가 죽으면 닫힘 신호가 없다.
`setWebSocketAutoResponse` 로 ping 을 런타임이 대신 답하게 해 둔 터라(하이버네이션 유지,
v2 §5.8) 서버는 그것으로도 생사를 못 본다. 그래서 지금은 **자수가 200 을 받고 아무도 없는
곳으로 넘어가고, 파티원 화면에는 오류도 안 뜨고 숫자도 안 움직인다.**

고침: 서버가 자수를 서기에게 넘긴 시각을 적어 둔다.

1. 넘긴 뒤 **`SCRIBE_ACK_MS` 안에 그 서기로부터 `state` 가 오지 않으면** 서기를 죽은 것으로
   본다.
2. 보낸 파티원에게 `{kind:"nope", cid, why:"scribe-off"}`.
3. **그 서기 소켓을 닫는다.** 닫히면 지금 있는 `gone()` 이 `{kind:"presence", scribeOn:false}`
   를 전원에게 뿌리고, 모두의 자수 칸이 같이 잠기며 `방장이 자리를 비웠어요.` 가 뜬다.

별도의 하트비트를 만들지 않는다. **자수 자체가 생존 확인 신호이고, 결과는 이미 있는
presence 배관으로 흐른다.** 새 문구도 없다.

- 방장이 살아 있는데 느려서 걸린 경우에도 회복은 저절로 된다. 앱이 소켓을 다시 붙이고,
  §2.2 의 규칙대로 붙는 즉시 판을 한 장 민다.
- 그래도 오진의 대가가 있으므로 `SCRIBE_ACK_MS` 는 넉넉히 잡는다. **3초.**
  왕복이 0.2~0.3초로 줄어든 뒤의 값이라 열 배 여유다.
- 자수가 하나도 안 들어오는 동안은 감시가 돌지 않는다. 그래도 된다 — **아무도 안 누르면
  방장의 생사가 화면에 미치는 것이 없다.**

---

## 3. 서버 변경 (worker/src/index.js)

- `Room.webSocketMessage(ws, msg)` 가 실제로 일한다. 지금은 전부 무시한다.
  - 태그가 `v`(뷰어)면 `confess` 하나만 받는다.
  - 태그가 `scribe`면 `state` 하나만 받는다.
  - 그 밖은 지금처럼 무시한다. `ping` 은 런타임이 계속 대신 답한다.
- `/confess` 와 `/state` 의 몸통을 함수로 뽑아 HTTP 와 소켓이 같이 부른다. **검사를 두 벌로
  만들지 않는다** — 갈라지면 소켓 쪽만 느슨해지는 사고가 난다.
- 자수를 서기로 넘길 때 대기 표시를 남기고, `state` 가 오면 지운다. 알람 하나로 만료를
  본다 — 방에는 이미 알람이 하나 돌고 있다(v2 §4.2).
- 기존 HTTP 라우트는 **하나도 지우지 않는다.**

## 4. 앱 변경 (src/GoldSettlement.jsx)

- 파티원: `sendConfess` 가 소켓으로 보낸다. `cid` 를 매기고, `nope` 를 지금 `catch` 가 하던
  처리에 그대로 연결한다. 소켓이 없으면 지금의 HTTP 로 떨어진다.
- 방장: 푸시가 서기 소켓으로 나간다. 소켓이 없으면 HTTP. 서기 소켓 `onopen` 에서 한 장.
- 즉시 푸시 갈래를 만든다(§2.3). 지금 푸시는 의존성 배열 하나에 전부 묶여 있다.

## 5. 안 바뀌는 것

원본이 방장 로컬이라는 것 · 서버가 판을 해석하지 않는다는 것 · 파티원 낙관 갱신 없음 ·
`worker/src/page.js` · 오버레이가 받는 판의 모양 · 계정별 외형 · 룰렛이 방장 것이라는 것 ·
§8 문구 전부. **새로 쓸 문구가 없다.**

## 6. 이 개편이 안 고치는 것

### 6.1 방장 기기 두 대

원칙 3(좌석 없음)이 같은 계정의 다중 접속을 허용하는데, 방장 탭이 둘이면 **장부가 둘이
된다.** 서버는 자수를 양쪽 서기에 다 넘기고, 양쪽이 각자 판 전체를 밀어 올린다. 한쪽에서만
누른 것이 있으면 나중에 민 쪽이 덮는다.

이건 **서버가 원본이어야만 풀린다**(LEDGER-SPEC.md). 이번 범위 밖이다.
싼 완화가 하나 있다 — 서기 소켓이 둘 이상이면 나중에 붙은 탭에 한 줄 띄우는 것. 만들지
여부는 미정으로 둔다.

### 6.2 기록에 안 남는 셋

항목 삭제 · 인원 삭제 · 기타 금액 직접 수정이 돈을 움직이면서 기록에 한 줄도 안 남는다
(LEDGER-SPEC.md §2.4, 2026-09-08 실측). **기록에 남기고 취소 버튼은 안 붙인다**로 확정됐다.
이 개편과 겹치는 곳이 없어서 따로 하면 된다.

---

## 7. 비용

자수 한 번당.

| 항목 | 무료 한도 | 지금 | 이 개편 |
|---|---|---|---|
| 워커 요청 | 100,000 / 일 | 2 | 0 |
| DO 요청 | 100,000 / 일 | 4 | 0.1 |
| 쓰기 | 100,000 / 일 | 3~4 | 3~4 |
| 시간 | 13,000 GB-s / 일 | 하이버네이션 중인 소켓은 안 셈 | 같음 |

소켓 수신은 20건이 요청 1건으로 계산된다. **요청이 줄고 쓰기는 그대로다.** 계정부(전역 DO
하나)를 두드리는 횟수가 자수마다 2회씩 사라지는 것이 실질적인 이득이다 — v2 §5.8 이 걱정하던
전역 DO 점유가 그만큼 준다.

---

## 8. 검증 (배포 전, 전부 로컬)

1. 자수가 소켓으로 가고, 방장 장부에 적히고, 판이 돌아와 파티원 화면이 바뀌는가.
2. 거절 넷이 그대로 뜨는가 — 30초 지난 되돌리기 · 분당 상한 · 남의 줄 · 얼어 있는 판.
3. 룰렛 한 판이 방장·파티원·방송에서 같은 박자로 도는가. 건너뛰기와 빠르게도 따라오는가.
4. **방장 탭을 닫으면** 파티원 칸이 잠기고 문구가 뜨는가. (지금도 되는 것 — 회귀 확인)
5. **방장 쪽 네트워크를 끊으면** 자수 한 번으로 3초 안에 같은 상태가 되는가.
6. 끊었다 이으면 방장이 붙는 즉시 판이 한 장 올라가고 전원이 회복되는가.
7. 소켓을 막아 둔 채(HTTP 만) 자수와 푸시가 그대로 되는가 — 옛 앱 호환 확인.
8. 방장이 연타할 때 판이 300ms 로 묶여 나가고, 자수는 그 사이에도 즉시 나가는가.
9. 오버레이가 안 바뀌었는가 — 외형·끈 열·슬라이드 회귀 확인.

---

## 9. 문구

**새로 만드는 것이 없다.** §2.4 의 결과는 이미 있는 `방장이 자리를 비웠어요.` 로 흐른다.
같은 상황에 같은 말을 쓰는 것이 이 개편의 이득 중 하나다.

---

## 10. 결정 경위

- **2026-09-08, 큰 안에서 배관 안으로.** 처음 요구는 "방장이 꺼진 상태로도 자수가 된다"를
  포함했고, 그것 하나가 서버에 장부를 두라고 요구했다(LEDGER-SPEC.md). 같은 날 요구가
  **"자수는 안 돼도 되지만 왜 안 되는지는 알아야 한다"** 로 바뀌었다. 그 순간 서버가 장부를
  가질 이유가 사라졌고, 앞서 "지연은 잡히지만 방장 오프라인을 못 푼다"며 뺐던 배관 안이
  정답이 됐다. 큰 안은 폐기가 아니라 보류다 — 기기 두 대 문제는 그 안에서만 풀린다.
- **왜 하트비트를 따로 안 만드는가.** 서버가 서기에게 주기적으로 물으면 하이버네이션이
  깨져서 v2 §5.8 이 지키려던 "떠 있는 소켓은 거의 공짜"가 무너진다. 자수를 신호로 쓰면
  **누가 실제로 기다릴 때만** 확인이 돌고, 아무도 안 누르는 동안은 아무 일도 안 한다.
- **왜 HTTP 라우트를 안 지우는가.** 배포해도 브라우저에 떠 있는 옛 앱은 즉시 안 바뀐다.
  두 길이 같은 함수를 부르므로 남겨 두는 값이 거의 0 이고, 그 대가로 배포 전환 장치가
  통째로 필요 없어진다.
- **왜 파티원 낙관 갱신을 여전히 안 넣는가.** 왕복이 0.2~0.3초가 되면 얻는 것이 적고,
  거절당했을 때 숫자가 올랐다 내려가는 그림과 예상값·확정값 두 벌의 상태가 남는다
  (2026-09-08 확정).
