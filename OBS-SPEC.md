# OBS 오버레이 중계 — 구현 명세

> **갱신(구현 반영):** 읽기 전용 화면은 워커의 별도 현황판이 아니라 **앱 자체의 읽기 전용 모드**다.
> 공유 주소를 브라우저로 열면 앱(`#live=ROOMID`)으로 넘어가 서기와 같은 3탭을 조작만 잠긴 채 본다.
> 이에 따라 state 에 `board`(오버레이용 순위)와 `full`(cols·rows·feePercent·unit)이 함께 실린다. 기록(log)은 보내지 않는다.
> §3.3 의 워커 현황판·테마 생성기는 v1 에서 제외 — 테마는 URL 파라미터(`?t=&bg=&s=`)로 지정한다.

이 문서는 설계 논의가 끝난 결과물이다. 여기 적힌 결정은 재논의 없이 그대로 구현한다.
문구는 **한 글자도 바꾸지 말고** 그대로 쓴다 (해요체). 배포는 사용자 지시가 있을 때만 한다.

## 0. 원칙

1. **로컬이 원본이다.** 진본은 서기 브라우저의 localStorage. 서버는 표시용 릴레이일 뿐이다.
   인터넷·서버가 죽어도 기록은 계속되고, 복구되면 오버레이가 따라잡는다.
2. **기록은 서기 한 명.** 읽기는 링크를 아는 누구나. 쓰기 열쇠는 어떤 URL에도 싣지 않는다.
3. **방송 화면에 에러를 그리지 않는다.** 오버레이는 어떤 실패 상황에서도 침묵(투명)이 기본.
4. 로그인·비밀번호·파일 백업 없음.
5. 기존 앱 구조(오프라인·URL 공유·역분개 기록)는 그대로. 동기화는 얹는 계층이다.

## 1. 용어와 모델

- **방(Room)** — 명단(프리셋) 하나당 하나. 주소는 서기가 재발급하기 전까지 영구.
- **공유 주소** — `https://live.lostark-sheet.workers.dev/r/{ROOM_ID}` (읽기 전용).
  OBS 브라우저 소스에 넣으면 오버레이, 일반 브라우저로 열면 현황판.
- **쓰기 열쇠(key)** — 방 생성 시 발급되는 긴 난수. 서기 브라우저 localStorage에만 저장.
- **예비 열쇠** — 열쇠 꾸러미를 6자리 1회용 코드로 다른 기기(PC·폰)에 복제하는 기능.
  기기 이사와 분실 대비를 같은 장치 하나로 해결한다.

### 시나리오 (전부 이 설계로 성립해야 한다)

| 상황 | 동작 |
|---|---|
| 같은 명단으로 다음날 또 | 아무도 아무것도 안 함. 서기는 '처음부터 다시해!!'만 |
| 로사단 ↔ 로스피 교대 | 명단마다 방이 달라 각자 주소로 나감. OBS 재설정 없음 |
| 다른 사람들로 새 파티 | 다른 방·다른 주소. 옛 파티원에겐 구조적으로 안 보임 |
| 스트리머 B·C도 자기 방송에 표시 | 같은 공유 주소를 각자 OBS에 넣음. 명단마다 소스 하나, 방송 날 눈 아이콘만 토글 |
| 방송 중에 링크 받아 소스 추가 | 파티가 살아 있으면 1초 안에 이름 8개가 뜸 = 성공 확인. 안 뜨면 실패 |
| 유출 의심·게스트 이탈 | 그 방만 [주소 새로 발급] |
| 서기 브라우저 초기화 | 예비 열쇠 기기에서 복원. 없으면 새 방 + 재공유 |

## 2. 서버 (worker/)

Cloudflare Workers + Durable Objects(SQLite 백엔드, 무료 플랜). 워커 이름 `live`,
계정 서브도메인 `lostark-sheet.workers.dev` (가입·로그인 완료 상태).
`worker/wrangler.jsonc` + `worker/src/index.js` 초안이 이미 있다 — **미검증**이므로
명세와 대조·수정해서 쓰고, 다르면 명세가 이긴다.

### 2.1 API

| 메서드·경로 | 하는 일 |
|---|---|
| `POST /api/rooms` | 방 생성. `{roomId(6자), key(26자)}` 반환. 문자는 `ABCDEFGHJKMNPQRSTVWXYZ23456789` (혼동 글자 제외) |
| `PUT·POST /api/r/:id/state` | 본문 `{key, state}`. 열쇠가 맞으면 저장 + 전 구독자에 브로드캐스트. 32KB 상한, 초과 413 |
| `POST /api/r/:id/kill` | 본문 `{key}`. 방을 dead 처리, 상태 삭제, 구독자에 `{kind:"dead"}` 보내고 닫음. 재발급의 뒷정리 |
| `GET /api/r/:id/live` | WebSocket 업그레이드. 접속 즉시 현재 상태(또는 dead) 1회 전송, 이후 변경분 push. 구독자 메시지는 전부 무시(읽기 전용). `ping`→`pong` 자동 응답(하이버네이션 API) |
| `GET /api/r/:id/peek` | `{state}` 또는 `{dead:true}` — 페이지 첫 렌더용 폴백 |
| `POST /api/handoff` | 본문(열쇠 꾸러미 JSON)을 5분간 보관, `{code(6자)}` 반환 |
| `GET /api/handoff/:code` | 꾸러미 반환 후 즉시 소각(1회용). 10분 창에 실패 30회면 429 |
| `GET /r/:id` | 공유 페이지 HTML (3장) |

- CORS: `*` 허용 (GitHub Pages에서 호출).
- 잘못된 열쇠 403, dead 방 쓰기 410.
- **수명: 내용은 지우지 않는다.** 마지막 갱신 후 **90일**이 지나면 알람으로 방을 통째 삭제
  (죽은 공대 청소). 24시간 TTL 같은 건 없다 — 링크가 살아 있는 한 이름이 항상 보여야
  "이름 보이면 정상" 진단이 성립한다.

### 2.2 state 스키마

```json
{ "v": 1, "name": "로사단", "rows": [ { "n": "눈가루", "g": 90000 }, ... ], "t": 1756... }
```

- `name` — 방(공대) 이름. 오버레이 머리에 표시.
- `rows` — 실명 행 전부(0골 포함). `(이름입력n)` 자리표시 행은 제외. `g`는 골드 정수
  (항목×단가 + 기타, 앱의 정산과 같은 계산).
- 서버는 스키마를 해석하지 않는다. 저장·중계만.

## 3. 공유 페이지 (워커가 서빙, 단일 HTML)

같은 주소의 두 얼굴. `window.obsstudio` 존재 또는 `?mode=overlay` → 오버레이,
아니면 현황판. `?mode=page` 로 강제 가능.

### 3.1 공통 렌더

- **정렬: 금액 내림차순, 동률은 원래(표) 순서 유지.** 0골도 항상 표시(살짝 흐리게).
- **순위 변동 시 FLIP 애니메이션** — 줄이 미끄러져 자리를 바꾼다(transform 0.35s).
  `prefers-reduced-motion: reduce`면 끔.
- 머리: 공대 이름(`state.name`). 금액 표기는 앱과 같은 만 단위(`9만`, `34만`, 0은 `0`).
- WebSocket 구독, 끊기면 1s→2s→…→15s 백오프 재접속. dead 방은 재접속 안 함.
- 50초마다 `ping` (하이버네이션 자동 응답이 받아줌).

### 3.2 오버레이 모드

- 배경 투명, vw 단위 글자 크기 → OBS 소스 크기에 맞춰 자동 스케일.
- 테마 파라미터:
  - `t=clear` (기본) 투명·흰 글씨·강한 검은 외곽선 / `t=cleardark` 투명·검은 글씨·흰 외곽선
  - `t=dark` 검은 판·흰 글씨 / `t=light` 흰 판·검은 글씨 — 판 투명도 `bg=20~100`(%)
  - `s=NN` 글자 크기 배율(%)
- **상태가 없거나 방이 죽었으면 아무것도 그리지 않는다.** 예외 하나: `obsstudio.getStatus`로
  방송·녹화 중이 아님이 **확인될 때만** 안내 한 줄 표시(아래 문구). 조회 실패·권한 없음 →
  방송 중일 수 있다고 보고 침묵. (getStatus가 기본 페이지 권한으로 되는지 구현 때 실측)

### 3.3 현황판 모드 (일반 브라우저)

- 최상단 배너(항상): `읽기 전용 화면이에요 — 기록은 장부를 쓰는 사람(서기)만 할 수 있어요.`
- 제목=공대 이름, 부제: `{n}명 · 총 {총액}골드 · 실시간`
- 죽은 방: 제목 `끝난 주소예요`, 본문
  `이 주소는 더 이상 갱신되지 않아요.` / `서기에게 새 주소를 받아 OBS 소스의 URL만 바꿔주세요.`
- 빈 방: `아직 기록이 없어요. 장부가 채워지면 여기 실시간으로 보여요.`
- **[OBS용 주소 만들기]** 섹션: 테마 4종 버튼 + 판 투명도 슬라이더(판 테마일 때만) +
  완성된 주소 + [복사]. 선택은 localStorage(`ovPref`)에 기억 — 주소가 바뀌어도 두 클릭 재발급.
  안내문: `이 주소를 OBS 브라우저 소스에 붙여넣으면, 여기 보이는 현황판이 방송용 오버레이로 떠요.`

오버레이 안내 문구(3.2 예외):
`이 주소는 더 이상 갱신되지 않아요. 서기에게 새 주소를 받아 URL만 바꿔주세요.`

## 4. 앱 연동 (src/GoldSettlement.jsx)

### 4.1 저장 구조 (localStorage, 표 데이터와 분리)

```js
"goldSettlement.relay" = {
  on: false,                  // 공유 켜짐 여부
  rooms: { "로사단": {roomId, key}, "기본": {...} },   // 명단 이름 → 방
  active: "로사단"            // 지금 송출 대상 명단 ("기본" = 프리셋 없이 쓸 때)
}
```

- 프리셋을 불러오면 `active`가 그 이름으로 바뀐다. 프리셋 없이 쓰면 `"기본"`.
- 방이 없는 명단으로 공유하려 하면 모달에서 [이 명단의 방 만들기].
- `active`가 바뀌었는데 공유가 켜져 있으면: 새 명단의 방이 있으면 자동 전환,
  없으면 공유 일시정지 + 모달 유도. 어느 쪽이든 **어느 방으로 나가는 중인지 항상 보이게**.

### 4.2 'OBS로 공유' 모달 (툴바 버튼 추가)

- 켜기/끄기 토글. 켜져 있으면 버튼에 표시(예: 점).
- 현재 명단 이름 + 공유 주소 + [주소 복사].
- **[파티원에게 보낼 메시지 복사]** — 템플릿 (문구 그대로):

  ```
  [벌금 현황판] {공대명}
  {공유 주소}
  · OBS 브라우저 소스에 이 주소를 넣으면 오버레이가 떠요
  · 브라우저로 열면 자세한 현황을 볼 수 있고, OBS용 테마도 고를 수 있어요
  ```

- 공대 이름 수정 칸 (기본값 = 프리셋 이름 또는 `벌금 현황판`).
- [주소 새로 발급] — 새 방 생성 → 매핑 교체 → 옛 방 kill. 확인창:
  제목 `주소를 새로 발급할까요?`
  본문 `지금 주소는 바로 못 쓰게 돼요. 파티원들에게 새 주소를 다시 보내야 해요.`
- **예비 열쇠**: `이 브라우저가 방의 열쇠예요. 잃어버릴 때를 대비해 다른 기기(PC·폰)를 예비
  열쇠로 등록해 두세요.` + [코드 발급] → 6자리 표시(5분 유효) / 받는 쪽엔 [코드로 열쇠 받기]
  입력칸. 꾸러미 = `relay` 전체(JSON). 받으면 명단별로 병합(같은 이름은 받은 쪽이 이김).
  두 기기가 다 열쇠를 가진다 — 물러남 없음, 동시 쓰기는 마지막 쓰기 우선.
- 테마 안내 줄: `테마는 공유 주소를 브라우저로 열면 고를 수 있어요.`

### 4.3 동기화

- 트리거: 공유 ON 상태에서 rows·cols·fee·extras가 바뀔 때. **디바운스 300ms.**
- 페이로드: 2.2 스키마. `rows`는 실명 행 전부(0골 포함, `(이름입력n)` 제외),
  `g = max(0, 항목합 + 기타합)` — 앱의 정산 계산(`cellGold` 경유)과 동일해야 한다.
- `fetch PUT /api/r/:id/state`. 실패는 조용히 무시하고 다음 변경 때 재시도
  (마지막 스냅샷 재전송 타이머 하나 정도는 둬도 좋다). 오프라인이어도 기록은 계속.
- '처음부터 다시해!!' → 0골 8명이 push됨(정상 — 새 판 시작이 그대로 방송에 보임).

### 4.4 첫 실행 안내

빈 첫 화면(저장 데이터 없음)에 한 줄:
`쓰던 방이 있었는데 사라졌다면 — 예비 열쇠 기기에서 [코드로 열쇠 받기]를 하거나, 새 주소를 만들어 다시 공유하세요.`
(모드 선택 인트로를 해치지 않는 위치로. 문구는 유지하되 배치는 구현 재량)

## 5. 검증 (배포 전, 전부 로컬)

1. `cd worker && npx.cmd wrangler dev --port 8787` (PowerShell은 npx.cmd).
2. API: 방 생성 → state PUT → peek 일치 → 나쁜 열쇠 403 → kill 후 410/dead.
3. WS: node(전역 WebSocket)로 구독 → 접속 즉시 스냅샷 → PUT마다 push → kill 시 dead.
4. 페이지: 브라우저 프리뷰로 `/r/:id` — 현황판 배너·정렬·FLIP(순위 뒤집는 PUT으로),
   `?mode=overlay&t=` 4종 + bg, 0골 흐림, dead 화면, 재접속(dev 재시작).
5. obsstudio 모의: `window.obsstudio={getStatus:cb=>cb({streaming:false})}` 주입 후
   오버레이 모드 dead 안내 표시/침묵 분기.
6. 핸드오프: 발급 → 수령 → 재수령 404 → 만료(코드상 시간 조작) → 30회 실패 429.
7. 앱: 두 탭(앱 + 현황판)으로 카운터 연타 → 300ms 배칭 확인, 프리셋 전환 → 방 전환 표시,
   주소 재발급 → 옛 탭 dead 화면. 기존 로직 배터리 ALL PASS 유지.
8. 배포는 사용자 지시 후: `cd worker && npx.cmd wrangler deploy` → 앱의 릴레이 호스트 상수를
   `live.lostark-sheet.workers.dev`로 → 앱 빌드·배포도 별도 지시.

## 6. v1에서 하지 않는 것

파티원 쓰기(제안·승인 포함) / 로그인·비밀번호 / OBS 상호작용 창 테마 픽커 /
'정산 완료' 상태 표시 / 현황판의 항목별 내역(이름+총액만) / 방 목록·관리 화면 /
서버측 백업·이력. — 전부 논의 끝에 뺀 것이므로 임의로 추가하지 않는다.
